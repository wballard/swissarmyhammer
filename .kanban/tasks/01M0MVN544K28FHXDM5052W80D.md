---
assignees:
- claude-code
comments:
- actor: claude-code
  id: 01m0nb806n374f8364gw9j7579
  text: |-
    Research done. Measured every one of the eleven proposed bullets against the shipped Swift tool gates before writing a word. swiftformat 0.62.1, swiftlint 0.65.0.

    Method: pulled the `tool.run` block out of each shipped rule file into a runnable script, then ran `idioms-swift`, `disallowed-constructs-swift`, `function-length-swift`, `magic-numbers-swift` and `missing-docs-swift` over a DON'T/DO probe pair for each bullet. `dead-code-swift` needs a built SPM package and decides unused declarations, which is a different question.

    Result: 22 probes, 0 findings from all four file-scope gates. The harness is live, not broken — a sanity file holding `value!` and `Optional<Int>` draws `force_unwrapping` from `disallowed-constructs-swift` and `typeSugar` from `idioms-swift`.

    ONE of the eleven IS tool-decidable and will NOT be written as a prompt bullet:

    - "Keep dynamic properties (`@State`, `@StateObject`) `private`". Measured over one probe holding `@State var counter` and `@StateObject var model` beside their private forms: swiftformat `privateStateVariables` reports both bad lines and stays silent on the good ones; swiftlint `private_swiftui_state` reports the same two lines with `SwiftUI state properties should be private`. Neither rule stands in a shipped roster today. `builtin/validators/swift/VALIDATOR.md` states "If a tool can decide it, the tool owns it", so the prompt bullet keeps the memberwise-init half alone and names the dynamic-property half as a carve-out. A card follows for the roster.

    Two more bullets are SPLIT, and each says which part the tool misses:

    - "Set every stored property at `init` time" — swiftlint `implicitly_unwrapped_optional` owns the `Type!` spelling. Measured: it says nothing about `private var session: Session?` a later `configure()` assigns. The bullet reads that shape.
    - "A callback closure that branches names a method" — swiftlint `closure_body_length` owns the LENGTH at 250 lines. Measured: silent on a 10-line branching closure.

    The autocorrect lesson from ^qs32yvp applies to the immutability bullet. Measured: `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`. The tool's own fix LANDS ON an accumulator loop, so the DON'T names the `for` + `append` shape, not the `forEach` shape it started from.

    swiftlint 0.65.0 knows no `early_exit` rule — `swiftlint rules early_exit` answers `Error: No rule with identifier: early_exit`. The guard-precondition bullet has no tool owner at all.
  timestamp: 2026-08-22T18:20:34.901382+00:00
- actor: claude-code
  id: 01m0nbx4ca1c0jjd0e7pr2zqx6
  text: |-
    Implementation landed.

    NEW `builtin/validators/swift/rules/initialization.md` — 4 bullets: `init` does no slow work; `init` sets every stored property (splits with swiftlint `implicitly_unwrapped_optional`); a property observer that branches calls a named method; a callback closure that branches calls a named method (splits with swiftlint `closure_body_length` at 250 lines).

    NEW `builtin/validators/swift/rules/immutability.md` — 3 bullets: build a collection with `map`/`compactMap`, not a `var` accumulator; a `static` member is a `let` or a computed `var`; a function belongs to a type.

    NEW `builtin/validators/swift/rules/preconditions.md` — 2 bullets: validate a precondition with `guard` at the head of a production scope; an unexpected but recoverable condition gets `assertionFailure` AND a production log.

    ADDED to `naming-clarity.md` — an event-handling method reads as a past-tense sentence.

    ADDED to `access-control.md` — a SwiftUI view keeps its input properties `internal` and its dynamic properties `private`. PLACEMENT: this went to `access-control.md`, not to `value-semantics.md` and not to a new `swiftui.md`. It IS an access-control requirement, `value-semantics.md` is about struct-versus-class, and one SwiftUI bullet is under the three-bullet threshold the card set for a file of its own.

    The accumulator bullet names the loop in BOTH shapes, because of the autocorrect lesson from ^qs32yvp: the `where` form and the nested-`if` form. Measured, `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`, so the nested-`if` accumulator is where the tool's own fix LANDS. A bullet that named only the `forEach` shape would walk the author into a hole.

    ## The fixture pair, and why it is not a `.tmpl` pair

    `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism — "Each tool rule ships two fixture files in the set's `fixtures/` directory" — and doctor materializes them to run a tool over them. A prompt rule has no tool to run, so no `.tmpl` file appears and `crates/mirdan/src/builtin_validators.rs` needs no edit. Checked: no roster in that file, or in any other Rust file, names a Swift prompt rule.

    The measurable half of the contract IS shipped, as `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `shipped.rs`. It carries a DON'T probe and a DO probe for each of the five rules, and two tests:

    - `every_swift_judgment_rule_states_the_forms_its_probes_hold` — 26 forms, each held to standing in the body of the prompt rule that states it AND in one of the two probes. Without it the probes would measure a house style nothing ships.
    - `no_shipped_swift_gate_decides_a_swift_judgment_rule` — drives the shipped script of all five file-scope Swift gates over both probes of each rule. 10 probes, 50 runs, 0 findings. The DON'T half proves ONE owner; the DO half proves the prompt rule does not walk the author into a tool finding.

    RED verified by hand for both, then reverted: a form spelled `func neverStatedAnywhere()` fails the first; a force unwrap written into the naming-clarity DO probe fails the second with `` `disallowed-constructs-swift` reports the shape `naming-clarity.md` ASKS for ... the run reported ["force_unwrapping"] ``.

    ## Token cost

    Measured with `tiktoken` `cl100k_base` over the shipped files:

    | the file | tokens |
    |---|---|
    | `initialization.md` (new) | 542 |
    | `immutability.md` (new) | 457 |
    | `preconditions.md` (new) | 356 |
    | `naming-clarity.md` (delta) | 93 |
    | `access-control.md` (delta) | 156 |
    | rule bodies, total | **1604** |
    | `VALIDATOR.md` (delta) | 216 |
    | everything, total | **1820** |

    Set against the 217 tokens the supersede card saved: the rule bodies add 7.4 times that. Eleven bullets is what the cost buys.

    ## Follow-up card

    ^w54rk97 — give the SwiftUI private-state half a tool owner. Both swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` decide it cleanly and neither stands in a shipped roster; the card carries the measurement, the roster edit, and the note that `no_shipped_swift_gate_decides_a_swift_judgment_rule` will fail loudly the moment the roster takes the rule, which is how the two-owner state is caught.
  timestamp: 2026-08-22T18:32:07.306192+00:00
- actor: claude-code
  id: 01m0nc07kc9grde2h5aejkaz0h
  text: |-
    ### implement — changed
    - evidence: 9 files. NEW `builtin/validators/swift/rules/initialization.md`, `builtin/validators/swift/rules/immutability.md`, `builtin/validators/swift/rules/preconditions.md`; EDITED `builtin/validators/swift/rules/naming-clarity.md`, `builtin/validators/swift/rules/access-control.md`, `builtin/validators/swift/VALIDATOR.md`; NEW `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`; NEW card `.kanban/tasks/01M0NBVN6Z77DS4FZBXW54RK97.md`.
    - eleven bullets: 4 initialization, 3 immutability, 2 preconditions, 1 naming-clarity, 1 access-control.
    - tool check: every bullet measured against the five shipped file-scope Swift gates before it was written. 10 probes, 50 runs, 0 findings. ONE bullet-half rejected as tool-decidable — the SwiftUI dynamic-property `private` — and card ^w54rk97 carries the roster edit.
    - token cost: 1604 tokens of rule body, 216 more of VALIDATOR.md prose, 1820 in all, measured with `tiktoken` `cl100k_base`. The supersede card saved 217.
    - fixture pair: realized as measured probe pairs, NOT `.tmpl` files. `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism ("Each tool rule ships two fixture files"), and a prompt rule has no tool for doctor to run over one. `no_shipped_swift_gate_decides_a_swift_judgment_rule` holds each pair. No `.tmpl` file appears, so `crates/mirdan/src/builtin_validators.rs` needs no edit; checked, and no roster in any Rust file names a Swift prompt rule.
    - tests: `cargo nextest run` at the repo root — 14186 tests run, 14186 passed, 0 failed, 0 skipped. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.
    - RED verified for both new tests, then reverted.
    - next: `/review`.
  timestamp: 2026-08-22T18:33:48.908100+00:00
- actor: claude-code
  id: 01m0ncad2hzacp4faawa9byw4c
  text: |
    ### test — green
    - evidence: `cargo nextest run --workspace` — 14186 tests run, 14186 passed, 0 failed, 0 skipped (29 slow, 1 leaky). `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean (0 warnings).
    - Baseline was 14184, this run is 14186 (+2), matching the implementer's claim exactly. No drop.

    **Confirmation 1 — new test file registered and running.**
    `mod swift_judgment_rules;` is present in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`. Targeted run `cargo nextest run --workspace -E 'test(swift_judgment_rules)'` shows both tests executing and passing:
    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::every_swift_judgment_rule_states_the_forms_its_probes_hold` — PASS
    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::no_shipped_swift_gate_decides_a_swift_judgment_rule` — PASS
    These 2 tests account for the +2 over baseline.

    **Confirmation 2 — no `.tmpl` fixtures, mirdan roster unchanged.**
    `git status --porcelain` shows no new `.tmpl` files (only new `.md` rule files and the new test file). `git diff HEAD -- crates/mirdan/src/builtin_validators.rs` is empty — the file is genuinely unchanged. Its coverage-guard tests still pass: `cargo nextest run --package mirdan -E 'test(builtin_validator) or test(fixture)'` — 9/9 passed, including `test_every_builtin_validator_file_is_embedded` and `test_each_set_has_a_manifest`, which would catch a stale roster against the new on-disk `.md` files.

    **Confirmation 3 — new rule files are loaded at runtime and part of the `swift` set.**
    `swift_prompt_rule_body()` in `shipped.rs` calls `loader.get_ruleset(SWIFT_VALIDATOR_SET)` (`SWIFT_VALIDATOR_SET = "swift"`) built by `builtin_loader()` (`crates/swissarmyhammer-validators/src/review/test_support.rs`), which calls `crate::load_builtins(&mut loader)` — a real disk read via `builtin_validators_dir()`, not a compiled-in fixture. It then does `.rules.iter().find(|shipped| shipped.name == rule)`, panicking `"the swift set must carry a {rule} prompt rule"` if missing. `every_swift_judgment_rule_states_the_forms_its_probes_hold` calls this for `initialization`, `immutability`, and `preconditions` (plus `naming-clarity`, `access-control`) and passed, proving all three new rule files load into the `swift` set at runtime. `builtin/validators/swift/VALIDATOR.md` also lists `initialization`, `immutability`, `preconditions` by name in its rule listing.
  timestamp: 2026-08-22T18:39:22.193815+00:00
- actor: claude-code
  id: 01m0ncd2z4szb0asgbs315p22h
  text: |-
    ### commit — changed
    - evidence: 379079d84 feat(validators): add eleven Swift rule bullets that no tool can judge
    - next: proceed to review
  timestamp: 2026-08-22T18:40:50.148381+00:00
- actor: claude-code
  id: 01m0ncwamqaw3jmd9m26b2rgrn
  text: |-
    ### review — findings
    - evidence: 8 open findings — immutability.md:8 (x2), initialization.md:9, naming-clarity.md:14, preconditions.md:9, VALIDATOR.md:44, swift_judgment_rules.rs:401, swift_judgment_rules.rs:394. Engine covered 2 of 14 files (both .rs) and produced 1 finding; all 6 markdown files went unread, so 7 of the 8 come from hand measurement with swiftformat 0.62.1 and swiftlint 0.65.0 over the shipped gate scripts. Reproduction 4 logged on ^dyad426.
    - confirmed by hand, not restated as findings: 10 probes x 5 file-scope gates = 50 runs, 0 findings, run twice (no .swift-version and 6.3) for 100 runs total; implicitly_unwrapped_optional reports `Session!` and not `Session?`; closure_body_length stands at 250 in function-length-swift; preferForLoop's autofix writes the exact second DON'T of immutability.md; privateStateVariables and private_swiftui_state each report the `@State var` probe and neither stands in a shipped roster.
    - next: fix the two-owner overlaps (immutability.md vs idioms.md, preconditions.md vs error-handling.md), narrow the initialization.md bold claim, add the two unmeasured forms to the probes, stage a .swift-version, and extract the shared test helper.
  timestamp: 2026-08-22T18:49:09.527427+00:00
- actor: claude-code
  id: 01m0ncxmx7kg0dggs8vx5d63jk
  text: |-
    ### finish iteration 1 — findings
    - implement: changed — 9 files; 11 bullets across 5 rule files, 3 of them new
    - test: green — cargo nextest run --workspace, 14186 passed, 0 failed, 0 skipped; fmt clean; clippy clean
    - commit: 379079d84
    - review: findings — 8 open. Engine covered 2 of 14 files and found 1; the other 7 came from hand measurement over the 6 markdown files the engine never opened.
    - engine bug: reproduction 6 on ^dyad426, plus a SECOND arithmetic fault — `counts.skipped` reads 0 beside a `skipped_files` list of 6.

    **The implementer's evidence held up under independent check.** The reviewer ran all ten probes rather than the three spot-checks asked for: 50 gate runs, 0 findings, then 50 more with `.swift-version` 6.3, still 0. Both splits are real (`implicitly_unwrapped_optional` owns `Session!` and is silent on `Session?`; `closure_body_length` stands at 250). The rejected bullet-half holds on both halves. The autocorrect lesson was correctly applied to `immutability.md`.

    **But the two-owner risk fired twice, both on the prompt side the engine cannot see.**

    1. **`immutability.md:8` contradicts `idioms.md:14`.** `immutability.md`'s first DON'T is `for user in users where user.isActive { names.append(user.name) }`. `idioms.md` writes that same loop shape as its **DO**. Both name the nested-`if` form as a DON'T, with different prescribed fixes. So an author who obeys `idioms.md`'s DO lands word for word on `immutability.md`'s DON'T. `idioms.md` carries no carve-out.

       This is the `preferForLoop` autocorrect hole reproduced between two PROMPT rules instead of between a tool and a prompt. The defect class outlived the tool that taught it to us.

    2. **`preconditions.md:9`'s `fatalError` DON'T is already owned by `error-handling.md:10`.** Straight duplication.

    **One evidence weakness worth naming.** `swift_judgment_rules.rs:401` stages no `.swift-version`, so the 50 runs measured `idioms-swift` with five rules disarmed. Proved: 0 findings without it, 3 `conditionalAssignment` findings with it. The conclusion still holds — the reviewer re-ran with 6.3 — but the shipped test does not prove what it appears to.
  timestamp: 2026-08-22T18:49:52.807305+00:00
- actor: claude-code
  id: 01m0ndvw9t169ak0442vz5cehs
  text: |-
    ## The DO-versus-DON'T sweep — the deliverable of this iteration

    Finding 1 is a defect CLASS, not one bullet: one shape read by two rules that prescribe different fixes, so obeying one lands the author word for word on the other's DON'T. It is the `preferForLoop` autocorrect hole reproduced between two PROMPT rules. The class outlived the tool that taught it to us.

    So every pair of the 14 Swift prompt rules was read, DO against DON'T. Nine pairs share a shape. Here is what stands after the sweep.

    ### Three collisions, all fixed

    1. **`idioms.md` DO -> `immutability.md` DON'T.** `for thing in things where thing > 2 { print(thing) }` is `idioms.md`'s DO; `for user in users where user.isActive { names.append(user.name) }` is `immutability.md`'s first DON'T — the same loop shape, opposite fixes. FIXED with mirrored boundary sentences. `idioms.md`: "A loop whose body only appends into a collection belongs to `immutability.md`, and its fix is `map`/`filter` rather than a `where` clause." `immutability.md`: "A loop whose body DOES something for each element it keeps belongs to `idioms.md`, and its fix is a `where` clause rather than `map`/`filter`." Neither rule was weakened: each keeps every DON'T it had.

    2. **`preconditions.md` bullet 1's DO -> `preconditions.md` bullet 2's DON'T. Inside ONE file.** Bullet 1's DO is `guard isConnected else { return }`. Bullet 2's DON'T was "a bare `else { return }` that records nothing". The sweep found this; no review finding named it. FIXED: bullet 2 now names its scope — "This reads the condition the code did not expect ... A requirement the caller is expected to miss is the bullet above, and its bare `else { return }` is right there."

    3. **`error-handling.md` DON'T -> `preconditions.md` DO.** `error-handling.md` refuses `-1`/`""` sentinels; `preconditions.md`'s DO returns `""`. FIXED with one clause: "That `""` is the default the log names, not a sentinel that hides a reason." The distinction is real — error-handling refuses a return that HIDES the reason, and this one logs it.

    ### One duplication, carded

    4. **`naming-clarity.md` `Color.colorRed` and `idioms.md` `static let redColor`.** Two rules, one requirement, the SAME fix — so no author is walked astray and no review round churns. It is still two owners, which `VALIDATOR.md` refuses. Carded as ^f1s9bx5 rather than fixed here: both bullets predate this card, and narrowing either needs its own gate measurement.

    ### Five pairs checked and CLEAR, with the reason

    5. **`optionals.md` "never `guard` in a test" against `preconditions.md` "`guard` at the head of a production scope".** Both rules already name the boundary, in both directions. This pair is the MODEL the other two were fixed to, and it is now row three of `SWIFT_SHARED_SHAPES` so it cannot decay.
    6. **`access-control.md` DON'T `private let title: String` against `initialization.md` DO `private let session: Session`.** Scope stated by the bold claim — "A SwiftUI view keeps its input properties `internal`". A plain class is not read.
    7. **`concurrency.md` DON'T `@escaping … completion` against `initialization.md`'s callback DO.** `concurrency.md` scopes itself to "new code ... at a public boundary"; `initialization.md` decides what goes INSIDE a closure the author already has, and its probe's `reload(loader:)` is internal.
    8. **`fluent-usage.md` DO `x.append(y)` against `immutability.md` DON'T `names.append(user.name)`.** `fluent-usage.md` decides the NAME of an operation you define; `immutability.md` decides whether the call belongs in a loop. Different questions.
    9. **`optionals.md` "`nil` is genuine absence" against `initialization.md`'s bold claim.** This WAS a collision — it is review finding 3 — and the narrowed bold claim fixes it. Now row two of `SWIFT_SHARED_SHAPES`.

    `doc-parameter-naming.md`, `casing.md`, `state-modeling.md` and `value-semantics.md` share no shape with any other rule. `casing.md` and `doc-parameter-naming.md` each already carry their own bidirectional churn guards.

    ### The sweep is now shipped, not remembered

    Two tests in `swift_judgment_rules.rs`, both costing zero tool runs:

    - `every_shape_two_swift_prompt_rules_read_states_the_boundary_in_both` — 3 rows, each holding BOTH rules to stating their half of the boundary. A rule that drops its half fails by name.
    - `every_swift_requirement_two_prompt_rules_could_state_stands_in_one_of_them` — holds `error-handling.md` to STATING the `fatalError` bullet, `preconditions.md` to writing no `fatalError(` at all, and `preconditions.md` to naming its owner.

    RED verified by hand for both, then reverted. Deleting the `idioms.md` carve-out fails the first; writing `fatalError(` back into `preconditions.md` fails the second with "`preconditions.md` writes `fatalError(`, so `preconditions.md` is a second owner of that requirement and one line draws two findings".

    ## The other findings

    **Findings 2, 3, 6** — `preconditions.md` no longer names `fatalError` (only the positive requirement plus the silent `else { return "" }` stay); `initialization.md`'s bold claim is now "**No stored property waits for a later method to fill it in.**" with its frontmatter description matched; `VALIDATOR.md` names the five FILE-SCOPE gates by name, states the `.swift-version` of `6.3` every run stood under, and says why `dead-code-swift` stands outside.

    **Findings 4 and 5** — `func settingsButtonDidTap()` now stands in `SWIFT_NAMING_CLARITY_ANSWER` under a `SettingsScreen`, and the `where` accumulator now stands in `SWIFT_IMMUTABILITY_REFUSAL` under `Roster` beside the nested-`if` shape under `Directory`. Both added to `forms`. RED verified: dropping either fails `every_swift_judgment_rule_states_the_forms_its_probes_hold` by name.

    **Finding 7 — the evidence defect.** `swift_judgment_gate_rules` now stages `SWIFT_VERSION_SUPPORT`. Measured MYSELF, not taken from the review, with swiftformat 0.62.1 over one file holding `let value: Int` beside an exhaustive `if`/`else` that assigns it: **0 findings with no `.swift-version`, 3 `conditionalAssignment` findings with `6.3`**. The doc comment carries that measurement. All 50 runs still report 0 findings with the version staged, so the conclusion holds AND the test now proves it.

    **Finding 8 — the reuse.** `swift_gate_reporting_rules(gate, probe_path, source, support)` now lives in `shipped.rs` beside `SWIFT_VERSION_PATH`, `SWIFT_PROBE_VERSION` and `SWIFT_VERSION_SUPPORT`. All three call sites delegate: `swift_idioms_reporting_rules`, `swift_disallowed_reporting_rules`, `swift_judgment_gate_rules`. `idioms_swift.rs` lost its own copies of the three version constants.

    `disallowed_constructs_swift.rs` passes `NO_SUPPORT_FILES` on purpose, and says why: swiftlint reads no `.swift-version`, so staging one would state a condition that gate never reads. Its measured answers are unchanged.

    ## Token cost

    Measured with `tiktoken` `cl100k_base`, this working tree against `HEAD`.

    | the file | HEAD | now | delta |
    |---|---|---|---|
    | `idioms.md` | 470 | 507 | +37 |
    | `immutability.md` | 457 | 466 | +9 |
    | `preconditions.md` | 356 | 391 | +35 |
    | `initialization.md` | 542 | 584 | +42 |
    | rule bodies | | | **+123** |
    | `VALIDATOR.md` | 754 | 903 | +149 |
    | everything | 2579 | 2851 | **+272** |

    The whole `swift` set now costs 7216 tokens over 15 files. The 123 tokens of rule body buy four boundary statements that stop one rule walking an author onto another's DON'T; the 149 of `VALIDATOR.md` buy a measurement claim that matches what the shipped test measures.
  timestamp: 2026-08-22T19:06:23.418593+00:00
- actor: claude-code
  id: 01m0ndw899ndwe8n6734jyn29y
  text: |-
    ### implement — changed
    - evidence: 9 files. `builtin/validators/swift/rules/idioms.md`, `immutability.md`, `preconditions.md`, `initialization.md`, `builtin/validators/swift/VALIDATOR.md`; `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`, `.../shipped/swift_judgment_rules.rs`, `.../shipped/idioms_swift.rs`, `.../shipped/disallowed_constructs_swift.rs`. NEW card ^f1s9bx5.
    - all 8 review findings addressed and checked on the description.
    - DO-versus-DON'T sweep over all 14 Swift prompt rules: 3 collisions found (one of them NOT in any review finding — `preconditions.md` bullet 1's DO against bullet 2's DON'T, inside one file), all 3 fixed by scoping; 1 duplication with the same fix carded as ^f1s9bx5; 5 pairs checked and clear with the reason recorded. Full table in the comment above.
    - 2 NEW tests hold the sweep: `every_shape_two_swift_prompt_rules_read_states_the_boundary_in_both` (3 rows, both directions each) and `every_swift_requirement_two_prompt_rules_could_state_stands_in_one_of_them`. RED verified for both, then reverted. RED also verified for the two added probe forms.
    - no bullet was weakened. Every DON'T that stood before still stands; `preconditions.md` lost only the `fatalError` half that `error-handling.md` already owned.
    - tests: `cargo nextest run --workspace` — 14188 run, 14188 passed, 0 failed, 0 skipped (29 slow). Baseline 14186, +2 for the new tests. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.
    - token cost: +272 over HEAD (rule bodies +123, VALIDATOR.md +149), `tiktoken` `cl100k_base`. Whole `swift` set now 7216 tokens over 15 files.
    - next: `/review`.
  timestamp: 2026-08-22T19:06:35.689783+00:00
- actor: claude-code
  id: 01m0ne5js0fhdbe3ax353b38k7
  text: |-
    ### test — green
    - evidence: `cargo nextest run --workspace` — 14188 tests run, 14188 passed (29 slow), 0 skipped, 0 failed. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.
    - Baseline 14186 → 14188 (net +2, matching the two new boundary tests). No drop.
    - Confirmation 1 (shared helper, three Swift modules): ran each module in isolation.
      - `idioms_swift` — 12/12 passed, including `every_shipped_idioms_tool_rule_passes_its_fixtures` (holds the failing fixture at 42 findings / 21 rules and the passing fixture at 0).
      - `disallowed_constructs_swift` — 8/8 passed, including `every_shipped_disallowed_constructs_tool_rule_passes_its_fixtures` and the polarity/custom-rule tests (`..._reads_the_unchecked_sendable_directive`, `..._reads_a_test_target_the_manifest_names`, `..._reads_no_comment_and_no_string_literal`, `..._owns_each_bullet_it_took`).
      - `swift_judgment_rules` — 4/4 passed.
      - Confirmed by grep that `idioms_swift.rs::swift_idioms_reporting_rules` and `disallowed_constructs_swift.rs::swift_disallowed_reporting_rules` both delegate to the shared `swift_gate_reporting_rules` in `shipped.rs`.
    - Confirmation 2 (`.swift-version` staging, finding 7): confirmed `SWIFT_VERSION_SUPPORT` (`shipped.rs`) is genuinely consumed by `swift_judgment_gate_rules` in `swift_judgment_rules.rs` and by nine call sites in `idioms_swift.rs`. `no_shipped_swift_gate_decides_a_swift_judgment_rule` (50 gate runs: 5 rules × 5 file gates × refusal+answer probes) passed with it staged, 0 findings. Independently re-measured the underlying claim by hand with swiftformat 0.62.1 on a fresh probe (`let value: Int` beside an exhaustive if/else assigning it): 0 `conditionalAssignment` findings with no `.swift-version`, exactly 3 with `.swift-version` = `6.3` — matches the doc's claimed measurement exactly.
    - Confirmation 3 (rule-boundary sentences): both new tests exist and passed — `every_shape_two_swift_prompt_rules_read_states_the_boundary_in_both` and `every_swift_requirement_two_prompt_rules_could_state_stands_in_one_of_them`. Read the full source: the boundary test asserts `shape.boundary` in the owning rule's body AND `shape.neighbour_boundary` in the neighbour rule's body for every row of `SWIFT_SHARED_SHAPES` (immutability/idioms, initialization/optionals, preconditions/optionals) — both directions, both rules.
  timestamp: 2026-08-22T19:11:41.344805+00:00
position_column: doing
position_ordinal: '8280'
project: swift-validator
title: 'swift: add the ten judgment rules we are missing (init cost, immutability, preconditions, handler naming)'
---
The Airbnb skill has about ten rules that NO tool can decide and that we do not have. These are real judgment rules. They belong in `builtin/validators/swift/rules/`.

Source: https://swift.airbnb.tech/skill

## The rules

Group them into new files, or add them to the closest existing file. Suggested placement:

NEW `initialization.md`
- Do no meaningful or slow work in `init()`. No database connections, no network requests, no large disk reads. Move that work to an explicit `start()`/`load()` the caller invokes.
- Prefer initializing properties at `init` time over deferring them. (Pairs with the IUO rule that moves to SwiftLint.)
- Extract complex property observers (`didSet`/`willSet`) into methods. This reduces nesting and separates side effects from the property declaration.
- Extract complex callback blocks into methods, for the same reason.

NEW `immutability.md`
- Prefer immutable values. Build a collection with `map`/`compactMap`, not by appending to a `var` accumulator.
- Prefer stored `static let` or computed `static var` over mutable stored `static var`.
- Avoid global functions. Prefer a method inside a type.

NEW `preconditions.md` — or fold into `error-handling.md`
- Validate preconditions at the start of a scope with `guard`, not `if`. This keeps the happy path unindented. (Our `optionals.md` teaches this for optional binding only; the general precondition form is missing.)
- Handle an unexpected but recoverable condition with `assert` plus production logging, not a crash and not silence.

ADD to `naming-clarity.md`
- Name event-handling functions as past-tense sentences: `didTap`, not `handleTap`. Omit the subject when it is not needed for clarity.

ADD to `value-semantics.md` or a new `swiftui.md`
- For internal SwiftUI views, declare properties `internal` so the synthesized memberwise init is usable. Keep dynamic properties (`@State`, `@StateObject`) `private`.

## Style

Match the existing rules in this bundle. Each bullet is a bold claim, then DON'T, then DO, with real Swift. Short sentences. Every rule that fires must be fixable — review is binary pass/fail, with no advisory tier. Do not add a rule you are not willing to block on.

## We have no SwiftUI content anywhere

Check this before you write: the repo has no SwiftUI guidance in any validator, partial, or rule. The one bullet above is a start, not coverage. If it grows past three bullets, make `swiftui.md` its own file.

## Acceptance

- Each new rule has a fixture pair proving it fires on the bad form and stays quiet on the good form.
- `crates/mirdan/src/builtin_validators.rs` fixture roster updated if new `.tmpl` files appear.
- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names. #tool-validators

## Review Findings (2026-08-22 19:05)

> Scope: `review sha HEAD~1..HEAD` over commit `379079d84`.
>
> The engine reviewed 2 of the 14 changed files — both `.rs`. All six markdown files of this commit went unread (reproduction 4 logged on ^dyad426). Items marked `hand/` were measured by hand with swiftformat 0.62.1 and swiftlint 0.65.0 over the shipped gate scripts; the item marked `reuse/reuse` is the engine's.
>
> What the hand measurement CONFIRMED, so it is not restated as a finding: all 10 shipped probes over all 5 Swift file-scope gates report 0 findings, run twice — once with no `.swift-version` and once with `6.3` (100 runs, 0 findings), so the implementer's 50-run claim holds and is stronger than claimed. swiftlint `implicitly_unwrapped_optional` reports `private var session: Session!` and reports nothing on the plain `Session?`, so the `initialization.md` split is real. `closure_body_length` stands at 250 in `function-length-swift`, so that split is real. `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }` character for character, so `immutability.md` names the shape the autofix lands on. swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` each report `@State var isExpanded = false` at line 6 of the probe, and neither name stands in any shipped roster, so both halves of the rejected bullet-half claim hold. The five file-scope gates are the complete file-scope set; `dead-code-swift` is the only other Swift gate and its scope is `workspace`.

- [x] `builtin/validators/swift/rules/immutability.md:8` `hand/two-owners` — The first DON'T is `var names: [String] = []` beside `for user in users where user.isActive { names.append(user.name) }`. `idioms.md:14` states the DO for that same loop shape as `for thing in things where thing > 2 { print(thing) }`, and both rules name the nested-`if` form as a DON'T. So one changed line draws a finding from two prompt rules of the same `swift` set with different prescribed fixes, and an author who takes `idioms.md`'s finding and writes `idioms.md`'s own DO lands word for word on `immutability.md`'s first DON'T. This is the `preferForLoop` autocorrect hole reproduced between two prompt rules, and `idioms.md` was not edited to carve out an accumulator body. Give the shape one owner: state the accumulator carve-out in `idioms.md:14` so its DO never writes `immutability.md`'s DON'T, and hold both directions in a test the way `the_shipped_swift_idioms_tool_rule_decides_no_shape_of_the_where_half` holds the tool side.
- [x] `builtin/validators/swift/rules/immutability.md:8` `hand/unmeasured-form` — The bullet names TWO DON'T shapes and only the nested-`if` shape stands in a probe or in the `forms` list of `swift_judgment_rules.rs`. The `where`-clause accumulator shape, which the bullet names FIRST, is measured by nothing, so `every_swift_judgment_rule_states_the_forms_its_probes_hold` cannot catch it. Measured by hand: that shape draws 0 findings from all five gates, so the conclusion holds — but nothing ships that holds it. Add the shape to `SWIFT_IMMUTABILITY_REFUSAL` and to `forms`.
- [x] `builtin/validators/swift/rules/initialization.md:9` `hand/enforceability` — The bold claim is "**`init` sets every stored property.**" The body narrows it to "A property the type fills in later". Review is binary and the bold claim is what a reviewer applies, so as written the bullet fails a stored `var selected: Item?` that no `init` sets and that means genuine absence — which `optionals.md:10` sanctions in as many words ("`nil` should mean genuine absence") and which `state-modeling.md` builds on. Narrow the bold claim to the defect the body describes: a stored property a LATER method fills in.
- [x] `builtin/validators/swift/rules/naming-clarity.md:14` `hand/unmeasured-form` — The bullet names a third form, `func settingsButtonDidTap()`, as the DO where two sources send the same event. It stands in neither probe and in no `forms` entry, so no gate has ever been measured against it. Add it to `SWIFT_NAMING_CLARITY_ANSWER` and to `forms`.
- [x] `builtin/validators/swift/rules/preconditions.md:9` `hand/two-owners` — The DON'T is `guard let found = table[key] else { fatalError("no value for \(key)") }`. `error-handling.md:10` already owns that line: "**`fatalError`/`preconditionFailure` are for programmer error only, never a recoverable path.**" A missing table key is a recoverable path, so both rules fire on the same declaration. The cross-reference at the end of the bullet names the boundary but does not remove the second owner. Keep the half that is this bullet's alone — the positive requirement (`assertionFailure` AND a production log) and the bare `else { return }` that records nothing — and let `error-handling.md` keep the `fatalError` prohibition.
- [x] `builtin/validators/swift/VALIDATOR.md:44` `hand/measurement-claim` — The text states each new rule "was measured against every shipped Swift gate before it was written". Measured: the shipped test runs 5 of the 6 Swift gates (`dead-code-swift` is excluded, correctly, for the reason its own doc comment gives) and it runs `idioms-swift` with five of its swiftformat rules silent, per the next finding. State what was measured: the five FILE-SCOPE gates, and name the `.swift-version` condition the run stands under.
- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs:401` `hand/disarmed-gate` — `ShippedStaging::of(&[(SWIFT_JUDGMENT_PROBE_PATH, source)])` stages the probe with no `.swift-version`. `idioms_swift.rs` stages one on purpose and says why: five enabled swiftformat rules read the Swift language version and stay silent without it. So this test's 50 runs measure `idioms-swift` with `preferCountWhere`, `environmentEntry`, `genericExtensions`, `opaqueGenericParameters` and `conditionalAssignment` disarmed. Measured on the shipped script over one file holding `let value: Int` beside an exhaustive `if`/`else` that assigns it: 0 findings with no `.swift-version`, 3 `conditionalAssignment` findings with `.swift-version` holding `6.3`. Re-running all 50 with `6.3` still gives 0 findings, so today's conclusion survives — but the test does not prove it, and the next bullet added to `SWIFT_JUDGMENT_RULES` will be measured against a disarmed gate. Stage a `.swift-version` the way `idioms_swift.rs` does.
- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs:394` `reuse/reuse` — The function `swift_gate_reporting_rules` reimplements logic that already exists in nearly identical form in other test modules. Consolidating this into a shared helper function would keep one canonical version that can be improved once, instead of maintaining three parallel copies across different test modules. Extract a shared test helper function in the test support infrastructure (e.g., `shipped.rs` or a new shared test utilities module) that both the existing and new test modules call, parameterized by gate name and source code. This eliminates the duplication and gives all test modules a single implementation to maintain.