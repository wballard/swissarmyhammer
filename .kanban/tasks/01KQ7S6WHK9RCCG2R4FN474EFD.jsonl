{"id":"01KQ7S6WHM2D08D75D1S95Y5HK","timestamp":"2026-04-27T15:33:59.988500Z","op":"create","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,186 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ab80\n+project: spatial-nav\n+title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n+---\n+## What\n+\n+The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n+\n+### The policy (per user, 2026-04-27)\n+\n+**Edge drill-out + zone-to-zone traversal.**\n+\n+When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n+\n+1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n+2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n+3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n+\n+This collapses three things that are separate today:\n+- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n+- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n+\n+### The user trajectories the policy must satisfy\n+\n+These three trajectories are the regression spec — each must work and each was reported broken at least once:\n+\n+**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n+1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n+4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n+\n+**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n+\n+**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n+\n+From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n+\n+### Files in scope\n+\n+**Kernel (the actual fix):**\n+\n+- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n+\n+  ```rust\n+  fn next(&self, registry, focused, direction) -> Option<Moniker> {\n+      let mut current = registry.entry(focused)?;\n+      loop {\n+          // Score same-level peers (siblings with matching parent_zone, same kind).\n+          if let Some(target) = beam_among_siblings(registry, current, direction) {\n+              return Some(target);\n+          }\n+          // Escalate to parent zone, if any.\n+          let Some(parent_key) = current.parent_zone() else { return None; };\n+          let Some(parent) = registry.entry(&parent_key) else { return None; };\n+          current = parent;\n+      }\n+  }\n+  ```\n+\n+  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n+\n+  Override (rule 0) still runs first.\n+\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+\n+**React side (no behavior change, but tests assert the new policy):**\n+\n+- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n+\n+**Tests — kernel is authoritative, browser test is supplementary:**\n+\n+- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n+\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n+\n+### What this card replaces\n+\n+These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n+\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n+- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n+- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n+\n+### Internal contradictions this card resolves\n+\n+(Audit findings from the prior conversation.)\n+\n+1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n+3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n+5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n+\n+### What this card does NOT do\n+\n+- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n+- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n+- Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n+- Does not change override rule 0; overrides still run first.\n+\n+### Open question — answer before merge\n+\n+**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n+\n+## Acceptance Criteria\n+\n+- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n+  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n+- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n+- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n+- [ ] Override rule 0 still runs first and short-circuits the cascade.\n+- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n+- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n+- [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n+- [ ] `cd kanban-app/ui && npm test` is green.\n+\n+## Tests\n+\n+### Kernel integration tests (authoritative)\n+\n+`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n+\n+- One `LayerKey` \"window\" containing:\n+  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n+  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n+\n+Test cases:\n+\n+1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n+   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n+   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n+   - `nav(\"ui:navbar\", Up) == None`\n+\n+2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n+   - Mirror left from `task:T1B`.\n+\n+3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n+   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n+   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n+\n+### Browser test (wiring guard)\n+\n+`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n+\n+1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n+3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n+4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n+\n+### How to run\n+\n+```\n+cargo test -p swissarmyhammer-focus\n+cd kanban-app/ui && npm test\n+```\n+\n+Both must pass headless.\n+\n+## Workflow\n+\n+- Use `/tdd`. Order:\n+  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n+  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n+  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n+  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,186 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ab80\n-project: spatial-nav\n-title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n----\n-## What\n-\n-The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n-\n-### The policy (per user, 2026-04-27)\n-\n-**Edge drill-out + zone-to-zone traversal.**\n-\n-When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n-\n-1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n-2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n-3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n-\n-This collapses three things that are separate today:\n-- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n-- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n-\n-### The user trajectories the policy must satisfy\n-\n-These three trajectories are the regression spec — each must work and each was reported broken at least once:\n-\n-**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n-1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n-4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n-\n-**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n-\n-**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n-\n-From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n-\n-### Files in scope\n-\n-**Kernel (the actual fix):**\n-\n-- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n-\n-  ```rust\n-  fn next(&self, registry, focused, direction) -> Option<Moniker> {\n-      let mut current = registry.entry(focused)?;\n-      loop {\n-          // Score same-level peers (siblings with matching parent_zone, same kind).\n-          if let Some(target) = beam_among_siblings(registry, current, direction) {\n-              return Some(target);\n-          }\n-          // Escalate to parent zone, if any.\n-          let Some(parent_key) = current.parent_zone() else { return None; };\n-          let Some(parent) = registry.entry(&parent_key) else { return None; };\n-          current = parent;\n-      }\n-  }\n-  ```\n-\n-  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n-\n-  Override (rule 0) still runs first.\n-\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n-\n-**React side (no behavior change, but tests assert the new policy):**\n-\n-- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n-\n-**Tests — kernel is authoritative, browser test is supplementary:**\n-\n-- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n-\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n-\n-### What this card replaces\n-\n-These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n-\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n-- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n-- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n-\n-### Internal contradictions this card resolves\n-\n-(Audit findings from the prior conversation.)\n-\n-1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n-3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n-5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n-\n-### What this card does NOT do\n-\n-- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n-- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n-- Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n-- Does not change override rule 0; overrides still run first.\n-\n-### Open question — answer before merge\n-\n-**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n-\n-## Acceptance Criteria\n-\n-- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n-- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n-  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n-- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n-- [ ] Override rule 0 still runs first and short-circuits the cascade.\n-- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n-- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n-- [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n-- [ ] `cd kanban-app/ui && npm test` is green.\n-\n-## Tests\n-\n-### Kernel integration tests (authoritative)\n-\n-`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n-\n-- One `LayerKey` \"window\" containing:\n-  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n-  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n-\n-Test cases:\n-\n-1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n-   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n-   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n-   - `nav(\"ui:navbar\", Up) == None`\n-\n-2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n-   - Mirror left from `task:T1B`.\n-\n-3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n-   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n-\n-### Browser test (wiring guard)\n-\n-`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n-\n-1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n-3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n-4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n-\n-### How to run\n-\n-```\n-cargo test -p swissarmyhammer-focus\n-cd kanban-app/ui && npm test\n-```\n-\n-Both must pass headless.\n-\n-## Workflow\n-\n-- Use `/tdd`. Order:\n-  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n-  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n-  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","transaction_id":null}
{"id":"01KQ7S6WHNMMW8G9VXH4VY7G62","timestamp":"2026-04-27T15:33:59.989160Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"create","changes":[["body",{"kind":"set","value":"## What\n\nThe user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n\n### The policy (per user, 2026-04-27)\n\n**Edge drill-out + zone-to-zone traversal.**\n\nWhen `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n\nThis collapses three things that are separate today:\n- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n### The user trajectories the policy must satisfy\n\nThese three trajectories are the regression spec — each must work and each was reported broken at least once:\n\n**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n\n**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n\n**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n\nFrom any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n\n### Files in scope\n\n**Kernel (the actual fix):**\n\n- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n\n  ```rust\n  fn next(&self, registry, focused, direction) -> Option<Moniker> {\n      let mut current = registry.entry(focused)?;\n      loop {\n          // Score same-level peers (siblings with matching parent_zone, same kind).\n          if let Some(target) = beam_among_siblings(registry, current, direction) {\n              return Some(target);\n          }\n          // Escalate to parent zone, if any.\n          let Some(parent_key) = current.parent_zone() else { return None; };\n          let Some(parent) = registry.entry(&parent_key) else { return None; };\n          current = parent;\n      }\n  }\n  ```\n\n  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n\n  Override (rule 0) still runs first.\n\n- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n\n**React side (no behavior change, but tests assert the new policy):**\n\n- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n\n**Tests — kernel is authoritative, browser test is supplementary:**\n\n- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n\n- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n\n### What this card replaces\n\nThese four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n\n- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n\n### Internal contradictions this card resolves\n\n(Audit findings from the prior conversation.)\n\n1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n\n### What this card does NOT do\n\n- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n- Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n- Does not change override rule 0; overrides still run first.\n\n### Open question — answer before merge\n\n**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n\n## Acceptance Criteria\n\n- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n- [ ] Override rule 0 still runs first and short-circuits the cascade.\n- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n- [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n- [ ] `cd kanban-app/ui && npm test` is green.\n\n## Tests\n\n### Kernel integration tests (authoritative)\n\n`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n\n- One `LayerKey` \"window\" containing:\n  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n\nTest cases:\n\n1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n   - `nav(\"ui:navbar\", Up) == None`\n\n2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n   - Mirror left from `task:T1B`.\n\n3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n\n### Browser test (wiring guard)\n\n`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n\n1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n\n### How to run\n\n```\ncargo test -p swissarmyhammer-focus\ncd kanban-app/ui && npm test\n```\n\nBoth must pass headless.\n\n## Workflow\n\n- Use `/tdd`. Order:\n  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n"}],["assignees",{"kind":"set","value":["claude-code"]}],["project",{"kind":"set","value":"spatial-nav"}],["position_ordinal",{"kind":"set","value":"ab80"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards"}]]}
{"id":"01KQ7SCFNF7WYQ2SHGCBA1205G","timestamp":"2026-04-27T15:37:03.407801Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -8,7 +8,7 @@\n ---\n ## What\n\n-The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n+The user has reported five spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n\n ### The policy (per user, 2026-04-27)\n\n@@ -22,26 +22,36 @@\n\n This collapses three things that are separate today:\n - **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n - **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n ### The user trajectories the policy must satisfy\n\n-These three trajectories are the regression spec — each must work and each was reported broken at least once:\n+These four trajectories are the regression spec — each must work and each was reported broken:\n\n **Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n 1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n-4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n+4. Up → `ui:navbar`.\n\n **Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+\n+**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+\n+**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n+- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n+- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n\n-**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n+Pressing Up/Down from a Field zone:\n+- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n+- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n+- Up mirror.\n+- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n\n-From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n+This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n\n ### Files in scope\n\n@@ -57,9 +67,11 @@\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any.\n+          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n+          // Hard layer boundary: never cross between layers via escalation.\n+          if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n   }\n@@ -69,7 +81,7 @@\n\n   Override (rule 0) still runs first.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n\n **React side (no behavior change, but tests assert the new policy):**\n\n@@ -77,28 +89,27 @@\n\n **Tests — kernel is authoritative, browser test is supplementary:**\n\n-- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n+- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n\n ### What this card replaces\n\n-These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n+These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n - `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n-- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n+- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n+- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n\n ### Internal contradictions this card resolves\n\n-(Audit findings from the prior conversation.)\n-\n-1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n-3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n-5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n+1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n+3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n+4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n\n ### What this card does NOT do\n\n@@ -105,7 +116,8 @@\n - Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n - Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n-- Does not change override rule 0; overrides still run first.\n+- Does not change override rule 0.\n+- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n\n ### Open question — answer before merge\n\n@@ -114,17 +126,20 @@\n ## Acceptance Criteria\n\n - [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n-- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n+- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n   - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n   - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n   - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n-- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n-- [ ] Override rule 0 still runs first and short-circuits the cascade.\n-- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n-- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n+  - `unified_trajectory_d_down_between_inspector_field_zones`\n+- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n+- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n+- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n+- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n+- [ ] Override rule 0 still runs first.\n+- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n@@ -132,40 +147,60 @@\n\n ### Kernel integration tests (authoritative)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n+`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n\n-- One `LayerKey` \"window\" containing:\n-  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n-  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n+**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n+- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n+- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n\n+**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n+- `panel:task:T1` zone (parent_zone = inspector root).\n+- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n+\n Test cases:\n\n 1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n    - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n    - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n    - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n    - `nav(\"ui:navbar\", Up) == None`\n\n 2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n    - Mirror left from `task:T1B`.\n\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n-   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n+   - `nav(\"task:T1A\", Left) == None`\n+   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+\n+4. `unified_trajectory_d_down_between_inspector_field_zones`\n+   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n+   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n+   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n\n-### Browser test (wiring guard)\n+### Browser tests (wiring guards)\n\n-`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n+Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n\n-1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n+**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n+1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n 3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n-4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n\n+**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n+1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n+2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n+3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n+4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n+5. Press Up. Walks back. Verify mirror.\n+6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n+7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+\n ### How to run\n\n ```\n@@ -178,9 +213,9 @@\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n+  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n   3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n+  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n   6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","reverse_patch":"--- original\n+++ modified\n@@ -8,7 +8,7 @@\n ---\n ## What\n\n-The user has reported five spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n+The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n\n ### The policy (per user, 2026-04-27)\n\n@@ -22,36 +22,26 @@\n\n This collapses three things that are separate today:\n - **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n - **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n ### The user trajectories the policy must satisfy\n\n-These four trajectories are the regression spec — each must work and each was reported broken:\n+These three trajectories are the regression spec — each must work and each was reported broken at least once:\n\n **Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n 1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n-4. Up → `ui:navbar`.\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n+4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n\n **Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n\n-**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n\n-**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n-- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n-- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n-\n-Pressing Up/Down from a Field zone:\n-- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n-- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n-- Up mirror.\n-- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n-\n-This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n+From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n\n ### Files in scope\n\n@@ -67,11 +57,9 @@\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any. Layer root stops the cascade.\n+          // Escalate to parent zone, if any.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n-          // Hard layer boundary: never cross between layers via escalation.\n-          if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n   }\n@@ -81,7 +69,7 @@\n\n   Override (rule 0) still runs first.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n\n **React side (no behavior change, but tests assert the new policy):**\n\n@@ -89,35 +77,35 @@\n\n **Tests — kernel is authoritative, browser test is supplementary:**\n\n-- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n+- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n\n ### What this card replaces\n\n-These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n+These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n - `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n-- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n-- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n+- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n\n ### Internal contradictions this card resolves\n\n-1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n-3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n-4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+(Audit findings from the prior conversation.)\n\n+1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n+3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n+5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n+\n ### What this card does NOT do\n\n - Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n - Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n-- Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n+- Does not change override rule 0; overrides still run first.\n\n ### Open question — answer before merge\n\n@@ -126,20 +114,17 @@\n ## Acceptance Criteria\n\n - [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n-- [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n   - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n   - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n   - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-  - `unified_trajectory_d_down_between_inspector_field_zones`\n-- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n-- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n-- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n-- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n-- [ ] Override rule 0 still runs first.\n-- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n+- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n+- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n+- [ ] Override rule 0 still runs first and short-circuits the cascade.\n+- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n+- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n@@ -147,60 +132,40 @@\n\n ### Kernel integration tests (authoritative)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n-\n-**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n-- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n-- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n+`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n\n-**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n-- `panel:task:T1` zone (parent_zone = inspector root).\n-- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n+- One `LayerKey` \"window\" containing:\n+  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n+  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n\n Test cases:\n\n 1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n    - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n    - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n    - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n    - `nav(\"ui:navbar\", Up) == None`\n\n 2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n    - Mirror left from `task:T1B`.\n\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None`\n-   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n+   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n\n-4. `unified_trajectory_d_down_between_inspector_field_zones`\n-   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n-   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n-   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n+### Browser test (wiring guard)\n\n-### Browser tests (wiring guards)\n-\n-Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n+`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n\n-**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n-1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n+1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n 3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n+4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n\n-**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n-1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n-2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n-3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n-4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n-5. Press Up. Walks back. Verify mirror.\n-6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n-7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n-\n ### How to run\n\n ```\n@@ -213,9 +178,9 @@\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n+  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n   3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n+  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n   6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","transaction_id":null}
{"id":"01KQ7SCFNGMC8JADHBTK11QGQ5","timestamp":"2026-04-27T15:37:03.408535Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n+The user has reported five spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n\n ### The policy (per user, 2026-04-27)\n\n@@ -14,26 +14,36 @@\n\n This collapses three things that are separate today:\n - **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n - **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n ### The user trajectories the policy must satisfy\n\n-These three trajectories are the regression spec — each must work and each was reported broken at least once:\n+These four trajectories are the regression spec — each must work and each was reported broken:\n\n **Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n 1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n-4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n+4. Up → `ui:navbar`.\n\n **Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+\n+**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+\n+**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n+- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n+- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n\n-**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n+Pressing Up/Down from a Field zone:\n+- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n+- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n+- Up mirror.\n+- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n\n-From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n+This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n\n ### Files in scope\n\n@@ -49,9 +59,11 @@\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any.\n+          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n+          // Hard layer boundary: never cross between layers via escalation.\n+          if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n   }\n@@ -61,7 +73,7 @@\n\n   Override (rule 0) still runs first.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n\n **React side (no behavior change, but tests assert the new policy):**\n\n@@ -69,28 +81,27 @@\n\n **Tests — kernel is authoritative, browser test is supplementary:**\n\n-- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n+- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n\n ### What this card replaces\n\n-These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n+These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n - `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n-- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n+- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n+- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n\n ### Internal contradictions this card resolves\n\n-(Audit findings from the prior conversation.)\n-\n-1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n-3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n-5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n+1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n+3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n+4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n\n ### What this card does NOT do\n\n@@ -97,7 +108,8 @@\n - Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n - Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n-- Does not change override rule 0; overrides still run first.\n+- Does not change override rule 0.\n+- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n\n ### Open question — answer before merge\n\n@@ -106,17 +118,20 @@\n ## Acceptance Criteria\n\n - [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n-- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n+- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n   - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n   - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n   - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n-- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n-- [ ] Override rule 0 still runs first and short-circuits the cascade.\n-- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n-- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n+  - `unified_trajectory_d_down_between_inspector_field_zones`\n+- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n+- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n+- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n+- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n+- [ ] Override rule 0 still runs first.\n+- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n@@ -124,40 +139,60 @@\n\n ### Kernel integration tests (authoritative)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n+`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n\n-- One `LayerKey` \"window\" containing:\n-  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n-  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n+**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n+- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n+- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n\n+**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n+- `panel:task:T1` zone (parent_zone = inspector root).\n+- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n+\n Test cases:\n\n 1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n    - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n    - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n    - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n    - `nav(\"ui:navbar\", Up) == None`\n\n 2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n    - Mirror left from `task:T1B`.\n\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n-   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n+   - `nav(\"task:T1A\", Left) == None`\n+   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+\n+4. `unified_trajectory_d_down_between_inspector_field_zones`\n+   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n+   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n+   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n\n-### Browser test (wiring guard)\n+### Browser tests (wiring guards)\n\n-`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n+Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n\n-1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n+**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n+1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n 3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n-4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n\n+**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n+1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n+2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n+3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n+4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n+5. Press Up. Walks back. Verify mirror.\n+6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n+7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+\n ### How to run\n\n ```\n@@ -170,9 +205,9 @@\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n+  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n   3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n+  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n   6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-The user has reported five spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n+The user has reported four spatial-nav bugs since the per-component cards were closed. They are all symptoms of the same architectural gap: **the kernel has no unified policy for what happens when navigation in a direction has no in-beam candidate at the focused entry's level.** The per-direction tactical cards each tried to fix their symptom in isolation; this card replaces all of them with one rule.\n\n ### The policy (per user, 2026-04-27)\n\n@@ -14,36 +14,26 @@\n\n This collapses three things that are separate today:\n - **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator drills the user back into that column's first card.\n - **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n ### The user trajectories the policy must satisfy\n\n-These four trajectories are the regression spec — each must work and each was reported broken:\n+These three trajectories are the regression spec — each must work and each was reported broken at least once:\n\n **Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n 1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n-4. Up → `ui:navbar`.\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone, which becomes the focused entry).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate to `ui:board`'s level, find perspective bar above).\n+4. Up → `ui:navbar` (one more escalation if needed; navbar sits above perspective bar at the layer root).\n\n **Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (The \"drill back in\" step is an adapter-level concern, NOT a kernel concern. Kernel returns the zone moniker; the adapter handles the drill-in.)\n\n-**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+**Trajectory C — left from leftmost column / up from board.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op (the user said Left here is fine to no-op; their fix is via Up).\n\n-**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n-- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n-- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n-\n-Pressing Up/Down from a Field zone:\n-- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n-- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n-- Up mirror.\n-- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n-\n-This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n+From any zone or leaf inside `ui:board`, pressing Up enough times eventually reaches `ui:perspective-bar` → `ui:navbar` → no-op above the layer root.\n\n ### Files in scope\n\n@@ -59,11 +49,9 @@\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any. Layer root stops the cascade.\n+          // Escalate to parent zone, if any.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n-          // Hard layer boundary: never cross between layers via escalation.\n-          if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n   }\n@@ -73,7 +61,7 @@\n\n   Override (rule 0) still runs first.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. Today `navigate_with` just calls `focus(target_key)`; that already focuses the zone. If the policy is \"land on the zone, let next press drill in,\" then no change. **Decision needed:** does the kernel ever pre-drill-in, or does the user's next press do it explicitly? Default: kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n\n **React side (no behavior change, but tests assert the new policy):**\n\n@@ -81,35 +69,35 @@\n\n **Tests — kernel is authoritative, browser test is supplementary:**\n\n-- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n+- `swissarmyhammer-focus/tests/navigate.rs` — add three integration tests that build the realistic board shape (layer \"window\" → ui:navbar zone with leaves, ui:perspective-bar zone with leaves, ui:board zone → column zones → cards + column.name leaf) and assert the three user trajectories A, B, C. **The kernel test is the definitive test for the algorithm. If it passes, the algorithm is right.**\n\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` (new) — browser-mode test that captures `spatial_register_*` calls into a JS shadow registry, runs the new cascade against the captured graph, and asserts the React side emits `focus-changed` and renders the expected `data-focused` attribute. **This test verifies wiring, not algorithm.** When the kernel test passes but this test fails, the bug is in the React layer's registration shape, not in the algorithm.\n\n ### What this card replaces\n\n-These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n+These four cards are **superseded** — close them as \"subsumed by [this card's ULID]\" once this card is in flight, **except** keep their existing test files as regression guards (move them under this card's test list):\n\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer: either fold their in-flight work into this card's kernel rewrite, or let the tactical fix ship and re-target the kernel under this card.\n - `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n-- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n-- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Update it to assert \"focus lands on a card in column B\" without mentioning rule 2.\n+- The \"left from leftmost column → navbar\" gesture I was about to file separately is folded into this card.\n\n ### Internal contradictions this card resolves\n\n-1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n-3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n-4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+(Audit findings from the prior conversation.)\n\n+1. Two cards diagnosed the same architectural gap as four-six \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\" No more speculation.\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become one mechanism (escalation cascade).\n+3. Tests were asserting on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+4. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test in `swissarmyhammer-focus/tests/navigate.rs` is the source of truth for the algorithm; the browser test only checks wiring. The JS port can drift from Rust without invalidating either test as long as the kernel test pins the algorithm.\n+5. The `<Inspectable>` refactor (`01KQ7K7KZNR3EHS9SY0XY79NYE`) and `<Focusable>` deletion (`01KQ5PSMYE3Q60SV8270S6K819`) overlap on the same primitives. They are independent of this card; cross-link them as a note (a follow-up coordination card if needed). Not blocked by this work.\n+\n ### What this card does NOT do\n\n - Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n - Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n-- Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n+- Does not change override rule 0; overrides still run first.\n\n ### Open question — answer before merge\n\n@@ -118,20 +106,17 @@\n ## Acceptance Criteria\n\n - [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n-- [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. The leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] Three Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C from the spec above:\n   - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n   - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n   - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-  - `unified_trajectory_d_down_between_inspector_field_zones`\n-- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n-- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n-- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n-- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n-- [ ] Override rule 0 still runs first.\n-- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n+- [ ] Browser test `board-view.unified-nav.spatial.test.tsx` captures registrations from the production wiring and asserts the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's three Up presses from `task:T1` produce the sequence `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar`. Each step is one keystroke, no auto-skipping.\n+- [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only (no escalation cascade for them).\n+- [ ] Override rule 0 still runs first and short-circuits the cascade.\n+- [ ] Cross-column right (the `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` regression) still works, now via escalation rather than rule 2 leaf fallback. The existing `board-view.cross-column-nav.spatial.test.tsx` still passes — its mechanism comments need updating, but its observable assertions hold.\n+- [ ] Vertical card nav (the `01KQ7RR4MJJPVTTB7GS6RKRH9E` symptom) works: Down from `task:T1` → `task:T2`, etc.\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n@@ -139,60 +124,40 @@\n\n ### Kernel integration tests (authoritative)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n-\n-**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n-- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n-- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n+`swissarmyhammer-focus/tests/navigate.rs` — three new tests building the realistic shape:\n\n-**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n-- `panel:task:T1` zone (parent_zone = inspector root).\n-- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n+- One `LayerKey` \"window\" containing:\n+  - `ui:navbar` zone (parent_zone = None) at the top of the layout, with `ui:navbar.search`, `ui:navbar.inspect` leaves inside.\n+  - `ui:perspective-bar` zone (parent_zone = None) below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+  - `ui:board` zone (parent_zone = None) below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+  - Each column zone has a `column.name` leaf at the top and three `task:*` leaves below it.\n\n Test cases:\n\n 1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n    - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (the column zone moniker)\n    - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n    - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n    - `nav(\"ui:navbar\", Up) == None`\n\n 2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — the kernel returns the zone moniker; the adapter handles drill-in if needed.\n    - Mirror left from `task:T1B`.\n\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None`\n-   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+   - `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root, return None.\n+   - `nav(\"column:TODO\", Down) == None` if no zone below `column:TODO` at the column-tier or board-tier or layer-root. The exact answer depends on whether the layout has anything below the board; for the test fixture it's None.\n\n-4. `unified_trajectory_d_down_between_inspector_field_zones`\n-   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n-   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n-   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n+### Browser test (wiring guard)\n\n-### Browser tests (wiring guards)\n-\n-Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n+`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` — uses the existing JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if that lands first). Asserts:\n\n-**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n-1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n+1. Pressing Up from `task:T1` emits a `focus-changed` event with `column:TODO.name` as the next moniker, AND the rendered DOM's `data-focused=\"true\"` element is `[data-moniker=\"column:TODO.name\"]`. Three presses walk the trajectory. Each step verified by both the event payload and the DOM.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first, then optionally on a card inside it after a follow-up press (depending on the adapter's drill-in policy; assert the policy as decided).\n 3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n+4. Existing tests keep passing: `board-view.cross-column-nav.spatial.test.tsx` and any vertical-nav test from `01KQ7RR4MJJPVTTB7GS6RKRH9E`. Update their docstrings to reference this card's policy.\n\n-**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n-1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n-2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n-3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n-4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n-5. Press Up. Walks back. Verify mirror.\n-6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n-7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n-\n ### How to run\n\n ```\n@@ -205,9 +170,9 @@\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n+  1. Write the three Rust kernel integration tests (`unified_trajectory_a/b/c`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C pass.\n   3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n+  4. Write the browser-mode wiring test. Confirm it passes against the new kernel via the shadow-registry harness.\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx`, the vertical-nav test (when filed under `01KQ7RR4MJJPVTTB7GS6RKRH9E`), and `01KQ7PXYP62VQ18K9XYS4Y42GA`'s Family 2 to remove mechanism assertions.\n   6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n"}]]}
{"id":"01KQ7VCZDVKGH856RXA63G5X3W","timestamp":"2026-04-27T16:12:16.699041Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -16,61 +16,59 @@\n\n When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n-1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n+1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n-3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n+3. If escalation reaches the layer root (or would cross a layer boundary) with no candidate, return `None`.\n+\n+This collapses what's separate today:\n+- Leaf rule 1 (in-zone leaves) → the first level of the cascade.\n+- Leaf rule 2 (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation.\n+- Zone-only `navigate_zone` → also goes away. Zones use the same cascade.\n\n-This collapses three things that are separate today:\n-- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n-- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n+### Test approach (decided 2026-04-27)\n\n-### The user trajectories the policy must satisfy\n+**Rust integration tests against realistic registry fixtures.** Source of truth is `cargo test -p swissarmyhammer-focus`. Browser tests demoted to wiring-only.\n\n-These four trajectories are the regression spec — each must work and each was reported broken:\n+Reuse the **shared fixture builder** introduced by `01KQ7STZN3G5N2WB3FF4PM4DKX`:\n+`swissarmyhammer-focus/tests/fixtures/` — builds a `SpatialRegistry` matching what production React generates: window layer with `ui:navbar`, `ui:perspective-bar`, `ui:board` zones; `ui:board` contains `column:*` zones with `column.name` leaves and `task:*` cards; plus a separate `inspector` layer with `panel:*` zones containing `field:*` zones.\n\n-**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n-1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n-4. Up → `ui:navbar`.\n+If `01KQ7STZN3G5N2WB3FF4PM4DKX` lands first, this card imports the fixture. If this card lands first, this card builds the fixture and the directional-nav card imports.\n\n-**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+### The user trajectories the policy must satisfy\n\n-**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+**Trajectory A — vertical traversal up the entity stack.**\n+- `nav(\"task:T1A\", Up) == Some(\"column:TODO.name\")`\n+- `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (escalate to column zone)\n+- `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")` (escalate to ui:board's level, find perspective bar above)\n+- `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n+- `nav(\"ui:navbar\", Up) == None`\n\n-**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n-- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n-- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n+**Trajectory B — cross-column horizontal.**\n+- `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — kernel returns the next-column zone moniker; the React adapter handles drill-back-in if desired.\n+- Mirror left.\n\n-Pressing Up/Down from a Field zone:\n-- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n-- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n-- Up mirror.\n-- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n+**Trajectory C — left from leftmost.**\n+- `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root.\n\n-This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n+**Trajectory D — inspector field nav.**\n+- `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+- `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+- `nav(\"field:task:T1.assignees\", Down) == None` — escalates to panel, no zone below within the inspector layer; **layer boundary guard** prevents crossing into the window layer.\n\n ### Files in scope\n\n-**Kernel (the actual fix):**\n-\n-- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n+**Kernel:**\n+- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` as the cascade. Pseudo-code:\n\n   ```rust\n   fn next(&self, registry, focused, direction) -> Option<Moniker> {\n       let mut current = registry.entry(focused)?;\n       loop {\n-          // Score same-level peers (siblings with matching parent_zone, same kind).\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n-          // Hard layer boundary: never cross between layers via escalation.\n           if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n@@ -77,129 +75,54 @@\n   }\n   ```\n\n-  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n+- Delete `navigate_leaf`, `navigate_zone`, the leaf-rule-2 fallback, and the zone-only beam helpers. One cascade replaces them all.\n\n-  Override (rule 0) still runs first.\n+- Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their level-bounded behavior — no escalation cascade for those.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n-\n-**React side (no behavior change, but tests assert the new policy):**\n-\n-- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n-\n-**Tests — kernel is authoritative, browser test is supplementary:**\n-\n-- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n-\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n-\n-### What this card replaces\n-\n-These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n-\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n-- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n-- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n-- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n+- Override (rule 0) still runs first.\n\n-### Internal contradictions this card resolves\n+**Tests (Rust, source of truth):**\n+- `swissarmyhammer-focus/tests/unified_trajectories.rs` (new) — runs trajectories A, B, C, D against the realistic fixture.\n\n-1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n-3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n-4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+**React side:** no behavior change. The wiring works the same way — register zones/leaves, dispatch `spatial_navigate`. The kernel's answers change.\n\n ### What this card does NOT do\n\n-- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n-- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n+- Does not change keymap dispatch.\n+- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside the zone walks deeper).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n - Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n-\n-### Open question — answer before merge\n-\n-**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n+- Does NOT cross layer boundaries via escalation. The inspector is captured-focus.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `navigate_leaf` and `navigate_zone` are merged into one cascade. Leaf-rule-2 cross-zone leaf fallback is deleted.\n - [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n-  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-  - `unified_trajectory_d_down_between_inspector_field_zones`\n-- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n-- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n-- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n+- [ ] `swissarmyhammer-focus/tests/unified_trajectories.rs` runs all four trajectories against the realistic fixture builder. All asserts pass.\n - [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n - [ ] Override rule 0 still runs first.\n-- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n+- [ ] Existing kernel tests pass; mechanism-specific tests (asserting \"rule 2 fired\") are updated to assert observable outcome instead.\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n ## Tests\n\n-### Kernel integration tests (authoritative)\n+### Rust integration tests (mandatory — source of truth)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n-\n-**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n-- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n-- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n+`swissarmyhammer-focus/tests/unified_trajectories.rs`. Uses the shared realistic fixture builder.\n\n-**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n-- `panel:task:T1` zone (parent_zone = inspector root).\n-- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n-\n Test cases:\n\n-1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n-   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n-   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n-   - `nav(\"ui:navbar\", Up) == None`\n-\n-2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n-   - Mirror left from `task:T1B`.\n-\n+1. `unified_trajectory_a_up_walks_card_to_header_to_column_to_perspective_bar_to_navbar`\n+2. `unified_trajectory_b_right_from_card_in_column_a_returns_column_doing_zone`\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None`\n-   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+4. `unified_trajectory_d_down_between_inspector_field_zones_with_layer_boundary_guard`\n\n-4. `unified_trajectory_d_down_between_inspector_field_zones`\n-   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n-   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n-   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n-\n-### Browser tests (wiring guards)\n+For trajectory D, **layer boundary guard** explicit assertion: `nav(\"field:task:T1.assignees\", Down) == None` and is NOT any moniker from the window layer.\n\n-Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n-\n-**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n-1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n-3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n+### Browser tests (wiring guards, supplementary)\n\n-**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n-1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n-2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n-3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n-4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n-5. Press Up. Walks back. Verify mirror.\n-6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n-7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+No new browser-mode test file. Existing tests stay green; their docstrings get updated to reference this card's policy. No assertions on kernel mechanism — only on observable wiring (DOM `data-focused`, `mockInvoke` shape).\n\n ### How to run\n\n@@ -208,14 +131,12 @@\n cd kanban-app/ui && npm test\n ```\n\n-Both must pass headless.\n-\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n-  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n-  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n+  1. Build (or import from `01KQ7STZN3G5N2WB3FF4PM4DKX`) the realistic fixture builder.\n+  2. Write the four trajectory tests. Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  3. Rewrite `BeamNavStrategy::next` as the unified cascade with layer-boundary guard. Delete the old helpers. Confirm A/B/C/D pass.\n+  4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n+  5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n+  6. Mark the superseded tactical cards as done.\n","reverse_patch":"--- original\n+++ modified\n@@ -16,59 +16,61 @@\n\n When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n-1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n+1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n-3. If escalation reaches the layer root (or would cross a layer boundary) with no candidate, return `None`.\n+3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n\n-This collapses what's separate today:\n-- Leaf rule 1 (in-zone leaves) → the first level of the cascade.\n-- Leaf rule 2 (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation.\n-- Zone-only `navigate_zone` → also goes away. Zones use the same cascade.\n+This collapses three things that are separate today:\n+- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n+- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n-### Test approach (decided 2026-04-27)\n+### The user trajectories the policy must satisfy\n\n-**Rust integration tests against realistic registry fixtures.** Source of truth is `cargo test -p swissarmyhammer-focus`. Browser tests demoted to wiring-only.\n+These four trajectories are the regression spec — each must work and each was reported broken:\n\n-Reuse the **shared fixture builder** introduced by `01KQ7STZN3G5N2WB3FF4PM4DKX`:\n-`swissarmyhammer-focus/tests/fixtures/` — builds a `SpatialRegistry` matching what production React generates: window layer with `ui:navbar`, `ui:perspective-bar`, `ui:board` zones; `ui:board` contains `column:*` zones with `column.name` leaves and `task:*` cards; plus a separate `inspector` layer with `panel:*` zones containing `field:*` zones.\n+**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n+1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n+4. Up → `ui:navbar`.\n\n-If `01KQ7STZN3G5N2WB3FF4PM4DKX` lands first, this card imports the fixture. If this card lands first, this card builds the fixture and the directional-nav card imports.\n+**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n\n-### The user trajectories the policy must satisfy\n+**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n\n-**Trajectory A — vertical traversal up the entity stack.**\n-- `nav(\"task:T1A\", Up) == Some(\"column:TODO.name\")`\n-- `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (escalate to column zone)\n-- `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")` (escalate to ui:board's level, find perspective bar above)\n-- `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n-- `nav(\"ui:navbar\", Up) == None`\n+**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n+- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n+- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n\n-**Trajectory B — cross-column horizontal.**\n-- `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — kernel returns the next-column zone moniker; the React adapter handles drill-back-in if desired.\n-- Mirror left.\n+Pressing Up/Down from a Field zone:\n+- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n+- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n+- Up mirror.\n+- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n\n-**Trajectory C — left from leftmost.**\n-- `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root.\n+This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n\n-**Trajectory D — inspector field nav.**\n-- `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-- `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-- `nav(\"field:task:T1.assignees\", Down) == None` — escalates to panel, no zone below within the inspector layer; **layer boundary guard** prevents crossing into the window layer.\n+### Files in scope\n\n-### Files in scope\n+**Kernel (the actual fix):**\n\n-**Kernel:**\n-- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` as the cascade. Pseudo-code:\n+- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n\n   ```rust\n   fn next(&self, registry, focused, direction) -> Option<Moniker> {\n       let mut current = registry.entry(focused)?;\n       loop {\n+          // Score same-level peers (siblings with matching parent_zone, same kind).\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n+          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n+          // Hard layer boundary: never cross between layers via escalation.\n           if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n@@ -75,55 +77,130 @@\n   }\n   ```\n\n-- Delete `navigate_leaf`, `navigate_zone`, the leaf-rule-2 fallback, and the zone-only beam helpers. One cascade replaces them all.\n+  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n+\n+  Override (rule 0) still runs first.\n+\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+\n+**React side (no behavior change, but tests assert the new policy):**\n+\n+- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n+\n+**Tests — kernel is authoritative, browser test is supplementary:**\n+\n+- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n+\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n\n-- Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their level-bounded behavior — no escalation cascade for those.\n+### What this card replaces\n\n-- Override (rule 0) still runs first.\n+These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n\n-**Tests (Rust, source of truth):**\n-- `swissarmyhammer-focus/tests/unified_trajectories.rs` (new) — runs trajectories A, B, C, D against the realistic fixture.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n+- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n+- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n+- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n\n-**React side:** no behavior change. The wiring works the same way — register zones/leaves, dispatch `spatial_navigate`. The kernel's answers change.\n+### Internal contradictions this card resolves\n\n+1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n+3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n+4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+\n ### What this card does NOT do\n\n-- Does not change keymap dispatch.\n-- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside the zone walks deeper).\n+- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n+- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n - Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. The inspector is captured-focus.\n+- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n+\n+### Open question — answer before merge\n+\n+**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `navigate_leaf` and `navigate_zone` are merged into one cascade. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n - [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] `swissarmyhammer-focus/tests/unified_trajectories.rs` runs all four trajectories against the realistic fixture builder. All asserts pass.\n+- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n+  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n+  - `unified_trajectory_d_down_between_inspector_field_zones`\n+- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n+- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n+- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n - [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n - [ ] Override rule 0 still runs first.\n-- [ ] Existing kernel tests pass; mechanism-specific tests (asserting \"rule 2 fired\") are updated to assert observable outcome instead.\n+- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n ## Tests\n\n-### Rust integration tests (mandatory — source of truth)\n+### Kernel integration tests (authoritative)\n+\n+`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n+\n+**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n+- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n+- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n\n-`swissarmyhammer-focus/tests/unified_trajectories.rs`. Uses the shared realistic fixture builder.\n+**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n+- `panel:task:T1` zone (parent_zone = inspector root).\n+- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n\n Test cases:\n\n-1. `unified_trajectory_a_up_walks_card_to_header_to_column_to_perspective_bar_to_navbar`\n-2. `unified_trajectory_b_right_from_card_in_column_a_returns_column_doing_zone`\n+1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n+   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n+   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n+   - `nav(\"ui:navbar\", Up) == None`\n+\n+2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n+   - Mirror left from `task:T1B`.\n+\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-4. `unified_trajectory_d_down_between_inspector_field_zones_with_layer_boundary_guard`\n+   - `nav(\"task:T1A\", Left) == None`\n+   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+\n+4. `unified_trajectory_d_down_between_inspector_field_zones`\n+   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n+   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n+   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n\n-For trajectory D, **layer boundary guard** explicit assertion: `nav(\"field:task:T1.assignees\", Down) == None` and is NOT any moniker from the window layer.\n+### Browser tests (wiring guards)\n\n-### Browser tests (wiring guards, supplementary)\n+Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n\n-No new browser-mode test file. Existing tests stay green; their docstrings get updated to reference this card's policy. No assertions on kernel mechanism — only on observable wiring (DOM `data-focused`, `mockInvoke` shape).\n+**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n+1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n+3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n\n+**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n+1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n+2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n+3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n+4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n+5. Press Up. Walks back. Verify mirror.\n+6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n+7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+\n ### How to run\n\n ```\n@@ -131,12 +208,14 @@\n cd kanban-app/ui && npm test\n ```\n\n+Both must pass headless.\n+\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Build (or import from `01KQ7STZN3G5N2WB3FF4PM4DKX`) the realistic fixture builder.\n-  2. Write the four trajectory tests. Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  3. Rewrite `BeamNavStrategy::next` as the unified cascade with layer-boundary guard. Delete the old helpers. Confirm A/B/C/D pass.\n-  4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n-  5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n-  6. Mark the superseded tactical cards as done.\n+  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n+  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n+  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n+  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n","transaction_id":null}
{"id":"01KQ7VCZDVEKDNQMNFGKMQX61C","timestamp":"2026-04-27T16:12:16.699775Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -8,61 +8,59 @@\n\n When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n-1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n+1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n-3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n+3. If escalation reaches the layer root (or would cross a layer boundary) with no candidate, return `None`.\n+\n+This collapses what's separate today:\n+- Leaf rule 1 (in-zone leaves) → the first level of the cascade.\n+- Leaf rule 2 (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation.\n+- Zone-only `navigate_zone` → also goes away. Zones use the same cascade.\n\n-This collapses three things that are separate today:\n-- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n-- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n-- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n+### Test approach (decided 2026-04-27)\n\n-### The user trajectories the policy must satisfy\n+**Rust integration tests against realistic registry fixtures.** Source of truth is `cargo test -p swissarmyhammer-focus`. Browser tests demoted to wiring-only.\n\n-These four trajectories are the regression spec — each must work and each was reported broken:\n+Reuse the **shared fixture builder** introduced by `01KQ7STZN3G5N2WB3FF4PM4DKX`:\n+`swissarmyhammer-focus/tests/fixtures/` — builds a `SpatialRegistry` matching what production React generates: window layer with `ui:navbar`, `ui:perspective-bar`, `ui:board` zones; `ui:board` contains `column:*` zones with `column.name` leaves and `task:*` cards; plus a separate `inspector` layer with `panel:*` zones containing `field:*` zones.\n\n-**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n-1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n-2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n-3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n-4. Up → `ui:navbar`.\n+If `01KQ7STZN3G5N2WB3FF4PM4DKX` lands first, this card imports the fixture. If this card lands first, this card builds the fixture and the directional-nav card imports.\n\n-**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n-- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n+### The user trajectories the policy must satisfy\n\n-**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n-- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n+**Trajectory A — vertical traversal up the entity stack.**\n+- `nav(\"task:T1A\", Up) == Some(\"column:TODO.name\")`\n+- `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (escalate to column zone)\n+- `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")` (escalate to ui:board's level, find perspective bar above)\n+- `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n+- `nav(\"ui:navbar\", Up) == None`\n\n-**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n-- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n-- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n+**Trajectory B — cross-column horizontal.**\n+- `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — kernel returns the next-column zone moniker; the React adapter handles drill-back-in if desired.\n+- Mirror left.\n\n-Pressing Up/Down from a Field zone:\n-- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n-- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n-- Up mirror.\n-- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n+**Trajectory C — left from leftmost.**\n+- `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root.\n\n-This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n+**Trajectory D — inspector field nav.**\n+- `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+- `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+- `nav(\"field:task:T1.assignees\", Down) == None` — escalates to panel, no zone below within the inspector layer; **layer boundary guard** prevents crossing into the window layer.\n\n ### Files in scope\n\n-**Kernel (the actual fix):**\n-\n-- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n+**Kernel:**\n+- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` as the cascade. Pseudo-code:\n\n   ```rust\n   fn next(&self, registry, focused, direction) -> Option<Moniker> {\n       let mut current = registry.entry(focused)?;\n       loop {\n-          // Score same-level peers (siblings with matching parent_zone, same kind).\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n-          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n-          // Hard layer boundary: never cross between layers via escalation.\n           if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n@@ -69,129 +67,54 @@\n   }\n   ```\n\n-  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n+- Delete `navigate_leaf`, `navigate_zone`, the leaf-rule-2 fallback, and the zone-only beam helpers. One cascade replaces them all.\n\n-  Override (rule 0) still runs first.\n+- Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their level-bounded behavior — no escalation cascade for those.\n\n-- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n-\n-**React side (no behavior change, but tests assert the new policy):**\n-\n-- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n-\n-**Tests — kernel is authoritative, browser test is supplementary:**\n-\n-- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n-\n-- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n-\n-### What this card replaces\n-\n-These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n-\n-- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n-- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n-- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n-- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n-- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n+- Override (rule 0) still runs first.\n\n-### Internal contradictions this card resolves\n+**Tests (Rust, source of truth):**\n+- `swissarmyhammer-focus/tests/unified_trajectories.rs` (new) — runs trajectories A, B, C, D against the realistic fixture.\n\n-1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n-2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n-3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n-4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n-5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+**React side:** no behavior change. The wiring works the same way — register zones/leaves, dispatch `spatial_navigate`. The kernel's answers change.\n\n ### What this card does NOT do\n\n-- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n-- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n+- Does not change keymap dispatch.\n+- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside the zone walks deeper).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n - Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n-\n-### Open question — answer before merge\n-\n-**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n+- Does NOT cross layer boundaries via escalation. The inspector is captured-focus.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n-- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `navigate_leaf` and `navigate_zone` are merged into one cascade. Leaf-rule-2 cross-zone leaf fallback is deleted.\n - [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n-  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-  - `unified_trajectory_d_down_between_inspector_field_zones`\n-- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n-- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n-- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n-- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n+- [ ] `swissarmyhammer-focus/tests/unified_trajectories.rs` runs all four trajectories against the realistic fixture builder. All asserts pass.\n - [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n - [ ] Override rule 0 still runs first.\n-- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n+- [ ] Existing kernel tests pass; mechanism-specific tests (asserting \"rule 2 fired\") are updated to assert observable outcome instead.\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n ## Tests\n\n-### Kernel integration tests (authoritative)\n+### Rust integration tests (mandatory — source of truth)\n\n-`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n-\n-**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n-- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n-- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n-- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n-- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n+`swissarmyhammer-focus/tests/unified_trajectories.rs`. Uses the shared realistic fixture builder.\n\n-**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n-- `panel:task:T1` zone (parent_zone = inspector root).\n-- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n-\n Test cases:\n\n-1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n-   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n-   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n-   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n-   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n-   - `nav(\"ui:navbar\", Up) == None`\n-\n-2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n-   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n-   - Mirror left from `task:T1B`.\n-\n+1. `unified_trajectory_a_up_walks_card_to_header_to_column_to_perspective_bar_to_navbar`\n+2. `unified_trajectory_b_right_from_card_in_column_a_returns_column_doing_zone`\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-   - `nav(\"task:T1A\", Left) == None`\n-   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+4. `unified_trajectory_d_down_between_inspector_field_zones_with_layer_boundary_guard`\n\n-4. `unified_trajectory_d_down_between_inspector_field_zones`\n-   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n-   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n-   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n-\n-### Browser tests (wiring guards)\n+For trajectory D, **layer boundary guard** explicit assertion: `nav(\"field:task:T1.assignees\", Down) == None` and is NOT any moniker from the window layer.\n\n-Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n-\n-**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n-1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n-2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n-3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n+### Browser tests (wiring guards, supplementary)\n\n-**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n-1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n-2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n-3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n-4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n-5. Press Up. Walks back. Verify mirror.\n-6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n-7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+No new browser-mode test file. Existing tests stay green; their docstrings get updated to reference this card's policy. No assertions on kernel mechanism — only on observable wiring (DOM `data-focused`, `mockInvoke` shape).\n\n ### How to run\n\n@@ -200,14 +123,12 @@\n cd kanban-app/ui && npm test\n ```\n\n-Both must pass headless.\n-\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n-  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n-  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n-  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n-  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n+  1. Build (or import from `01KQ7STZN3G5N2WB3FF4PM4DKX`) the realistic fixture builder.\n+  2. Write the four trajectory tests. Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  3. Rewrite `BeamNavStrategy::next` as the unified cascade with layer-boundary guard. Delete the old helpers. Confirm A/B/C/D pass.\n+  4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n+  5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n+  6. Mark the superseded tactical cards as done.\n","reverse_patch":"--- original\n+++ modified\n@@ -8,59 +8,61 @@\n\n When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n-1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n+1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test or scores reasonably, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n-3. If escalation reaches the layer root (or would cross a layer boundary) with no candidate, return `None`.\n+3. If escalation reaches the layer root with no candidate, return `None` (genuine no-op at the layer boundary).\n\n-This collapses what's separate today:\n-- Leaf rule 1 (in-zone leaves) → the first level of the cascade.\n-- Leaf rule 2 (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation.\n-- Zone-only `navigate_zone` → also goes away. Zones use the same cascade.\n+This collapses three things that are separate today:\n+- **Leaf rule 1** (in-zone leaves) → the first level of the cascade.\n+- **Leaf rule 2** (cross-zone leaf fallback in same layer) → goes away. Cross-column nav happens via escalation: a card with no leaf to its right escalates to its column zone, finds the next column to the right, then the navigator returns the next column's moniker; the adapter handles the drill-back-in.\n+- **Zone-only `navigate_zone`** → also goes away. Zones use the same cascade.\n\n-### Test approach (decided 2026-04-27)\n+### The user trajectories the policy must satisfy\n\n-**Rust integration tests against realistic registry fixtures.** Source of truth is `cargo test -p swissarmyhammer-focus`. Browser tests demoted to wiring-only.\n+These four trajectories are the regression spec — each must work and each was reported broken:\n\n-Reuse the **shared fixture builder** introduced by `01KQ7STZN3G5N2WB3FF4PM4DKX`:\n-`swissarmyhammer-focus/tests/fixtures/` — builds a `SpatialRegistry` matching what production React generates: window layer with `ui:navbar`, `ui:perspective-bar`, `ui:board` zones; `ui:board` contains `column:*` zones with `column.name` leaves and `task:*` cards; plus a separate `inspector` layer with `panel:*` zones containing `field:*` zones.\n+**Trajectory A — vertical traversal up the entity stack.** From `task:T1` (first card in leftmost column), pressing Up:\n+1. Up → `column:TODO.name` (the column header leaf, geographically above T1).\n+2. Up → `column:TODO` zone itself (no leaf above the header within the column → escalate to the column zone).\n+3. Up → `ui:perspective-bar` (no zone above the column inside `ui:board` → escalate, find perspective bar).\n+4. Up → `ui:navbar`.\n\n-If `01KQ7STZN3G5N2WB3FF4PM4DKX` lands first, this card imports the fixture. If this card lands first, this card builds the fixture and the directional-nav card imports.\n+**Trajectory B — cross-column horizontal.** From `task:T1A` (a card), pressing Right:\n+- No card to the right inside `column:TODO` → escalate to `column:TODO` → look right at column-zone level → find `column:DOING` → kernel returns `column:DOING`'s moniker → adapter resolves to \"drill into column:DOING's last-focused leaf, or its first card.\" (Drill-back-in is an adapter-level concern, NOT a kernel concern.)\n\n-### The user trajectories the policy must satisfy\n+**Trajectory C — left from leftmost column.** From any leaf in the leftmost column, pressing Left:\n+- No leaf to the left → escalate to column → no zone to the left of `column:TODO` → escalate to `ui:board` → no zone to the left of `ui:board` → return `None`. Genuine no-op at the layer-root edge.\n\n-**Trajectory A — vertical traversal up the entity stack.**\n-- `nav(\"task:T1A\", Up) == Some(\"column:TODO.name\")`\n-- `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")` (escalate to column zone)\n-- `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")` (escalate to ui:board's level, find perspective bar above)\n-- `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n-- `nav(\"ui:navbar\", Up) == None`\n+**Trajectory D — inspector field row navigation.** Inside the inspector layer (`<FocusLayer name=\"inspector\">`, parented to the window layer), the structure is:\n+- `panel:{type}:{id}` zone per inspected entity (parent_zone = inspector layer root).\n+- Inside each panel: `field:{type}:{id}.{name}` zones stacked vertically.\n\n-**Trajectory B — cross-column horizontal.**\n-- `nav(\"task:T1A\", Right) == Some(\"column:DOING\")` — kernel returns the next-column zone moniker; the React adapter handles drill-back-in if desired.\n-- Mirror left.\n+Pressing Up/Down from a Field zone:\n+- Down from `field:task:T1.title` → `field:task:T1.status` (next sibling Field zone below in the same panel).\n+- Down from the last Field in a panel → escalate to `panel:*` → no panel below within the inspector layer → return None (genuine no-op).\n+- Up mirror.\n+- Tab between panels (if the inspector stacks multiple) is handled by the same cascade: Down past the last Field of panel A → escalate → if there's a sibling panel below in the inspector layer, focus there.\n\n-**Trajectory C — left from leftmost.**\n-- `nav(\"task:T1A\", Left) == None` — escalation reaches `ui:board`, no left sibling at the layer root.\n+This trajectory requires a fixture that mounts the inspector layer separately from the board layer (per `01KNQXYC4RBQP1N2NQ33P8DPB9`). The inspector layer's parent is the window layer; the cascade must NOT escalate ACROSS layers. When escalation hits the layer root of the inspector layer, the answer is None — the inspector is a captured-focus context.\n\n-**Trajectory D — inspector field nav.**\n-- `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n-- `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n-- `nav(\"field:task:T1.assignees\", Down) == None` — escalates to panel, no zone below within the inspector layer; **layer boundary guard** prevents crossing into the window layer.\n+### Files in scope\n\n-### Files in scope\n+**Kernel (the actual fix):**\n\n-**Kernel:**\n-- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` as the cascade. Pseudo-code:\n+- `swissarmyhammer-focus/src/navigate.rs` — rewrite `BeamNavStrategy::next` so a single cascade replaces `navigate_leaf` + `navigate_zone` + the various beam helpers. Pseudo-code:\n\n   ```rust\n   fn next(&self, registry, focused, direction) -> Option<Moniker> {\n       let mut current = registry.entry(focused)?;\n       loop {\n+          // Score same-level peers (siblings with matching parent_zone, same kind).\n           if let Some(target) = beam_among_siblings(registry, current, direction) {\n               return Some(target);\n           }\n+          // Escalate to parent zone, if any. Layer root stops the cascade.\n           let Some(parent_key) = current.parent_zone() else { return None; };\n           let Some(parent) = registry.entry(&parent_key) else { return None; };\n+          // Hard layer boundary: never cross between layers via escalation.\n           if parent.layer_key() != current.layer_key() { return None; }\n           current = parent;\n       }\n@@ -67,55 +69,130 @@\n   }\n   ```\n\n-- Delete `navigate_leaf`, `navigate_zone`, the leaf-rule-2 fallback, and the zone-only beam helpers. One cascade replaces them all.\n+  Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their current at-the-focused-level behavior — they are deliberate jumps within a level, not edge-cascading.\n+\n+  Override (rule 0) still runs first.\n+\n+- `swissarmyhammer-focus/src/state.rs` — when `navigate_with` receives a zone moniker as the result, the adapter is responsible for \"drill back in\" semantics if the user expected to land on a leaf. **Decision noted:** kernel never auto-drills-in; the user pressing again from the zone walks deeper. This matches the strict \"navigation moves one step at a time\" contract.\n+\n+**React side (no behavior change, but tests assert the new policy):**\n+\n+- No primitive code changes expected. `<FocusScope>`, `<FocusZone>`, `<Focusable>` continue to register the same way.\n+\n+**Tests — kernel is authoritative, browser test is supplementary:**\n+\n+- `swissarmyhammer-focus/tests/navigate.rs` — add four integration tests that build the realistic shapes (board layer + inspector layer) and assert the four trajectories. **The kernel test is the definitive test for the algorithm.**\n+\n+- `kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx` and `kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx` (new) — browser-mode tests that capture `spatial_register_*` calls into a JS shadow registry, run the new cascade against the captured graph, and assert the React side emits `focus-changed` and renders the expected `data-focused` attribute. **These tests verify wiring, not algorithm.**\n\n-- Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) keep their level-bounded behavior — no escalation cascade for those.\n+### What this card replaces\n\n-- Override (rule 0) still runs first.\n+These five symptom reports are **superseded** — close superseded cards as \"subsumed by [this ULID]\" once this card is in flight, **except** keep their existing test files as regression guards:\n\n-**Tests (Rust, source of truth):**\n-- `swissarmyhammer-focus/tests/unified_trajectories.rs` (new) — runs trajectories A, B, C, D against the realistic fixture.\n+- `01KQ7GWE9V2XKWYAQ0HCPDE0EZ` (cross-column right) — currently `doing`. Coordinate with the implementer.\n+- `01KQ7RR4MJJPVTTB7GS6RKRH9E` (vertical card nav) — `todo`, fully subsumed.\n+- `01KQ7PXYP62VQ18K9XYS4Y42GA` (end-to-end smoke) — its Family 2 test (\"right from card lands on card in column B\") needs to assert observable outcome only, NOT mechanism. Same with Family 3's drill-in/out language.\n+- The \"left from leftmost column → navbar\" gesture (folded in here, never filed as its own card).\n+- The \"inspector up/down between fields\" gesture (this report; folded in here as Trajectory D).\n\n-**React side:** no behavior change. The wiring works the same way — register zones/leaves, dispatch `spatial_navigate`. The kernel's answers change.\n+### Internal contradictions this card resolves\n\n+1. Two cards diagnosed the same architectural gap as multiple \"suspect root causes\" each. The unified policy makes the suspect lists moot: the answer is \"implement the cascade.\"\n+2. Cross-column right vs. left-to-navbar were two different mechanisms today (rule-2 leaf fallback vs. drill-out). Under this policy, both become the escalation cascade.\n+3. Inspector-field nav had no defined mechanism — would have been yet another tactical card.\n+4. Tests asserted on kernel mechanism (rule 1, rule 2). They now assert on observable outcome only.\n+5. The JS-shadow-registry tests were doing kernel work (running their own port of `BeamNavStrategy`). The kernel integration test is the source of truth for the algorithm; the browser test only checks wiring.\n+\n ### What this card does NOT do\n\n-- Does not change keymap dispatch.\n-- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside the zone walks deeper).\n+- Does not change keymap dispatch — `nav.up/down/left/right` continue to call `spatial_navigate`.\n+- Does not introduce auto-drill-in (kernel returns the zone moniker; subsequent press inside that zone is a separate user action).\n - Does not change `<FocusZone>` / `<FocusScope>` registration shape.\n - Does not change override rule 0.\n-- Does NOT cross layer boundaries via escalation. The inspector is captured-focus.\n+- Does NOT cross layer boundaries via escalation. Inspector layer escalation stops at the inspector's root; it does NOT bubble up to the window layer.\n+\n+### Open question — answer before merge\n+\n+**Test fidelity.** The browser-mode tests still mock the Tauri boundary with a JS port of the kernel. That cannot catch Rust-React serialization bugs, real `focus-changed` event timing, or kernel-vs-port drift. The kernel integration test (`cargo test`) is the source of truth for the algorithm. Decision needed: do we accept the browser tests as wiring-only and document the gap, or commit to a real-Tauri test rig as a follow-up? File a follow-up card either way; do NOT block this card on the answer.\n\n ## Acceptance Criteria\n\n-- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `navigate_leaf` and `navigate_zone` are merged into one cascade. Leaf-rule-2 cross-zone leaf fallback is deleted.\n+- [ ] `swissarmyhammer-focus/src/navigate.rs` implements the cascade. `BeamNavStrategy::next` walks the parent chain when no in-direction candidate exists at the focused entry's level, until a candidate is found or the layer root is reached.\n+- [ ] `navigate_leaf` and `navigate_zone` are merged into one cascade implementation. Leaf-rule-2 cross-zone leaf fallback is deleted.\n - [ ] Cascade respects layer boundaries: escalation never crosses from one `LayerKey` to another.\n-- [ ] `swissarmyhammer-focus/tests/unified_trajectories.rs` runs all four trajectories against the realistic fixture builder. All asserts pass.\n+- [ ] Four Rust integration tests in `swissarmyhammer-focus/tests/navigate.rs` pin trajectories A, B, C, D:\n+  - `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+  - `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+  - `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n+  - `unified_trajectory_d_down_between_inspector_field_zones`\n+- [ ] Browser tests `board-view.unified-nav.spatial.test.tsx` and `entity-inspector.unified-nav.spatial.test.tsx` capture registrations from production wiring and assert the resulting `data-focused=\"true\"` element matches each trajectory's expected target. Asserts only on observable outcome, NOT on which kernel rule fired.\n+- [ ] Trajectory A's Up sequence from `task:T1` produces `task:T1` → `column:TODO.name` → `column:TODO` → `ui:perspective-bar` → `ui:navbar`. Each step is one keystroke.\n+- [ ] Trajectory D's Down sequence inside the inspector walks through the Field zones of the focused panel.\n+- [ ] Trajectory D's Down past the last Field returns `None` and does NOT cross into the window layer.\n - [ ] Edge commands (`First`, `Last`, `RowStart`, `RowEnd`) still operate on the focused entry's level only.\n - [ ] Override rule 0 still runs first.\n-- [ ] Existing kernel tests pass; mechanism-specific tests (asserting \"rule 2 fired\") are updated to assert observable outcome instead.\n+- [ ] Existing tests pass: `board-view.cross-column-nav.spatial.test.tsx` (mechanism comments updated, observable assertions hold).\n - [ ] `cargo test -p swissarmyhammer-focus -p swissarmyhammer-kanban` is green.\n - [ ] `cd kanban-app/ui && npm test` is green.\n\n ## Tests\n\n-### Rust integration tests (mandatory — source of truth)\n+### Kernel integration tests (authoritative)\n+\n+`swissarmyhammer-focus/tests/navigate.rs` — four new tests building the realistic shapes:\n+\n+**Fixture for A, B, C — board layout.** One `LayerKey` \"window\" containing:\n+- `ui:navbar` zone at the top, with `ui:navbar.search`, `ui:navbar.inspect` leaves.\n+- `ui:perspective-bar` zone below navbar, with `perspective_tab:default`, `perspective_tab:secondary` leaves.\n+- `ui:board` zone below perspective-bar, with `column:TODO`, `column:DOING`, `column:DONE` zones inside.\n+- Each column zone has a `column.name` leaf and three `task:*` leaves below it.\n\n-`swissarmyhammer-focus/tests/unified_trajectories.rs`. Uses the shared realistic fixture builder.\n+**Fixture for D — inspector layout.** A second `LayerKey` \"inspector\" parented to the window layer, containing:\n+- `panel:task:T1` zone (parent_zone = inspector root).\n+- Inside the panel: three `field:task:T1.title`, `field:task:T1.status`, `field:task:T1.assignees` zones stacked vertically.\n\n Test cases:\n\n-1. `unified_trajectory_a_up_walks_card_to_header_to_column_to_perspective_bar_to_navbar`\n-2. `unified_trajectory_b_right_from_card_in_column_a_returns_column_doing_zone`\n+1. `unified_trajectory_a_up_from_first_card_walks_through_column_to_perspective_bar`\n+   - `nav(\"task:T1\", Up) == Some(\"column:TODO.name\")`\n+   - `nav(\"column:TODO.name\", Up) == Some(\"column:TODO\")`\n+   - `nav(\"column:TODO\", Up) == Some(\"ui:perspective-bar\")`\n+   - `nav(\"ui:perspective-bar\", Up) == Some(\"ui:navbar\")`\n+   - `nav(\"ui:navbar\", Up) == None`\n+\n+2. `unified_trajectory_b_right_from_card_in_column_a_lands_on_column_b`\n+   - `nav(\"task:T1A\", Right) == Some(\"column:DOING\")`\n+   - Mirror left from `task:T1B`.\n+\n 3. `unified_trajectory_c_left_from_leftmost_card_returns_none_at_layer_root`\n-4. `unified_trajectory_d_down_between_inspector_field_zones_with_layer_boundary_guard`\n+   - `nav(\"task:T1A\", Left) == None`\n+   - `nav(\"column:TODO\", Down) == None` (no zone below the column tier within ui:board).\n+\n+4. `unified_trajectory_d_down_between_inspector_field_zones`\n+   - `nav(\"field:task:T1.title\", Down) == Some(\"field:task:T1.status\")`\n+   - `nav(\"field:task:T1.status\", Down) == Some(\"field:task:T1.assignees\")`\n+   - `nav(\"field:task:T1.assignees\", Down) == None` (escalates to panel, no zone below panel within inspector layer, escalates to inspector layer root, hits layer boundary, returns None).\n+   - `nav(\"field:task:T1.status\", Up) == Some(\"field:task:T1.title\")`\n+   - **Layer boundary guard:** `nav(\"field:task:T1.assignees\", Down)` MUST NOT return any moniker from the window layer. Verify by asserting the result is `None`, not `Some` of a window-layer moniker.\n\n-For trajectory D, **layer boundary guard** explicit assertion: `nav(\"field:task:T1.assignees\", Down) == None` and is NOT any moniker from the window layer.\n+### Browser tests (wiring guards)\n\n-### Browser tests (wiring guards, supplementary)\n+Two new files using the JS-shadow-registry harness (or the extracted helper from `01KQ7PXYP62VQ18K9XYS4Y42GA` if it lands first):\n\n-No new browser-mode test file. Existing tests stay green; their docstrings get updated to reference this card's policy. No assertions on kernel mechanism — only on observable wiring (DOM `data-focused`, `mockInvoke` shape).\n+**`kanban-app/ui/src/components/board-view.unified-nav.spatial.test.tsx`**\n+1. Pressing Up from `task:T1` walks the trajectory A sequence — each keystroke verified by both the `focus-changed` event payload and the rendered `data-focused=\"true\"` element.\n+2. Cross-column right (Trajectory B) — focus lands on `column:DOING` zone first; assert that's the resolved moniker, not auto-drilled.\n+3. Left at the layer-root edge is a no-op — no `focus-changed` event, `data-focused` does not move.\n\n+**`kanban-app/ui/src/components/entity-inspector.unified-nav.spatial.test.tsx`** (new)\n+1. Open the inspector for a task with three fields. Drive focus to `field:task:T1.title` via `fireFocusChanged`.\n+2. Press Down. Assert `data-focused=\"true\"` lands on `[data-moniker=\"field:task:T1.status\"]`.\n+3. Press Down again. Lands on `[data-moniker=\"field:task:T1.assignees\"]`.\n+4. Press Down once more. No `focus-changed` event; `data-focused` stays on `field:task:T1.assignees`. Verify the `mockInvoke` for `spatial_navigate` returned None / no next moniker.\n+5. Press Up. Walks back. Verify mirror.\n+6. **Registry shape diagnostic** — capture every `spatial_register_*` call, print on failure: every `field:task:T1.*` zone has `parent_zone === <panel:task:T1's zone key>`, and the panel has `parent_zone === <inspector layer's root>`. If a Field's `parent_zone` is wrong (e.g. the inspector's root, or the window layer's root, or null), the diagnostic surfaces it.\n+7. **Layer boundary guard** — at the bottom of the panel's Field stack, pressing Down must NOT focus any element in the window layer. Assert no `focus-changed` event reports a moniker outside the inspector layer.\n+\n ### How to run\n\n ```\n@@ -123,12 +200,14 @@\n cd kanban-app/ui && npm test\n ```\n\n+Both must pass headless.\n+\n ## Workflow\n\n - Use `/tdd`. Order:\n-  1. Build (or import from `01KQ7STZN3G5N2WB3FF4PM4DKX`) the realistic fixture builder.\n-  2. Write the four trajectory tests. Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n-  3. Rewrite `BeamNavStrategy::next` as the unified cascade with layer-boundary guard. Delete the old helpers. Confirm A/B/C/D pass.\n-  4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n-  5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n-  6. Mark the superseded tactical cards as done.\n+  1. Write the four Rust kernel integration tests (`unified_trajectory_a/b/c/d`). Confirm they fail against the current `navigate_leaf` / `navigate_zone` split.\n+  2. Rewrite `BeamNavStrategy::next` as the unified cascade, including the layer-boundary guard. Delete `navigate_leaf`, `navigate_zone`, and the leaf-rule-2 fallback. Confirm A/B/C/D pass.\n+  3. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically — they must assert on observable trajectory outcome only.\n+  4. Write the two browser-mode wiring tests. The inspector test surfaces whether Field zones in the inspector are registering with the correct `parent_zone` (this is a likely registry-shape bug separate from the kernel rewrite).\n+  5. Update `board-view.cross-column-nav.spatial.test.tsx` and `01KQ7PXYP62VQ18K9XYS4Y42GA` Family 2 to remove mechanism assertions.\n+  6. Move the superseded tactical cards to done with a \"subsumed by <this ULID>\" note; keep their test files as regression guards.\n"}]]}
{"id":"01KQ820SRERTTHH98W9ZAFGD0D","timestamp":"2026-04-27T18:07:57.710577Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ab80\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: ab80\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ820SRK6DD96MRZW78SNKS8","timestamp":"2026-04-27T18:07:57.715804Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ab80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ab80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9W13T03NABTFR6BYKVAW8S","timestamp":"2026-04-28T11:01:45.408269Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9W13T10E2FTRJEBSK5WRZM","timestamp":"2026-04-28T11:01:45.409287Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9WE5TZYMC3EFNW7HKR1DH4","timestamp":"2026-04-28T11:08:53.471736Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -14,7 +14,7 @@\n\n **Edge drill-out + zone-to-zone traversal.**\n\n-When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n+When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the focused entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n 1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n@@ -140,3 +140,14 @@\n   4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n   5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n   6. Mark the superseded tactical cards as done.\n+\n+## Review Findings (2026-04-27 16:30)\n+\n+Cascade implementation verified correct against all four user trajectories. Geometry traced step-by-step against the realistic-app fixture; iter-0 and iter-1 candidate sets, beam scoring, drill-out fallback, and layer-boundary guard all match the spec. Edge commands stay level-bounded. Override rule 0 runs first. Same-kind filtering is sound (rationale documented; no legitimate candidate is eliminated against the production registry shape). Deleted helpers (`navigate_leaf`, `navigate_zone`, `beam_in_zone`, `beam_all_leaves_in_layer`, `beam_sibling_zones`, `edge_command_for_leaf`, `edge_command_for_zone`) have zero remaining `fn` definitions and zero callers in Rust source. JS shadow navigator faithfully mirrors the Rust kernel including same-kind filtering and drill-out fallback. `cargo check`, `cargo clippy --tests`, and the targeted `cargo test` for `unified_trajectories`, `navigate`, and `card_directional_nav` are all clean (41 tests pass).\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/entity-card.tsx:65-73` — Comment block cites the deleted \"rule 2 (cross-zone leaf fallback)\" mechanism and `navigate_zone` helper as the reason cards are leaves, and references the now-deleted kernel test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. The structural conclusion (cards-as-leaves) is still correct under the unified cascade, but the rationale and the dangling test reference are stale. Suggested fix: replace with a reference to the unified cascade's same-kind filtering rule (cards-as-leaves so iter-0 leaf candidates and iter-1 zone candidates work as the user expects), and point to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column).\n+- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test.\n+- [ ] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar.\n","reverse_patch":"--- original\n+++ modified\n@@ -14,7 +14,7 @@\n\n **Edge drill-out + zone-to-zone traversal.**\n\n-When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the focused entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n+When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n 1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n@@ -140,14 +140,3 @@\n   4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n   5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n   6. Mark the superseded tactical cards as done.\n-\n-## Review Findings (2026-04-27 16:30)\n-\n-Cascade implementation verified correct against all four user trajectories. Geometry traced step-by-step against the realistic-app fixture; iter-0 and iter-1 candidate sets, beam scoring, drill-out fallback, and layer-boundary guard all match the spec. Edge commands stay level-bounded. Override rule 0 runs first. Same-kind filtering is sound (rationale documented; no legitimate candidate is eliminated against the production registry shape). Deleted helpers (`navigate_leaf`, `navigate_zone`, `beam_in_zone`, `beam_all_leaves_in_layer`, `beam_sibling_zones`, `edge_command_for_leaf`, `edge_command_for_zone`) have zero remaining `fn` definitions and zero callers in Rust source. JS shadow navigator faithfully mirrors the Rust kernel including same-kind filtering and drill-out fallback. `cargo check`, `cargo clippy --tests`, and the targeted `cargo test` for `unified_trajectories`, `navigate`, and `card_directional_nav` are all clean (41 tests pass).\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/entity-card.tsx:65-73` — Comment block cites the deleted \"rule 2 (cross-zone leaf fallback)\" mechanism and `navigate_zone` helper as the reason cards are leaves, and references the now-deleted kernel test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. The structural conclusion (cards-as-leaves) is still correct under the unified cascade, but the rationale and the dangling test reference are stale. Suggested fix: replace with a reference to the unified cascade's same-kind filtering rule (cards-as-leaves so iter-0 leaf candidates and iter-1 zone candidates work as the user expects), and point to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column).\n-- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test.\n-- [ ] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar.\n","transaction_id":null}
{"id":"01KQ9WE5V02KB40GBHH70M625K","timestamp":"2026-04-28T11:08:53.472397Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n\n **Edge drill-out + zone-to-zone traversal.**\n\n-When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n+When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the focused entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n 1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n@@ -132,3 +132,14 @@\n   4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n   5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n   6. Mark the superseded tactical cards as done.\n+\n+## Review Findings (2026-04-27 16:30)\n+\n+Cascade implementation verified correct against all four user trajectories. Geometry traced step-by-step against the realistic-app fixture; iter-0 and iter-1 candidate sets, beam scoring, drill-out fallback, and layer-boundary guard all match the spec. Edge commands stay level-bounded. Override rule 0 runs first. Same-kind filtering is sound (rationale documented; no legitimate candidate is eliminated against the production registry shape). Deleted helpers (`navigate_leaf`, `navigate_zone`, `beam_in_zone`, `beam_all_leaves_in_layer`, `beam_sibling_zones`, `edge_command_for_leaf`, `edge_command_for_zone`) have zero remaining `fn` definitions and zero callers in Rust source. JS shadow navigator faithfully mirrors the Rust kernel including same-kind filtering and drill-out fallback. `cargo check`, `cargo clippy --tests`, and the targeted `cargo test` for `unified_trajectories`, `navigate`, and `card_directional_nav` are all clean (41 tests pass).\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/entity-card.tsx:65-73` — Comment block cites the deleted \"rule 2 (cross-zone leaf fallback)\" mechanism and `navigate_zone` helper as the reason cards are leaves, and references the now-deleted kernel test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. The structural conclusion (cards-as-leaves) is still correct under the unified cascade, but the rationale and the dangling test reference are stale. Suggested fix: replace with a reference to the unified cascade's same-kind filtering rule (cards-as-leaves so iter-0 leaf candidates and iter-1 zone candidates work as the user expects), and point to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column).\n+- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test.\n+- [ ] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar.\n","reverse_patch":"--- original\n+++ modified\n@@ -6,7 +6,7 @@\n\n **Edge drill-out + zone-to-zone traversal.**\n\n-When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the focused entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n+When `BeamNavStrategy::next` runs from a focused entry and finds no in-direction candidate at the entry's level, it walks up the parent chain until either a candidate exists at some ancestor's level or the layer root is reached. In one rule:\n\n 1. Score the focused entry's same-level peers (siblings sharing `parent_zone`) for the requested direction. If a candidate satisfies the beam test, return it.\n 2. Otherwise, **escalate**: treat the focused entry's parent zone as the new search origin. Score the parent's same-level peers. Repeat.\n@@ -132,14 +132,3 @@\n   4. Run `cargo test -p swissarmyhammer-focus` to confirm no other kernel tests regress. Update any tests that asserted on rule-2 mechanism specifically.\n   5. Update browser-mode tests' docstrings to reference this policy; remove any mechanism assertions there.\n   6. Mark the superseded tactical cards as done.\n-\n-## Review Findings (2026-04-27 16:30)\n-\n-Cascade implementation verified correct against all four user trajectories. Geometry traced step-by-step against the realistic-app fixture; iter-0 and iter-1 candidate sets, beam scoring, drill-out fallback, and layer-boundary guard all match the spec. Edge commands stay level-bounded. Override rule 0 runs first. Same-kind filtering is sound (rationale documented; no legitimate candidate is eliminated against the production registry shape). Deleted helpers (`navigate_leaf`, `navigate_zone`, `beam_in_zone`, `beam_all_leaves_in_layer`, `beam_sibling_zones`, `edge_command_for_leaf`, `edge_command_for_zone`) have zero remaining `fn` definitions and zero callers in Rust source. JS shadow navigator faithfully mirrors the Rust kernel including same-kind filtering and drill-out fallback. `cargo check`, `cargo clippy --tests`, and the targeted `cargo test` for `unified_trajectories`, `navigate`, and `card_directional_nav` are all clean (41 tests pass).\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/entity-card.tsx:65-73` — Comment block cites the deleted \"rule 2 (cross-zone leaf fallback)\" mechanism and `navigate_zone` helper as the reason cards are leaves, and references the now-deleted kernel test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. The structural conclusion (cards-as-leaves) is still correct under the unified cascade, but the rationale and the dangling test reference are stale. Suggested fix: replace with a reference to the unified cascade's same-kind filtering rule (cards-as-leaves so iter-0 leaf candidates and iter-1 zone candidates work as the user expects), and point to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column).\n-- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test.\n-- [ ] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar.\n"}]]}
{"id":"01KQ9WKMB4J1VJZECNRPJJJRMP","timestamp":"2026-04-28T11:11:52.164331Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9WKMB5A0S4ZKN4M5614B7T","timestamp":"2026-04-28T11:11:52.165384Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9WS43JE4SVN2X5TC1RKMP7","timestamp":"2026-04-28T11:14:52.146146Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9WS43KAK4XSDY5RGTHRDV3","timestamp":"2026-04-28T11:14:52.147029Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9X0QTK68QVTZ49CM9K0EJS","timestamp":"2026-04-28T11:19:01.715687Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -151,3 +151,20 @@\n - [x] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column). **Resolved 2026-04-27**: docstring rewritten to describe iter-0 in-column peers, iter-1 escalation to the parent column zone, and landing on the neighbouring column zone.\n - [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test. **Resolved 2026-04-27**: top docstring rewritten to describe the iter-0 / iter-1 trajectory and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n - [x] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar. **Resolved 2026-04-27**: renamed to `\"ArrowRight from task:T1 lands on column DOING (zone or card, unified cascade)\"`.\n+\n+## Review Findings (2026-04-28 11:35)\n+\n+Verified: all four prior `Nits` are genuinely resolved at the cited line numbers. The renamed kernel test (`cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`) exists at `swissarmyhammer-focus/tests/navigate.rs:459`, so the redirected references in the rewritten docstrings actually resolve.\n+\n+The implementer's scope-discipline call is **mostly defensible** — the truly other-file drift (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) is reasonably deferred to a follow-up cleanup card. Those touch files outside the immediate iter-0/iter-1 trajectory the prior nits called out. The kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context (describing what the cascade replaced) and should stay.\n+\n+However, two stale blocks remain in the **same files** the implementer just touched, and they carry the **identical** stale rationale + dangling test name pattern that the prior review flagged. Fixing only the line range cited and ignoring the same defect twenty-eighty lines lower in the same file is line-number cargo-culting, not scope discipline. These need to be cleaned up before the task lands.\n+\n+### Warnings\n+\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\"\n+- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n+\n+### Nits\n+\n+- [ ] Once the two warnings above are cleared, capture a single follow-up card for the residual cross-file doc drift the implementer correctly deferred: `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`, `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`, `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`, `kanban-app/ui/src/components/entity-inspector.tsx:59`, `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`. All carry the same stale \"rule 1 (in-zone)\" / \"rule 2 (cross-zone leaf fallback)\" vocabulary and (in two cases) the dangling test name. Out of scope for this card; tracked for cleanup so the search-and-replace doesn't get lost.\n","reverse_patch":"--- original\n+++ modified\n@@ -151,20 +151,3 @@\n - [x] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column). **Resolved 2026-04-27**: docstring rewritten to describe iter-0 in-column peers, iter-1 escalation to the parent column zone, and landing on the neighbouring column zone.\n - [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test. **Resolved 2026-04-27**: top docstring rewritten to describe the iter-0 / iter-1 trajectory and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n - [x] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar. **Resolved 2026-04-27**: renamed to `\"ArrowRight from task:T1 lands on column DOING (zone or card, unified cascade)\"`.\n-\n-## Review Findings (2026-04-28 11:35)\n-\n-Verified: all four prior `Nits` are genuinely resolved at the cited line numbers. The renamed kernel test (`cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`) exists at `swissarmyhammer-focus/tests/navigate.rs:459`, so the redirected references in the rewritten docstrings actually resolve.\n-\n-The implementer's scope-discipline call is **mostly defensible** — the truly other-file drift (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) is reasonably deferred to a follow-up cleanup card. Those touch files outside the immediate iter-0/iter-1 trajectory the prior nits called out. The kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context (describing what the cascade replaced) and should stay.\n-\n-However, two stale blocks remain in the **same files** the implementer just touched, and they carry the **identical** stale rationale + dangling test name pattern that the prior review flagged. Fixing only the line range cited and ignoring the same defect twenty-eighty lines lower in the same file is line-number cargo-culting, not scope discipline. These need to be cleaned up before the task lands.\n-\n-### Warnings\n-\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\"\n-- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n-\n-### Nits\n-\n-- [ ] Once the two warnings above are cleared, capture a single follow-up card for the residual cross-file doc drift the implementer correctly deferred: `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`, `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`, `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`, `kanban-app/ui/src/components/entity-inspector.tsx:59`, `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`. All carry the same stale \"rule 1 (in-zone)\" / \"rule 2 (cross-zone leaf fallback)\" vocabulary and (in two cases) the dangling test name. Out of scope for this card; tracked for cleanup so the search-and-replace doesn't get lost.\n","transaction_id":null}
{"id":"01KQ9X0QTMMFGWQG71TW1CAKPZ","timestamp":"2026-04-28T11:19:01.716184Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -143,3 +143,20 @@\n - [x] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column). **Resolved 2026-04-27**: docstring rewritten to describe iter-0 in-column peers, iter-1 escalation to the parent column zone, and landing on the neighbouring column zone.\n - [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test. **Resolved 2026-04-27**: top docstring rewritten to describe the iter-0 / iter-1 trajectory and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n - [x] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar. **Resolved 2026-04-27**: renamed to `\"ArrowRight from task:T1 lands on column DOING (zone or card, unified cascade)\"`.\n+\n+## Review Findings (2026-04-28 11:35)\n+\n+Verified: all four prior `Nits` are genuinely resolved at the cited line numbers. The renamed kernel test (`cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`) exists at `swissarmyhammer-focus/tests/navigate.rs:459`, so the redirected references in the rewritten docstrings actually resolve.\n+\n+The implementer's scope-discipline call is **mostly defensible** — the truly other-file drift (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) is reasonably deferred to a follow-up cleanup card. Those touch files outside the immediate iter-0/iter-1 trajectory the prior nits called out. The kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context (describing what the cascade replaced) and should stay.\n+\n+However, two stale blocks remain in the **same files** the implementer just touched, and they carry the **identical** stale rationale + dangling test name pattern that the prior review flagged. Fixing only the line range cited and ignoring the same defect twenty-eighty lines lower in the same file is line-number cargo-culting, not scope discipline. These need to be cleaned up before the task lands.\n+\n+### Warnings\n+\n+- [ ] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\"\n+- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n+\n+### Nits\n+\n+- [ ] Once the two warnings above are cleared, capture a single follow-up card for the residual cross-file doc drift the implementer correctly deferred: `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`, `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`, `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`, `kanban-app/ui/src/components/entity-inspector.tsx:59`, `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`. All carry the same stale \"rule 1 (in-zone)\" / \"rule 2 (cross-zone leaf fallback)\" vocabulary and (in two cases) the dangling test name. Out of scope for this card; tracked for cleanup so the search-and-replace doesn't get lost.\n","reverse_patch":"--- original\n+++ modified\n@@ -143,20 +143,3 @@\n - [x] `kanban-app/ui/src/components/column-view.tsx:42-44` — Same stale \"rule 2 (cross-zone leaf fallback)\" / `navigate_zone` rationale as above. Suggested fix: rewrite to describe the unified cascade's iter-1 escalation (cards as leaves so iter 0 finds in-zone peers; iter 1 escalates to the column zone and lands on the next column). **Resolved 2026-04-27**: docstring rewritten to describe iter-0 in-column peers, iter-1 escalation to the parent column zone, and landing on the neighbouring column zone.\n - [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:7-13` — Same stale mechanism reference and same dangling test name (`rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`). Suggested fix: update to reference the unified cascade's observable outcome and point at the renamed kernel test. **Resolved 2026-04-27**: top docstring rewritten to describe the iter-0 / iter-1 trajectory and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n - [x] `kanban-app/ui/src/spatial-nav-end-to-end.spatial.test.tsx:738` — Test name `\"ArrowRight from task:T1 lands on a card in column DOING (cross-zone)\"` says \"lands on a card\", but under the unified cascade the focused element will carry `data-moniker=\"column:DOING\"` (a zone), not a card moniker. The assertion correctly accepts both shapes via `columnOfTaskMoniker`, but the title is misleading. Suggested fix: rename to `\"ArrowRight from task:T1 lands on column DOING (zone or card)\"` or similar. **Resolved 2026-04-27**: renamed to `\"ArrowRight from task:T1 lands on column DOING (zone or card, unified cascade)\"`.\n-\n-## Review Findings (2026-04-28 11:35)\n-\n-Verified: all four prior `Nits` are genuinely resolved at the cited line numbers. The renamed kernel test (`cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`) exists at `swissarmyhammer-focus/tests/navigate.rs:459`, so the redirected references in the rewritten docstrings actually resolve.\n-\n-The implementer's scope-discipline call is **mostly defensible** — the truly other-file drift (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) is reasonably deferred to a follow-up cleanup card. Those touch files outside the immediate iter-0/iter-1 trajectory the prior nits called out. The kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context (describing what the cascade replaced) and should stay.\n-\n-However, two stale blocks remain in the **same files** the implementer just touched, and they carry the **identical** stale rationale + dangling test name pattern that the prior review flagged. Fixing only the line range cited and ignoring the same defect twenty-eighty lines lower in the same file is line-number cargo-culting, not scope discipline. These need to be cleaned up before the task lands.\n-\n-### Warnings\n-\n-- [ ] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\"\n-- [ ] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\n-\n-### Nits\n-\n-- [ ] Once the two warnings above are cleared, capture a single follow-up card for the residual cross-file doc drift the implementer correctly deferred: `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`, `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`, `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`, `kanban-app/ui/src/components/entity-inspector.tsx:59`, `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`. All carry the same stale \"rule 1 (in-zone)\" / \"rule 2 (cross-zone leaf fallback)\" vocabulary and (in two cases) the dangling test name. Out of scope for this card; tracked for cleanup so the search-and-replace doesn't get lost.\n"}]]}
{"id":"01KQ9X45K2NAGA27GFWBTGA623","timestamp":"2026-04-28T11:20:54.114071Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9X45K34AEKTKK7PB2Y7AY0","timestamp":"2026-04-28T11:20:54.115099Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9XDQDFR49QC64MGKP3KNTR","timestamp":"2026-04-28T11:26:07.279866Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9XDQDGAPPRDZ285DAXB892","timestamp":"2026-04-28T11:26:07.280776Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9XJ1QAMMTDN76TGK6MHQY6","timestamp":"2026-04-28T11:28:28.906259Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffd280\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffd280\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Adopt unified spatial-nav policy: edge drill-out + zone-to-zone — supersedes per-direction tactical cards'\n ---\n","transaction_id":null}
{"id":"01KQ9XJ1QE69FW8HYA7XTM4H6E","timestamp":"2026-04-28T11:28:28.910432Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffd280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffd280\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BV7R2DY5RXH51826EQZY","timestamp":"2026-05-06T18:38:51.640674Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -183,4 +183,4 @@\n - `pnpm tsc --noEmit` — exit 0, no diagnostics.\n - `pnpm vitest run` — 160 test files; **1754 passed | 1 skipped** (matches the gate the reviewer set).\n - `cargo test -p swissarmyhammer-focus` — 12 test binaries, **134 tests passed | 0 failed**, including the 4 unified-trajectory tests and the 8 override tests.\n-- `cargo build --workspace` — clean (`Finished dev profile`).\n+- `cargo build --workspace` — clean (`Finished dev profile`).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -183,4 +183,4 @@\n - `pnpm tsc --noEmit` — exit 0, no diagnostics.\n - `pnpm vitest run` — 160 test files; **1754 passed | 1 skipped** (matches the gate the reviewer set).\n - `cargo test -p swissarmyhammer-focus` — 12 test binaries, **134 tests passed | 0 failed**, including the 4 unified-trajectory tests and the 8 override tests.\n-- `cargo build --workspace` — clean (`Finished dev profile`).\n\\ No newline at end of file\n+- `cargo build --workspace` — clean (`Finished dev profile`).\n","transaction_id":null}
{"id":"01KQZ9BV7W4J2EDWXGBF5E0WFZ","timestamp":"2026-05-06T18:38:51.644363Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -175,4 +175,4 @@\n - `pnpm tsc --noEmit` — exit 0, no diagnostics.\n - `pnpm vitest run` — 160 test files; **1754 passed | 1 skipped** (matches the gate the reviewer set).\n - `cargo test -p swissarmyhammer-focus` — 12 test binaries, **134 tests passed | 0 failed**, including the 4 unified-trajectory tests and the 8 override tests.\n-- `cargo build --workspace` — clean (`Finished dev profile`).\n+- `cargo build --workspace` — clean (`Finished dev profile`).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -175,4 +175,4 @@\n - `pnpm tsc --noEmit` — exit 0, no diagnostics.\n - `pnpm vitest run` — 160 test files; **1754 passed | 1 skipped** (matches the gate the reviewer set).\n - `cargo test -p swissarmyhammer-focus` — 12 test binaries, **134 tests passed | 0 failed**, including the 4 unified-trajectory tests and the 8 override tests.\n-- `cargo build --workspace` — clean (`Finished dev profile`).\n\\ No newline at end of file\n+- `cargo build --workspace` — clean (`Finished dev profile`).\n"}]]}
{"id":"01KRE1V91F27ZD8GZN3QT00DH5","timestamp":"2026-05-12T12:16:05.167868Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -163,7 +163,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n","reverse_patch":"--- original\n+++ modified\n@@ -163,7 +163,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n","transaction_id":null}
{"id":"01KRE1V91N5JMHQDWZX0DAHERJ","timestamp":"2026-05-12T12:16:05.173127Z","entity_type":"task","entity_id":"01KQ7S6WHK9RCCG2R4FN474EFD","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -155,7 +155,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n","reverse_patch":"--- original\n+++ modified\n@@ -155,7 +155,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test #1b\" comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n"}]]}
{"id":"01M129TS846J8W2CW935R37GM7","timestamp":"2026-08-27T19:06:00.836896Z","op":"update","item_id":"01KQ7S6WHK9RCCG2R4FN474EFD","forward_patch":"--- original\n+++ modified\n@@ -163,7 +163,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test `#1b` comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n@@ -174,7 +174,7 @@\n Same-file sweep applied per the reviewer's scope-discipline call:\n\n - `kanban-app/ui/src/components/column-view.tsx` — `ScopeRegisterEntry` docstring rewritten (mirrors the `<ColumnView>` props docstring's iter-0 / iter-1 vocabulary).\n-- `kanban-app/ui/src/components/entity-card.spatial.test.tsx` — test #1b comment block rewritten (mirrors the file's top docstring).\n+- `kanban-app/ui/src/components/entity-card.spatial.test.tsx` — test `#1b` comment block rewritten (mirrors the file's top docstring).\n - Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` matches in either file.\n - Kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context describing what the cascade replaced — left untouched per the reviewer's note.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -163,7 +163,7 @@\n ### Warnings\n\n - [x] `kanban-app/ui/src/components/column-view.tsx:320-330` — The `ScopeRegisterEntry` interface docstring carries the **identical** stale block the implementer just rewrote at lines 42-52: `\"Cards must be leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback) — see the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Both the \"rule 2 (cross-zone leaf fallback)\" mechanism and the test `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` no longer exist. Suggested fix: rewrite the closing sentences to mirror what the rewritten `<ColumnView>` props docstring now says — \"Cards must be leaves so the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the column zone and lands on the neighbouring column zone). See the docstring on `<EntityCard>` and the kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`.\" **Resolved 2026-04-28**: `ScopeRegisterEntry` docstring rewritten using the unified-cascade vocabulary; rationale describes iter-0 in-column card peers and iter-1 escalation to the parent column zone, and the kernel-test pointer is redirected to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` references in `column-view.tsx`.\n-- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test `#1b` comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n+- [x] `kanban-app/ui/src/components/entity-card.spatial.test.tsx:536-542` — The \"test comment block (`does not register the card root as a FocusZone (the card is a leaf, not a zone)`) carries the **identical** stale block the implementer just rewrote at the top of the same file (lines 7-13): `\"Cards must register as leaves so cross-column right/left navigation falls through to rule 2 (cross-zone leaf fallback). If the card ever flips back to being a zone, sibling-zones-only navigation would trap focus in the column. See the docstring on <EntityCard> and the kernel test rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b.\"` Same stale \"rule 2\" mechanism, same dangling test name. Suggested fix: rewrite to mirror the file's top docstring — describe the unified cascade's observable outcome (iter 0 in-column peers; iter 1 escalates to the column zone) and redirect the test reference to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. **Resolved 2026-04-28**: test #1b comment block rewritten to mirror the file's top docstring; describes iter-0 in-column peers and iter-1 escalation to the parent column zone, and redirects the kernel-test pointer to `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone`. Workspace grep confirms zero remaining stale references in `entity-card.spatial.test.tsx`.\n\n ### Nits\n\n@@ -174,7 +174,7 @@\n Same-file sweep applied per the reviewer's scope-discipline call:\n\n - `kanban-app/ui/src/components/column-view.tsx` — `ScopeRegisterEntry` docstring rewritten (mirrors the `<ColumnView>` props docstring's iter-0 / iter-1 vocabulary).\n-- `kanban-app/ui/src/components/entity-card.spatial.test.tsx` — test `#1b` comment block rewritten (mirrors the file's top docstring).\n+- `kanban-app/ui/src/components/entity-card.spatial.test.tsx` — test #1b comment block rewritten (mirrors the file's top docstring).\n - Workspace grep confirms zero remaining `rule 2` / `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` matches in either file.\n - Kernel-side mentions in `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, and `tests/unified_trajectories.rs:23` are intentional historical context describing what the cascade replaced — left untouched per the reviewer's note.\n\n","transaction_id":null}
