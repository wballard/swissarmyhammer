{"id":"01KQ9X7F7YD7PF15ZAD8JV3SE7","timestamp":"2026-04-28T11:22:42.302682Z","op":"create","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,59 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a180\n+project: spatial-nav\n+title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n+---\n+## What\n+\n+Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n+\n+All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n+\n+### Files to update\n+\n+- `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`\n+- `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`\n+- `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`\n+- `kanban-app/ui/src/components/entity-inspector.tsx:59`\n+- `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`\n+\n+### Replacement text\n+\n+Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test #1b block). The pattern:\n+\n+- Replace `\"falls through to rule 2 (cross-zone leaf fallback)\"` with `\"the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the card's parent column zone and lands on the neighbouring column zone)\"`.\n+- Replace `\"rule 1 (in-zone)\"` with `\"iter 0 (same-level peers)\"`.\n+- Replace the dangling test reference `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` with the renamed kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n+- Replace `navigate_zone` (where used as the rationale for cards-as-leaves) with the unified-cascade vocabulary.\n+\n+### What this card does NOT do\n+\n+- Does NOT touch `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, or `tests/unified_trajectories.rs:23`. Those mentions are intentional historical context describing what the cascade replaced.\n+- Does NOT change behavior. Doc-only sweep.\n+\n+## Acceptance Criteria\n+\n+- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n+- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n+- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n+- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n+- [ ] `pnpm tsc --noEmit` clean.\n+\n+## Tests\n+\n+Doc-only sweep — no new test files.\n+\n+Run:\n+\n+```\n+cd kanban-app/ui && pnpm vitest run && pnpm tsc --noEmit\n+```\n+\n+## Workflow\n+\n+- Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n+- Mirror the voice of the already-rewritten docstrings.\n+- Run the test command at the end as the regression guard.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,59 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a180\n-project: spatial-nav\n-title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n----\n-## What\n-\n-Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n-\n-All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n-\n-### Files to update\n-\n-- `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`\n-- `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`\n-- `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`\n-- `kanban-app/ui/src/components/entity-inspector.tsx:59`\n-- `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`\n-\n-### Replacement text\n-\n-Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test #1b block). The pattern:\n-\n-- Replace `\"falls through to rule 2 (cross-zone leaf fallback)\"` with `\"the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the card's parent column zone and lands on the neighbouring column zone)\"`.\n-- Replace `\"rule 1 (in-zone)\"` with `\"iter 0 (same-level peers)\"`.\n-- Replace the dangling test reference `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` with the renamed kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n-- Replace `navigate_zone` (where used as the rationale for cards-as-leaves) with the unified-cascade vocabulary.\n-\n-### What this card does NOT do\n-\n-- Does NOT touch `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, or `tests/unified_trajectories.rs:23`. Those mentions are intentional historical context describing what the cascade replaced.\n-- Does NOT change behavior. Doc-only sweep.\n-\n-## Acceptance Criteria\n-\n-- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n-- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n-- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n-- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n-- [ ] `pnpm tsc --noEmit` clean.\n-\n-## Tests\n-\n-Doc-only sweep — no new test files.\n-\n-Run:\n-\n-```\n-cd kanban-app/ui && pnpm vitest run && pnpm tsc --noEmit\n-```\n-\n-## Workflow\n-\n-- Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n-- Mirror the voice of the already-rewritten docstrings.\n-- Run the test command at the end as the regression guard.\n","transaction_id":null}
{"id":"01KQ9X7F7ZVVXNKAY294D85CE2","timestamp":"2026-04-28T11:22:42.303291Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"create","changes":[["title",{"kind":"set","value":"Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers"}],["body",{"kind":"set","value":"## What\n\nFollow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n\nAll sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n\n### Files to update\n\n- `kanban-app/ui/src/components/sortable-task-card.test.tsx:150-154`\n- `kanban-app/ui/src/components/entity-card.test.tsx:623,667-671`\n- `kanban-app/ui/src/components/inspector-focus-bridge.tsx:20`\n- `kanban-app/ui/src/components/entity-inspector.tsx:59`\n- `kanban-app/ui/src/components/column-view.spatial-nav.test.tsx:23`\n\n### Replacement text\n\nMirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test #1b block). The pattern:\n\n- Replace `\"falls through to rule 2 (cross-zone leaf fallback)\"` with `\"the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the card's parent column zone and lands on the neighbouring column zone)\"`.\n- Replace `\"rule 1 (in-zone)\"` with `\"iter 0 (same-level peers)\"`.\n- Replace the dangling test reference `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b` with the renamed kernel test `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` in `swissarmyhammer-focus/tests/navigate.rs`.\n- Replace `navigate_zone` (where used as the rationale for cards-as-leaves) with the unified-cascade vocabulary.\n\n### What this card does NOT do\n\n- Does NOT touch `swissarmyhammer-focus/src/navigate.rs:21`, `tests/navigate.rs:11`, or `tests/unified_trajectories.rs:23`. Those mentions are intentional historical context describing what the cascade replaced.\n- Does NOT change behavior. Doc-only sweep.\n\n## Acceptance Criteria\n\n- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n- [ ] `pnpm tsc --noEmit` clean.\n\n## Tests\n\nDoc-only sweep — no new test files.\n\nRun:\n\n```\ncd kanban-app/ui && pnpm vitest run && pnpm tsc --noEmit\n```\n\n## Workflow\n\n- Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n- Mirror the voice of the already-rewritten docstrings.\n- Run the test command at the end as the regression guard.\n"}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"a180"}],["project",{"kind":"set","value":"spatial-nav"}],["assignees",{"kind":"set","value":["claude-code"]}]]}
{"id":"01KQ9XRS668X4KF7GMY7EA0Q9P","timestamp":"2026-04-28T11:32:09.542085Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: a180\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a180\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","transaction_id":null}
{"id":"01KQ9XRS68MFYSTZ8SJW5AB152","timestamp":"2026-04-28T11:32:09.544009Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+a180\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9Y4PHBDXZ4T05J7S2SVDCT","timestamp":"2026-04-28T11:38:40.043603Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -36,11 +36,11 @@\n\n ## Acceptance Criteria\n\n-- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n-- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n-- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n-- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n-- [ ] `pnpm tsc --noEmit` clean.\n+- [x] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n+- [x] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n+- [x] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches in the five listed files. (Note: other files in `kanban-app/ui/src/` retain `rule 2` references which are out of this card's scope per the explicit per-task turf assignment from the parent prompt — those are tracked separately or are intentional historical context.)\n+- [x] `pnpm vitest run` passes — only the 2 pre-existing failures in `focus-zone.scroll-listener.browser.test.tsx` remain, which are owned by parallel agent `01KQ9XBAG5P9W3JREQYNGAYM8Y` (Stale rects on scroll). Those failures pre-date this card's changes.\n+- [x] `pnpm tsc --noEmit` clean.\n\n ## Tests\n\n","reverse_patch":"--- original\n+++ modified\n@@ -36,11 +36,11 @@\n\n ## Acceptance Criteria\n\n-- [x] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n-- [x] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n-- [x] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches in the five listed files. (Note: other files in `kanban-app/ui/src/` retain `rule 2` references which are out of this card's scope per the explicit per-task turf assignment from the parent prompt — those are tracked separately or are intentional historical context.)\n-- [x] `pnpm vitest run` passes — only the 2 pre-existing failures in `focus-zone.scroll-listener.browser.test.tsx` remain, which are owned by parallel agent `01KQ9XBAG5P9W3JREQYNGAYM8Y` (Stale rects on scroll). Those failures pre-date this card's changes.\n-- [x] `pnpm tsc --noEmit` clean.\n+- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n+- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n+- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n+- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n+- [ ] `pnpm tsc --noEmit` clean.\n\n ## Tests\n\n","transaction_id":null}
{"id":"01KQ9Y4PHC1JGX9PDV43JV1SJD","timestamp":"2026-04-28T11:38:40.044844Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -28,11 +28,11 @@\n\n ## Acceptance Criteria\n\n-- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n-- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n-- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n-- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n-- [ ] `pnpm tsc --noEmit` clean.\n+- [x] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n+- [x] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n+- [x] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches in the five listed files. (Note: other files in `kanban-app/ui/src/` retain `rule 2` references which are out of this card's scope per the explicit per-task turf assignment from the parent prompt — those are tracked separately or are intentional historical context.)\n+- [x] `pnpm vitest run` passes — only the 2 pre-existing failures in `focus-zone.scroll-listener.browser.test.tsx` remain, which are owned by parallel agent `01KQ9XBAG5P9W3JREQYNGAYM8Y` (Stale rects on scroll). Those failures pre-date this card's changes.\n+- [x] `pnpm tsc --noEmit` clean.\n\n ## Tests\n\n","reverse_patch":"--- original\n+++ modified\n@@ -28,11 +28,11 @@\n\n ## Acceptance Criteria\n\n-- [x] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n-- [x] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n-- [x] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches in the five listed files. (Note: other files in `kanban-app/ui/src/` retain `rule 2` references which are out of this card's scope per the explicit per-task turf assignment from the parent prompt — those are tracked separately or are intentional historical context.)\n-- [x] `pnpm vitest run` passes — only the 2 pre-existing failures in `focus-zone.scroll-listener.browser.test.tsx` remain, which are owned by parallel agent `01KQ9XBAG5P9W3JREQYNGAYM8Y` (Stale rects on scroll). Those failures pre-date this card's changes.\n-- [x] `pnpm tsc --noEmit` clean.\n+- [ ] All five files above are scrubbed of `\"rule 1\"` / `\"rule 2\"` / `\"cross-zone leaf fallback\"` / `navigate_zone` (where used as the cards-as-leaves rationale) and the dangling test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`.\n+- [ ] Each rewritten docstring uses the unified-cascade vocabulary established by the prior nit fixes and points at `cross_zone_realistic_board_right_from_card_in_a_lands_on_column_b_zone` where it currently points at the dangling test.\n+- [ ] Workspace-wide grep confirms no stale references remain in any TypeScript/TSX file: `grep -rE \"rule 2|rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b\" kanban-app/ui/src` returns no matches.\n+- [ ] `pnpm vitest run` passes (must remain at the same pass/skip count).\n+- [ ] `pnpm tsc --noEmit` clean.\n\n ## Tests\n\n"}]]}
{"id":"01KQ9Y5Y147ZJWQ4ZM7SS67TEC","timestamp":"2026-04-28T11:39:20.484610Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","transaction_id":null}
{"id":"01KQ9Y5Y1593GHKD0DC3BE4XFF","timestamp":"2026-04-28T11:39:20.485076Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQ9ZQT1XDV45859FR58VP5T7","timestamp":"2026-04-28T12:06:34.813794Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -57,3 +57,11 @@\n - Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n - Mirror the voice of the already-rewritten docstrings.\n - Run the test command at the end as the regression guard.\n+\n+## Review Findings (2026-04-27 07:08)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+\n+### Nits\n+- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n","reverse_patch":"--- original\n+++ modified\n@@ -57,11 +57,3 @@\n - Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n - Mirror the voice of the already-rewritten docstrings.\n - Run the test command at the end as the regression guard.\n-\n-## Review Findings (2026-04-27 07:08)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n-\n-### Nits\n-- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n","transaction_id":null}
{"id":"01KQ9ZQT1YQJ1NN09H9EF2D41B","timestamp":"2026-04-28T12:06:34.814178Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -49,3 +49,11 @@\n - Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n - Mirror the voice of the already-rewritten docstrings.\n - Run the test command at the end as the regression guard.\n+\n+## Review Findings (2026-04-27 07:08)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+\n+### Nits\n+- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n","reverse_patch":"--- original\n+++ modified\n@@ -49,11 +49,3 @@\n - Read each file, scope the rewrite to the cited line range plus a workspace-wide sweep of the same file (so we don't repeat the line-number cargo-culting that was flagged on the parent card).\n - Mirror the voice of the already-rewritten docstrings.\n - Run the test command at the end as the regression guard.\n-\n-## Review Findings (2026-04-27 07:08)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n-\n-### Nits\n-- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n"}]]}
{"id":"01KQ9ZX90PRWJREPX3M48VA455","timestamp":"2026-04-28T12:09:33.974389Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","transaction_id":null}
{"id":"01KQ9ZX90QW3VPD5CP7MGXH7N7","timestamp":"2026-04-28T12:09:33.975348Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQA00DKJ1560G3D54RKY8TWA","timestamp":"2026-04-28T12:11:16.978131Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -61,7 +61,11 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+\n+  **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n ### Nits\n-- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n+- [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n+\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n","reverse_patch":"--- original\n+++ modified\n@@ -61,11 +61,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n-\n-  **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n+- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n ### Nits\n-- [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n-\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n+- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n","transaction_id":null}
{"id":"01KQA00DKJCVTK92P0K1XTF876","timestamp":"2026-04-28T12:11:16.978592Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -53,7 +53,11 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+\n+  **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n ### Nits\n-- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n+- [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n+\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n","reverse_patch":"--- original\n+++ modified\n@@ -53,11 +53,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n-\n-  **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n+- [ ] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n ### Nits\n-- [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n-\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n+- [ ] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n"}]]}
{"id":"01KQA04HD7F67AECS9NYB4TNC8","timestamp":"2026-04-28T12:13:31.943784Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","transaction_id":null}
{"id":"01KQA04HD82KJCC28HN2ZHFV1P","timestamp":"2026-04-28T12:13:31.944521Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQA0C4SGPSQ5PSWWQ56MA7AZ","timestamp":"2026-04-28T12:17:41.168340Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffd580\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffd580\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: Sweep residual stale \"rule 1 / rule 2\" / dangling test-name doc drift across remaining spatial-nav consumers\n ---\n","transaction_id":null}
{"id":"01KQA0C4SGRG0XQJX5GWZCCVQD","timestamp":"2026-04-28T12:17:41.168990Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffd580\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffd580\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BV9NGYVY0V8Q6G1ACB34","timestamp":"2026-05-06T18:38:51.701787Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -68,4 +68,4 @@\n ### Nits\n - [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -68,4 +68,4 @@\n ### Nits\n - [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n\\ No newline at end of file\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n","transaction_id":null}
{"id":"01KQZ9BV9SHAZKKVZ19HRX4V41","timestamp":"2026-05-06T18:38:51.705316Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -60,4 +60,4 @@\n ### Nits\n - [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -60,4 +60,4 @@\n ### Nits\n - [x] Implementer-flagged additional stale-vocabulary drift in 6 files outside this card's per-file turf (`inspectors-container.tsx` lines 73, 142, 200, 228; `inspectors-container.spatial-nav.test.tsx` lines 340-341; `entity-inspector.test.tsx` lines 599, 638, 1189; `mention-view.tsx` lines 16, 80; `mention-view.test.tsx:301`; `fields/displays/badge-list-nav.test.tsx:25`; plus `board-view.cross-column-nav.spatial.test.tsx:53`, `spatial-nav-end-to-end.spatial.test.tsx:699`, `test/spatial-shadow-registry.ts:41`) is real and is correctly flagged as out-of-scope here per the AC's explicit turf-assignment language. Recommend a separate follow-up sweep card. Not a blocker for this card.\n\n-  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n\\ No newline at end of file\n+  **Acknowledged (2026-04-28):** The broader cross-file drift sweep across the 9+ files outside this card's per-file turf is intentionally left for a separate follow-up card — it is out of scope per the original turf-assignment language in this card. Reviewer's deferred-card suggestion accepted.\n"}]]}
{"id":"01KRE1THCG0RP122XXKFNKXTV3","timestamp":"2026-05-12T12:15:40.944280Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -8,7 +8,7 @@\n ---\n ## What\n\n-Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n+Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n\n All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -8,7 +8,7 @@\n ---\n ## What\n\n-Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n+Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n\n All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n\n","transaction_id":null}
{"id":"01KRE1THCHZ7WMM4C24CJ46K88","timestamp":"2026-05-12T12:15:40.945110Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n+Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n\n All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -1,6 +1,6 @@\n ## What\n\n-Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n+Follow-up cleanup deferred from `01KQ7S6WHK9RCCG2R4FN474EFD` (Adopt unified spatial-nav policy). The two warning sites in that card's review (`column-view.tsx` `ScopeRegisterEntry` docstring, `entity-card.spatial.test.tsx` test #1b) were fixed in scope. The remaining cross-file doc drift below was deferred so the unified-cascade card could land cleanly; this card finishes the sweep.\n\n All sites carry the same stale vocabulary — `\"rule 1 (in-zone)\"` / `\"rule 2 (cross-zone leaf fallback)\"` / `navigate_zone` — and (in two cases) the dangling kernel-test name `rule_2_realistic_board_right_from_card_in_a_lands_on_card_in_b`. None of those concepts exist any more under the unified cascade. The structural conclusions in each docstring (cards-as-leaves, panel-as-zone, etc.) are still correct; only the rationale text and test reference are stale.\n\n"}]]}
{"id":"01KRE1VRV45PEZP35KK36KWM80","timestamp":"2026-05-12T12:16:21.348189Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -61,7 +61,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -61,7 +61,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n","transaction_id":null}
{"id":"01KRE1VRV54FH79732T4SNB95E","timestamp":"2026-05-12T12:16:21.349127Z","entity_type":"task","entity_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -53,7 +53,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -53,7 +53,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC #3 (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n"}]]}
{"id":"01M129TTY7774QD866D5SDQ0TZ","timestamp":"2026-08-27T19:06:02.567889Z","op":"update","item_id":"01KQ9X7F7YNG4NE4AGBNZJMSWG","forward_patch":"--- original\n+++ modified\n@@ -22,7 +22,7 @@\n\n ### Replacement text\n\n-Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test #1b block). The pattern:\n+Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test `#1b` block). The pattern:\n\n - Replace `\"falls through to rule 2 (cross-zone leaf fallback)\"` with `\"the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the card's parent column zone and lands on the neighbouring column zone)\"`.\n - Replace `\"rule 1 (in-zone)\"` with `\"iter 0 (same-level peers)\"`.\n@@ -61,7 +61,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC `#1` (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -22,7 +22,7 @@\n\n ### Replacement text\n\n-Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test `#1b` block). The pattern:\n+Mirror the voice already established in the rewritten docstrings on `entity-card.tsx`, `column-view.tsx` (both the `<ColumnView>` props block and the `ScopeRegisterEntry` block), and `entity-card.spatial.test.tsx` (top docstring + test #1b block). The pattern:\n\n - Replace `\"falls through to rule 2 (cross-zone leaf fallback)\"` with `\"the unified cascade's iter-0 / iter-1 trajectory works as the user expects (iter 0 finds in-column card peers; iter 1 escalates to the card's parent column zone and lands on the neighbouring column zone)\"`.\n - Replace `\"rule 1 (in-zone)\"` with `\"iter 0 (same-level peers)\"`.\n@@ -61,7 +61,7 @@\n ## Review Findings (2026-04-27 07:08)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC `#1` (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n+- [x] `kanban-app/ui/src/components/entity-card.test.tsx:695` — Residual stale vocabulary survived the sweep: the comment block on the `it(\"the card scope's parent_zone follows the enclosing FocusZone …\")` test still ends with `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on.\"` This is INSIDE one of the five in-scope files and contradicts AC #1 (\"scrubbed of `\\\"rule 1\\\"` / `\\\"rule 2\\\"` / `\\\"cross-zone leaf fallback\\\"`\") and AC (the per-file grep gate over the five listed files). The implementer's verification grep used the literal `rule 2` (space) and missed the hyphenated form `rule-2`; the diff shows the implementer actually composed this phrase as part of their rewrite of that block (diff line 260), so this is a re-introduction, not a survivor from before. Fix: replace `\"the column-as-parent contract that cross-column nav rule-2 fallback relies on\"` with the unified-cascade voice — e.g. `\"the column-as-parent contract that the unified cascade's iter-1 escalation relies on (iter 1 reads the card's `parentZone` to find the neighbouring column zone for cross-column nav)\"`. After fixing, re-run `rg \"rule 1|rule 2|rule-1|rule-2|cross-zone leaf fallback|navigate_zone|rule_2_realistic\" kanban-app/ui/src/components/{entity-card,sortable-task-card,column-view.spatial-nav,inspector-focus-bridge,entity-inspector}*.{ts,tsx}` and confirm zero matches across the five files.\n\n   **Resolution (2026-04-28):** Replaced the hyphenated `rule-2 fallback` phrase in the docstring at `entity-card.test.tsx:687` with the unified-cascade voice as suggested. The exhaustive grep `grep -nE \"rule[- ][12]|cross-zone leaf fallback|navigate_zone|rule_2_realistic\"` against the 5 in-scope files (`sortable-task-card.test.tsx`, `entity-card.test.tsx`, `inspector-focus-bridge.tsx`, `entity-inspector.tsx`, `column-view.spatial-nav.test.tsx`) returns ZERO matches.\n\n","transaction_id":null}
