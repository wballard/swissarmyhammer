# NEVER add features or feature flags to this Cargo.toml!
# SwissArmyHammer provides ALL functionality ALL the time.
# This is a SwissArmyHammer - every tool, every time!

[package]
name = "swissarmyhammer-tools"
version.workspace = true
edition.workspace = true
authors.workspace = true
license.workspace = true
repository.workspace = true
description = "MCP tools and server implementation for SwissArmyHammer"
keywords = ["mcp", "tools", "ai", "assistant", "protocol"]
categories = ["command-line-utilities", "development-tools"]

[dependencies]
# Core library dependency removed - now uses domain crates only

swissarmyhammer-common = { workspace = true }
swissarmyhammer-directory = { workspace = true }
swissarmyhammer-config = { workspace = true }
swissarmyhammer-git = { workspace = true }
swissarmyhammer-kanban = { workspace = true }
swissarmyhammer-skills = { workspace = true }
swissarmyhammer-templating = { workspace = true }
swissarmyhammer-agents = { workspace = true }
swissarmyhammer-shell = { workspace = true }
mirdan = { workspace = true }
tempfile = { workspace = true }
swissarmyhammer-web = { workspace = true }
swissarmyhammer-code-context = { workspace = true }
swissarmyhammer-treesitter = { workspace = true }
swissarmyhammer-project-detection = { workspace = true }

swissarmyhammer-mcp-proxy = { workspace = true }


# Async and JSON processing
tokio = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
serde_yaml_ng = { workspace = true }
anyhow = { workspace = true }

once_cell = { workspace = true }
base64 = { workspace = true }

# MCP protocol
rmcp = { workspace = true }

# Logging
tracing = { workspace = true }
tracing-subscriber = { workspace = true }
test-log = { workspace = true }

# ULID for unique identifiers
ulid = { workspace = true }

# Async traits
async-trait = { workspace = true }

# Additional dependencies needed by MCP tools
chrono = { workspace = true }
dirs = { workspace = true }
regex = { workspace = true }
schemars = { workspace = true }
thiserror = { workspace = true }

# File tools dependencies
glob = { workspace = true }
walkdir = { workspace = true }
ignore = { workspace = true }
grep = { workspace = true }

# File watching
notify = { workspace = true }
async-watcher = { workspace = true }

# File editing enhanced dependencies
encoding_rs = { workspace = true }
filetime = { workspace = true }

# Process liveness and ancestry for ralph instruction ownership
sysinfo = { workspace = true }

# Web dependencies
libc = { workspace = true }
rand = { workspace = true }
futures-util = { workspace = true }
reqwest = { workspace = true }

# Embedding model for semantic search (platform-aware: ANE on Apple Silicon, llama.cpp elsewhere)
swissarmyhammer-embedding = { workspace = true }
model-embedding = { workspace = true }

# Pluggable review engine: the operation-based `review` MCP tool is a thin dispatch
# shim over this crate's `run_review` pipeline + rules-as-data validator loader.
swissarmyhammer-validators = { workspace = true }
# ACP primitives for supplying the review engine its live agent connection.
claude-agent = { workspace = true }
agent-client-protocol = { workspace = true }

# SQLite for embedding storage
rusqlite = { workspace = true }

# Hashing and tree-sitter for incremental indexing
md5 = { workspace = true }
tree-sitter = { workspace = true }

# Hashline anchor primitives — `read files` tags lines `N:HH|` so models can
# reference them; `edit files` resolves those anchors back to lines.
swissarmyhammer-hashline = { workspace = true }

# Literal-find ladder for `edit files`: resolves a bare-string `find` (which may
# have lost indentation or drifted) to a concrete byte span in the file.
swissarmyhammer-edit-match = { workspace = true }

# Line-level diffing for the `edit files` no-match near-miss: renders how the
# supplied `find` diverged from the nearest current text.
similar = { workspace = true }

# LSP server lifecycle management
swissarmyhammer-lsp = { workspace = true }

# LSP diagnostic type carried by the in-process fan-out and the subscribable
# diagnostics MCP resource (`mcp::diagnostics_resource`).
lsp-types = { workspace = true }

# Model-free diagnostics core (settle engine + sharp report) for the diagnostics tool
swissarmyhammer-diagnostics = { workspace = true }

# Operations pattern
swissarmyhammer-operations = { workspace = true }

# Semantic diffing
swissarmyhammer-sem = { workspace = true }

# HTTP server dependencies
axum = { workspace = true }
http = { workspace = true }
tower = { workspace = true }
tower-http = { workspace = true, features = ["trace"] }
hyper = { workspace = true }
byte-unit = { workspace = true }
owo-colors = { workspace = true }

[dev-dependencies]
tokio-test = { workspace = true }
swissarmyhammer-web = { workspace = true }
swissarmyhammer-config = { workspace = true }
tempfile = { workspace = true }
futures-util = { workspace = true }
tracing-subscriber = { workspace = true }
proptest = { workspace = true }
serial_test = { workspace = true }
git2 = { workspace = true }
rmcp = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
# MockEmbedder for code_context indexing tests, to avoid loading a real ML model.
model-embedding = { workspace = true, features = ["test-support"] }
# The ONE shared scripted ACP agent harness (`ScriptedAgent`/`ScriptedAdapter`/
# `prompt_text`) the review-tool tests drive their agents through — consumed via
# the validators crate's `test-support` feature, never copied per-file.
swissarmyhammer-validators = { workspace = true, features = ["test-support"] }
# Scripted ACP agent harness for the review-tool integration tests.
agent-client-protocol-extras = { workspace = true }
# Captures `tracing` output to assert the review engine's observability traces
# fire on the real tool path (through spawn_blocking).
tracing-test = { workspace = true }
# `test-support` exposes `mirdan::test_support::MirdanConfigGuard`, the one RAII
# redirect of `MIRDAN_AGENTS_CONFIG` every test that touches mirdan agent
# detection uses.
mirdan = { workspace = true, features = ["test-support"] }
# `test-helpers` exposes `cli_gen::test_support` (build_commands_from_schema /
# collect_verb_noun_pairs) used by the review tool's CLI command-tree coverage test.
swissarmyhammer-operations = { workspace = true, features = ["test-helpers"] }
