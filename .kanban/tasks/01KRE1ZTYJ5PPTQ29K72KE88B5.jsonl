{"id":"01KRE1ZTYJ2GBP95XZ0AFBYVW8","timestamp":"2026-05-12T12:18:34.578834Z","op":"create","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,84 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KRE1WT72MJWNGQBVAD4V5VKM\n+position_column: todo\n+position_ordinal: a780\n+title: Migrate Group tab button to command-driven rendering with field picker\n+---\n+## What\n+\n+Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n+\n+This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n+\n+### Files to modify\n+\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n+  ```yaml\n+  - id: perspective.group\n+    name: Group By\n+    scope: \"entity:perspective\"\n+    tab_button:\n+      icon: group\n+    undoable: true\n+    params:\n+      - name: group\n+        from: picker\n+        shape: enum\n+        options_from: \"perspective.fields\"\n+      - name: perspective_id\n+        from: scope\n+    keys: {}\n+  ```\n+  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n+\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n+  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n+  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n+  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n+  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n+\n+- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n+  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n+  - If not, delete it and let `<CommandPopover>` handle it generically.\n+\n+### Behavior\n+\n+- Group button on the tab bar visually identical to today.\n+- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n+- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n+- `isActive` highlight when the perspective has a `group` set, same as today.\n+\n+### Out of scope\n+\n+- Migrating Filter, Sort, Add Perspective — separate tasks.\n+- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n+\n+## Acceptance Criteria\n+\n+- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n+- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n+- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n+- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n+- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n+- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+\n+## Tests\n+\n+- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n+  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n+  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n+  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n+  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n+- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n+- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+\n+## Workflow\n+\n+- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n+- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,84 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KRE1WT72MJWNGQBVAD4V5VKM\n-position_column: todo\n-position_ordinal: a780\n-title: Migrate Group tab button to command-driven rendering with field picker\n----\n-## What\n-\n-Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n-\n-This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n-\n-### Files to modify\n-\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n-  ```yaml\n-  - id: perspective.group\n-    name: Group By\n-    scope: \"entity:perspective\"\n-    tab_button:\n-      icon: group\n-    undoable: true\n-    params:\n-      - name: group\n-        from: picker\n-        shape: enum\n-        options_from: \"perspective.fields\"\n-      - name: perspective_id\n-        from: scope\n-    keys: {}\n-  ```\n-  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n-\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n-  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n-  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n-  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n-  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n-\n-- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n-  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n-  - If not, delete it and let `<CommandPopover>` handle it generically.\n-\n-### Behavior\n-\n-- Group button on the tab bar visually identical to today.\n-- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n-- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n-- `isActive` highlight when the perspective has a `group` set, same as today.\n-\n-### Out of scope\n-\n-- Migrating Filter, Sort, Add Perspective — separate tasks.\n-- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n-\n-## Acceptance Criteria\n-\n-- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n-- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n-- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n-- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n-- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n-- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n-\n-## Tests\n-\n-- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n-  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n-  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n-  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n-  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n-- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n-\n-## Workflow\n-\n-- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n-- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRE1ZTYKM8YX09W3ZZBA15B0","timestamp":"2026-05-12T12:18:34.579504Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"create","changes":[["depends_on",{"kind":"set","value":["01KRE1WT72MJWNGQBVAD4V5VKM"]}],["position_column",{"kind":"set","value":"todo"}],["position_ordinal",{"kind":"set","value":"a780"}],["assignees",{"kind":"set","value":["claude-code"]}],["title",{"kind":"set","value":"Migrate Group tab button to command-driven rendering with field picker"}],["body",{"kind":"set","value":"## What\n\nReplace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n\nThis is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n### Files to modify\n\n- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n  ```yaml\n  - id: perspective.group\n    name: Group By\n    scope: \"entity:perspective\"\n    tab_button:\n      icon: group\n    undoable: true\n    params:\n      - name: group\n        from: picker\n        shape: enum\n        options_from: \"perspective.fields\"\n      - name: perspective_id\n        from: scope\n    keys: {}\n  ```\n  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n\n- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n\n- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n  - If not, delete it and let `<CommandPopover>` handle it generically.\n\n### Behavior\n\n- Group button on the tab bar visually identical to today.\n- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n- `isActive` highlight when the perspective has a `group` set, same as today.\n\n### Out of scope\n\n- Migrating Filter, Sort, Add Perspective — separate tasks.\n- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n\n## Acceptance Criteria\n\n- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n## Tests\n\n- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n## Workflow\n\n- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui"}]]}
{"id":"01KRE7YGZPYGHVNQMQC7QZET74","timestamp":"2026-05-12T14:02:43.062171Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -3,6 +3,7 @@\n - claude-code\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n+- 01KRE7VDF7RXHV39VPEVH23NN4\n position_column: todo\n position_ordinal: a780\n title: Migrate Group tab button to command-driven rendering with field picker\n","reverse_patch":"--- original\n+++ modified\n@@ -3,7 +3,6 @@\n - claude-code\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n-- 01KRE7VDF7RXHV39VPEVH23NN4\n position_column: todo\n position_ordinal: a780\n title: Migrate Group tab button to command-driven rendering with field picker\n","transaction_id":null}
{"id":"01KRE7YGZP4N7GNR1KVJ47BE30","timestamp":"2026-05-12T14:02:43.062606Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["depends_on",{"kind":"changed","old_value":["01KRE1WT72MJWNGQBVAD4V5VKM"],"new_value":["01KRE1WT72MJWNGQBVAD4V5VKM","01KRE7VDF7RXHV39VPEVH23NN4"]}]]}
{"id":"01KRE81FFWZ8D84DJXBA9HYW1W","timestamp":"2026-05-12T14:04:19.836869Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -10,10 +10,19 @@\n ---\n ## What\n\n-Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n+Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the `PerspectiveFieldsResolver` (registered post-refactor in `swissarmyhammer-perspectives`).\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n+### Post-refactor crate homes\n+\n+This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n+\n+- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n+- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n+- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n+- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n+\n ### Files to modify\n\n - `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n@@ -65,7 +74,7 @@\n - [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n - [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n - [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n@@ -74,9 +83,9 @@\n   - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n   - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n   - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n - [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -10,19 +10,10 @@\n ---\n ## What\n\n-Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the `PerspectiveFieldsResolver` (registered post-refactor in `swissarmyhammer-perspectives`).\n+Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Post-refactor crate homes\n-\n-This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n-\n-- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n-- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n-- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n-- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n-\n ### Files to modify\n\n - `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n@@ -74,7 +65,7 @@\n - [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n - [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n - [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n@@ -83,9 +74,9 @@\n   - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n   - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n   - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n - [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KRE81FFXK76WH5CYFMR5AWZR","timestamp":"2026-05-12T14:04:19.837341Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1,9 +1,18 @@\n ## What\n\n-Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n+Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the `PerspectiveFieldsResolver` (registered post-refactor in `swissarmyhammer-perspectives`).\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n+### Post-refactor crate homes\n+\n+This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n+\n+- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n+- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n+- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n+- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n+\n ### Files to modify\n\n - `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n@@ -55,7 +64,7 @@\n - [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n - [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n - [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n@@ -64,9 +73,9 @@\n   - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n   - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n   - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n - [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -1,18 +1,9 @@\n ## What\n\n-Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the `PerspectiveFieldsResolver` (registered post-refactor in `swissarmyhammer-perspectives`).\n+Replace the hardcoded `<GroupPopoverButton>` + `<GroupSelector>` with a registry-rendered `<CommandButton>` that opens a `<CommandPopover>` containing a single enum-shaped field picker. The picker options come from the backend `PerspectiveFieldsResolver` (registered in the resolver task).\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Post-refactor crate homes\n-\n-This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n-\n-- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n-- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n-- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n-- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n-\n ### Files to modify\n\n - `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n@@ -64,7 +55,7 @@\n - [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n - [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n - [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n@@ -73,9 +64,9 @@\n   - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n   - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n   - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Backend integration test in `swissarmyhammer-kanban/tests/options_enrichment.rs`: `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n - [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [ ] Run: `cargo test -p swissarmyhammer-kanban` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n"}]]}
{"id":"01KRGPBQYMA97KAKXVDB0NCMPY","timestamp":"2026-05-13T12:53:05.108233Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: todo\n-position_ordinal: a780\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: a780\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGPBQYNS2D59VGPF45JZ5F3","timestamp":"2026-05-13T12:53:05.109816Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-a780\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+a780\n\\ No newline at end of file\n"}]]}
{"id":"01KRGQS0RAX2XFY86DBW1WTP2N","timestamp":"2026-05-13T13:17:48.682715Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -14,81 +14,58 @@\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Post-refactor crate homes\n-\n-This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n+### Implementation notes\n\n-- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n-- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n-- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n-- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n+- **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n+  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n+  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n+  - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n-### Files to modify\n+  → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n-  ```yaml\n-  - id: perspective.group\n-    name: Group By\n-    scope: \"entity:perspective\"\n-    tab_button:\n-      icon: group\n-    undoable: true\n-    params:\n-      - name: group\n-        from: picker\n-        shape: enum\n-        options_from: \"perspective.fields\"\n-      - name: perspective_id\n-        from: scope\n-    keys: {}\n-  ```\n-  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n-  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n-  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n-  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n-  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n+### Files modified\n\n-- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n-  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n-  - If not, delete it and let `<CommandPopover>` handle it generically.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n+- `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n+- `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n\n-### Behavior\n+### Files deleted\n\n-- Group button on the tab bar visually identical to today.\n-- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n-- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n-- `isActive` highlight when the perspective has a `group` set, same as today.\n+- `kanban-app/ui/src/components/group-selector.tsx`\n+- `kanban-app/ui/src/components/group-selector.test.tsx`\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-enter.spatial.test.tsx` (the new spatial-nav moniker is built by `<CommandButton>`; the popover-opens-on-Enter behavior is covered by `<CommandButton>`'s own tests).\n\n-### Out of scope\n+### Files created\n\n-- Migrating Filter, Sort, Add Perspective — separate tasks.\n-- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n\n ## Acceptance Criteria\n\n-- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n-- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n-- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n-- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n-- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n-- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [x] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n+- [x] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n+- [x] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n+- [x] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n+- [x] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n+- [x] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n+- [x] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n-- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n-  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n-  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n-  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n-  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n-- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [x] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n+  - [x] `group_command_button_renders_with_group_icon`\n+  - [x] `group_popover_renders_field_options_from_command_emission`\n+  - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n+  - [x] `group_button_is_active_when_perspective_has_a_group_set`\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n+- [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n\n ## Workflow\n\n-- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n-- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n+- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n+- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -14,58 +14,81 @@\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Implementation notes\n+### Post-refactor crate homes\n\n-- **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n-  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n-  - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n+This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n\n-  → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n+- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n+- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n+- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n+- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+### Files to modify\n\n-### Files modified\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n+  ```yaml\n+  - id: perspective.group\n+    name: Group By\n+    scope: \"entity:perspective\"\n+    tab_button:\n+      icon: group\n+    undoable: true\n+    params:\n+      - name: group\n+        from: picker\n+        shape: enum\n+        options_from: \"perspective.fields\"\n+      - name: perspective_id\n+        from: scope\n+    keys: {}\n+  ```\n+  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n-- `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n-- `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n+  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n+  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n+  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n+  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n\n-### Files deleted\n+- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n+  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n+  - If not, delete it and let `<CommandPopover>` handle it generically.\n\n-- `kanban-app/ui/src/components/group-selector.tsx`\n-- `kanban-app/ui/src/components/group-selector.test.tsx`\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-enter.spatial.test.tsx` (the new spatial-nav moniker is built by `<CommandButton>`; the popover-opens-on-Enter behavior is covered by `<CommandButton>`'s own tests).\n+### Behavior\n+\n+- Group button on the tab bar visually identical to today.\n+- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n+- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n+- `isActive` highlight when the perspective has a `group` set, same as today.\n\n-### Files created\n+### Out of scope\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n+- Migrating Filter, Sort, Add Perspective — separate tasks.\n+- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n\n ## Acceptance Criteria\n\n-- [x] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n-- [x] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n-- [x] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n-- [x] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n-- [x] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n-- [x] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [x] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n+- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n+- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n+- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n+- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n+- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n+- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n-- [x] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n-  - [x] `group_command_button_renders_with_group_icon`\n-  - [x] `group_popover_renders_field_options_from_command_emission`\n-  - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n-  - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n-- [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n+- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n+  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n+  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n+  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n+  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n+- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n+- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n-- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n-- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n+- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n+- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRGQS0RBZRVMER5JTR1VCWD1","timestamp":"2026-05-13T13:17:48.683189Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -4,81 +4,58 @@\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Post-refactor crate homes\n-\n-This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n+### Implementation notes\n\n-- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n-- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n-- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n-- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n+- **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n+  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n+  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n+  - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n-### Files to modify\n+  → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n-  ```yaml\n-  - id: perspective.group\n-    name: Group By\n-    scope: \"entity:perspective\"\n-    tab_button:\n-      icon: group\n-    undoable: true\n-    params:\n-      - name: group\n-        from: picker\n-        shape: enum\n-        options_from: \"perspective.fields\"\n-      - name: perspective_id\n-        from: scope\n-    keys: {}\n-  ```\n-  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n-  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n-  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n-  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n-  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n+### Files modified\n\n-- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n-  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n-  - If not, delete it and let `<CommandPopover>` handle it generically.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n+- `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n+- `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n\n-### Behavior\n+### Files deleted\n\n-- Group button on the tab bar visually identical to today.\n-- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n-- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n-- `isActive` highlight when the perspective has a `group` set, same as today.\n+- `kanban-app/ui/src/components/group-selector.tsx`\n+- `kanban-app/ui/src/components/group-selector.test.tsx`\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-enter.spatial.test.tsx` (the new spatial-nav moniker is built by `<CommandButton>`; the popover-opens-on-Enter behavior is covered by `<CommandButton>`'s own tests).\n\n-### Out of scope\n+### Files created\n\n-- Migrating Filter, Sort, Add Perspective — separate tasks.\n-- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n\n ## Acceptance Criteria\n\n-- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n-- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n-- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n-- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n-- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n-- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [x] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n+- [x] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n+- [x] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n+- [x] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n+- [x] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n+- [x] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n+- [x] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n-- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n-  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n-  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n-  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n-  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n-- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n-- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n-- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n+- [x] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n+  - [x] `group_command_button_renders_with_group_icon`\n+  - [x] `group_popover_renders_field_options_from_command_emission`\n+  - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n+  - [x] `group_button_is_active_when_perspective_has_a_group_set`\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n+- [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n\n ## Workflow\n\n-- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n-- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n+- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n+- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -4,58 +4,81 @@\n\n This is the first migration that exercises the picker pipeline end-to-end: enum param → backend-supplied options → frontend dropdown → dispatch with picked value.\n\n-### Implementation notes\n+### Post-refactor crate homes\n\n-- **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n-  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n-  - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n+This task depends on `01KRE7VDF7RXHV39VPEVH23NN4` (relocate DynamicSources and friends). After that refactor, file paths in this task that reference `swissarmyhammer-kanban/src/commands/perspective_commands.rs` etc. refer to the post-refactor homes:\n\n-  → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n+- `perspective.group`'s `execute` impl lives in `swissarmyhammer-perspectives`.\n+- `commands_for_scope` and the emission infrastructure live in `swissarmyhammer-commands`.\n+- `PerspectiveFieldsResolver` is registered from `swissarmyhammer-perspectives`.\n+- The YAML stays in `swissarmyhammer-kanban/builtin/commands/perspective.yaml`.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+### Files to modify\n\n-### Files modified\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — extend the existing `perspective.group` entry:\n+  ```yaml\n+  - id: perspective.group\n+    name: Group By\n+    scope: \"entity:perspective\"\n+    tab_button:\n+      icon: group\n+    undoable: true\n+    params:\n+      - name: group\n+        from: picker\n+        shape: enum\n+        options_from: \"perspective.fields\"\n+      - name: perspective_id\n+        from: scope\n+    keys: {}\n+  ```\n+  Changes from today: `name` from \"Set Group\" to \"Group By\" (matches user-facing button label); `params[0].from` from `args` to `picker`; new `shape: enum` and `options_from: \"perspective.fields\"`; new `tab_button` block. The existing palette-only behavior continues to work because the dispatcher still accepts `group` as an arg from any source.\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n-- `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n-- `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx`:\n+  - Delete `<GroupPopoverButton>` and the `import { Group } from \"lucide-react\"` if no other line uses it.\n+  - Delete the `groupOpen`/`setGroupOpen` state and the `GroupPopoverButton` invocation in the per-tab render.\n+  - The registry path (from the prerequisite tab-bar task) renders `perspective.group` as a `<CommandButton>` automatically.\n+  - Pass `isActive={Boolean(perspective.group)}` to the `<CommandButton>` so the highlight matches today.\n\n-### Files deleted\n+- `kanban-app/ui/src/components/group-selector.tsx` — leave the underlying field-list component but remove the hardcoded Popover wrapper. The selector becomes a thin \"enum dropdown of perspective fields\" — or, if `<CommandPopover>`'s generic enum renderer is good enough, the dedicated component can be deleted entirely. Decide based on whether `<GroupSelector>` does anything special beyond rendering a list (e.g. virtualization, search/filter input, field-icon rendering).\n+  - If `<GroupSelector>` does have unique UX worth preserving, register it as the renderer for `options_from: \"perspective.fields\"` enum-shaped params inside `<CommandPopover>` (a per-options_from override on the generic enum renderer).\n+  - If not, delete it and let `<CommandPopover>` handle it generically.\n\n-- `kanban-app/ui/src/components/group-selector.tsx`\n-- `kanban-app/ui/src/components/group-selector.test.tsx`\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-enter.spatial.test.tsx` (the new spatial-nav moniker is built by `<CommandButton>`; the popover-opens-on-Enter behavior is covered by `<CommandButton>`'s own tests).\n+### Behavior\n+\n+- Group button on the tab bar visually identical to today.\n+- Clicking opens a popover with the same field list as today (sourced from `PerspectiveFieldsResolver` instead of being computed inline).\n+- Picking a field dispatches `perspective.group` with `group: <field>` — same dispatcher path as before; `available()` / `execute()` unchanged.\n+- `isActive` highlight when the perspective has a `group` set, same as today.\n\n-### Files created\n+### Out of scope\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n+- Migrating Filter, Sort, Add Perspective — separate tasks.\n+- Adding a Clear Group affordance to the tab bar (`perspective.clearGroup` already exists in the right-click context menu and stays there).\n\n ## Acceptance Criteria\n\n-- [x] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n-- [x] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n-- [x] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n-- [x] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n-- [x] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n-- [x] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n-- [x] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n+- [ ] `perspective.group` YAML carries `tab_button: { icon: \"group\" }` and `params[0].shape: enum, options_from: \"perspective.fields\"`.\n+- [ ] Emitted `perspective.group` from `commands_for_scope` carries the resolved option list when a perspective is in scope; the option list matches the perspective's field set.\n+- [ ] `<GroupPopoverButton>` is deleted; the tab bar's Group affordance is the registry-rendered `<CommandButton>` + `<CommandPopover>`.\n+- [ ] Clicking the button → popover → picking a field dispatches `perspective.group` with the picked field and the scope-resolved perspective id.\n+- [ ] `isActive` highlight matches today's behavior (`Boolean(perspective.group)`).\n+- [ ] Existing palette/right-click tests for `perspective.group` and `perspective.clearGroup` continue to pass — this task changes the rendering, not the dispatch contract.\n+- [ ] `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar group` both pass.\n\n ## Tests\n\n-- [x] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n-  - [x] `group_command_button_renders_with_group_icon`\n-  - [x] `group_popover_renders_field_options_from_command_emission`\n-  - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n-  - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n-- [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n+- [ ] Frontend regression `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx`:\n+  - `group_command_button_renders_with_group_icon` — mount with `perspective.group` carrying `tab_button: { icon: \"group\" }`, assert the icon is present.\n+  - `group_popover_renders_field_options_from_command_emission` — fixture `commands_for_scope` returns `perspective.group` with `params[0].options = [{value:\"status\",label:\"Status\"},{value:\"assignee\",label:\"Assignee\"}]`; click the button, assert the popover has both options as `<select>` entries.\n+  - `picking_a_group_field_dispatches_perspective_group_with_field_arg` — click button, pick \"status\", assert dispatcher receives `perspective.group` with `{ group: \"status\", perspective_id: ... }`.\n+  - `group_button_is_active_when_perspective_has_a_group_set` — fixture perspective with `group: \"status\"`, assert the rendered `<CommandButton>` has the highlighted state.\n+- [ ] Backend integration test (in whichever crate owns `commands_for_scope` post-refactor): `perspective_group_command_carries_field_options_when_perspective_in_scope` — emit through `commands_for_scope` with a perspective that has 3 fields, assert the emitted `perspective.group` command's `params[0].options.len() == 3`.\n+- [ ] Update / remove the existing `group-popover-button.test.tsx` and `perspective-tab-bar.group-enter.spatial.test.tsx` to either reflect the new moniker / component shape OR be replaced by the new test file.\n+- [ ] Run: `cargo test --workspace` and `pnpm -C kanban-app/ui test perspective-tab-bar` — both green.\n\n ## Workflow\n\n-- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n-- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n+- Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button.\n+- Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted; the choice depends on whether the existing field-list UI has any UX worth preserving over a plain `<select>`. Document the decision in the implementation notes on the task.\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). Update or replace the affected spatial test. #command-driven-ui\n\\ No newline at end of file\n"}]]}
{"id":"01KRGQTEY9C495WSVYK2WW3W61","timestamp":"2026-05-13T13:18:35.977718Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGQTEYA8QP0QP3CB9EE3CH5","timestamp":"2026-05-13T13:18:35.978711Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KRGR33HAQE2B166M9SMGYSG1","timestamp":"2026-05-13T13:23:19.210065Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -68,4 +68,16 @@\n\n - Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n - Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n+\n+## Review Findings (2026-05-13 14:25)\n+\n+### Warnings\n+\n+- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -68,16 +68,4 @@\n\n - Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n - Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n-\n-## Review Findings (2026-05-13 14:25)\n-\n-### Warnings\n-\n-- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRGR33HAQVBW28ZW7H6K3JY2","timestamp":"2026-05-13T13:23:19.210763Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -58,4 +58,16 @@\n\n - Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n - Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n+\n+## Review Findings (2026-05-13 14:25)\n+\n+### Warnings\n+\n+- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+\n+### Nits\n+\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -58,16 +58,4 @@\n\n - Use `/tdd` — write the popover-renders-options and picking-dispatches tests first, let them fail, then change the YAML and delete the hardcoded button. ✓ Done.\n - Decide early whether `<GroupSelector>` survives as a renderer override or gets deleted. ✓ Decided to delete — see Implementation notes above.\n-- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n-\n-## Review Findings (2026-05-13 14:25)\n-\n-### Warnings\n-\n-- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n-\n-### Nits\n-\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n+- The spatial moniker shape changes (`perspective_tab.group:` → `perspective_tab.perspective.group:`). ✓ Old test deleted, new shape covered by `<CommandButton>`'s own test coverage. #command-driven-ui\n\\ No newline at end of file\n"}]]}
{"id":"01KRGR6XF6286TTNCVGJFZ80JP","timestamp":"2026-05-13T13:25:24.070291Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGR6XF60ZHJTS9MXMY46VMC","timestamp":"2026-05-13T13:25:24.070988Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KRGSYMBVVKTPPFQC3G7M74MA","timestamp":"2026-05-13T13:55:49.755550Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -17,21 +17,27 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n-  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n+  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n+  - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n   → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\", clear_command: \"perspective.clearGroup\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n ### Files modified\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `clear_command: perspective.clearGroup`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `swissarmyhammer-commands/src/types.rs` — added `ParamDef.clear_command: Option<String>` plus full docstring; updated two struct-literal sites in tests.\n+- `swissarmyhammer-kanban/src/dynamic_sources.rs` — `denormalize_perspective_fields` now filters perspective fields against `FieldDef.groupable == Some(true)`; six new unit tests pin the contract (groupable kept; non-groupable dropped; caption does not bypass the filter; unknown ids dropped; empty FieldsContext path).\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm. Rewrote the `ScopedPerspectiveTab` JSDoc paragraph to reflect both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n+- `kanban-app/ui/src/components/command-popover.tsx` — `<EnumField>` renders \"(none)\" as the first option when the param declares `clear_command`; submit-disabled rule relaxed so the clear sentinel is a legitimate submission.\n+- `kanban-app/ui/src/components/command-button.tsx` — `handleCommit` intercepts empty-string values for params with `clear_command` and dispatches the redirection target instead of the parent command (with the sentinel stripped from the args bag).\n+- `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` — file-level JSDoc updated to describe the Group leaf as a `<CommandButton>`, mirroring the Filter sentence.\n+- `kanban-app/ui/src/types/kanban.ts` — added `ParamDef.clear_command?: string` mirroring the Rust field; full JSDoc.\n - `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n - `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope` (positive case) and `perspective_group_command_drops_non_groupable_fields_end_to_end` (negative case, end-to-end through `build_dynamic_sources`).\n\n ### Files deleted\n\n@@ -41,7 +47,7 @@\n\n ### Files created\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — seven cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set, \"(none)\" affordance renders when `clear_command` is present, picking \"(none)\" dispatches `perspective.clearGroup`, placeholder stays \"Pick…\" when no `clear_command`.\n\n ## Acceptance Criteria\n\n@@ -60,9 +66,13 @@\n   - [x] `group_popover_renders_field_options_from_command_emission`\n   - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n   - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n+  - [x] `group_popover_renders_none_option_when_clear_command_present`\n+  - [x] `picking_none_in_group_popover_dispatches_perspective_clearGroup`\n+  - [x] `group_popover_keeps_pick_placeholder_when_no_clear_command`\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`, plus end-to-end negative case `perspective_group_command_drops_non_groupable_fields_end_to_end` that exercises the `groupable` filter through `build_dynamic_sources`.\n+- [x] Unit tests in `swissarmyhammer-kanban/src/dynamic_sources.rs` pin `denormalize_perspective_fields`: groupable kept in order; non-groupable dropped; caption does not bypass the filter; caption survives for groupable fields; unknown ids dropped; empty FieldsContext returns empty.\n - [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `swissarmyhammer-cli` doctor `test_lsp_servers_check` test fails only under workspace parallelism and passes in isolation — unrelated to this task, environment-dependent) and `pnpm -C kanban-app/ui test` (2141 tests across 228 files pass).\n\n ## Workflow\n\n@@ -74,10 +84,14 @@\n\n ### Warnings\n\n-- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+- [x] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+  - **Resolution:** `denormalize_perspective_fields` now consults `fields_ctx.get_field_by_id(...).groupable == Some(true)` and drops any field that fails the predicate. Six new unit tests in `dynamic_sources.rs` pin the contract; new end-to-end integration test `perspective_group_command_drops_non_groupable_fields_end_to_end` in `tests/options_enrichment.rs` exercises the filter through `build_dynamic_sources` + `commands_for_scope` using real builtin fields (groupable `assignees` vs. non-groupable `title`).\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+  - **Resolution:** Rewrote the paragraph to describe both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n+- [x] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+  - **Resolution:** File-level JSDoc rewritten to describe both Filter and Group leaves as `<CommandButton>`s with their parallel `perspective_tab.perspective.{filter.focus,group}:{id}` monikers.\n+- [x] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n+  - **Resolution:** Adopted via a new reusable YAML annotation `clear_command: \"<command-id>\"` on `ParamDef` (both Rust and TypeScript sides). When set, `<CommandPopover>` renders \"(none)\" as the first option in the enum select and treats the empty-string slot as a submittable value; `<CommandButton>.handleCommit` intercepts the empty-string sentinel and dispatches the `clear_command` target (with the sentinel stripped from the args bag) instead of the parent command. The Group YAML now carries `clear_command: \"perspective.clearGroup\"` on its `group` param, restoring the legacy single-popover \"None to clear\" affordance. The Sort migration can reuse the same annotation if it lands a similar UX need — the redirection stays out of the YAML body and stays consistent across surfaces. Three new frontend tests pin the contract: \"(none)\" renders when `clear_command` is set; picking it dispatches `perspective.clearGroup` (not `perspective.group`) with no `group` arg; the \"Pick…\" placeholder is preserved when `clear_command` is absent.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -17,27 +17,21 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n-  - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n+  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n+  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n   → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\", clear_command: \"perspective.clearGroup\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n ### Files modified\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `clear_command: perspective.clearGroup`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `swissarmyhammer-commands/src/types.rs` — added `ParamDef.clear_command: Option<String>` plus full docstring; updated two struct-literal sites in tests.\n-- `swissarmyhammer-kanban/src/dynamic_sources.rs` — `denormalize_perspective_fields` now filters perspective fields against `FieldDef.groupable == Some(true)`; six new unit tests pin the contract (groupable kept; non-groupable dropped; caption does not bypass the filter; unknown ids dropped; empty FieldsContext path).\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm. Rewrote the `ScopedPerspectiveTab` JSDoc paragraph to reflect both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n-- `kanban-app/ui/src/components/command-popover.tsx` — `<EnumField>` renders \"(none)\" as the first option when the param declares `clear_command`; submit-disabled rule relaxed so the clear sentinel is a legitimate submission.\n-- `kanban-app/ui/src/components/command-button.tsx` — `handleCommit` intercepts empty-string values for params with `clear_command` and dispatches the redirection target instead of the parent command (with the sentinel stripped from the args bag).\n-- `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` — file-level JSDoc updated to describe the Group leaf as a `<CommandButton>`, mirroring the Filter sentence.\n-- `kanban-app/ui/src/types/kanban.ts` — added `ParamDef.clear_command?: string` mirroring the Rust field; full JSDoc.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n - `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n - `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope` (positive case) and `perspective_group_command_drops_non_groupable_fields_end_to_end` (negative case, end-to-end through `build_dynamic_sources`).\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n\n ### Files deleted\n\n@@ -47,7 +41,7 @@\n\n ### Files created\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — seven cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set, \"(none)\" affordance renders when `clear_command` is present, picking \"(none)\" dispatches `perspective.clearGroup`, placeholder stays \"Pick…\" when no `clear_command`.\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n\n ## Acceptance Criteria\n\n@@ -66,13 +60,9 @@\n   - [x] `group_popover_renders_field_options_from_command_emission`\n   - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n   - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-  - [x] `group_popover_renders_none_option_when_clear_command_present`\n-  - [x] `picking_none_in_group_popover_dispatches_perspective_clearGroup`\n-  - [x] `group_popover_keeps_pick_placeholder_when_no_clear_command`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`, plus end-to-end negative case `perspective_group_command_drops_non_groupable_fields_end_to_end` that exercises the `groupable` filter through `build_dynamic_sources`.\n-- [x] Unit tests in `swissarmyhammer-kanban/src/dynamic_sources.rs` pin `denormalize_perspective_fields`: groupable kept in order; non-groupable dropped; caption does not bypass the filter; caption survives for groupable fields; unknown ids dropped; empty FieldsContext returns empty.\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n - [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `swissarmyhammer-cli` doctor `test_lsp_servers_check` test fails only under workspace parallelism and passes in isolation — unrelated to this task, environment-dependent) and `pnpm -C kanban-app/ui test` (2141 tests across 228 files pass).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n\n ## Workflow\n\n@@ -84,14 +74,10 @@\n\n ### Warnings\n\n-- [x] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n-  - **Resolution:** `denormalize_perspective_fields` now consults `fields_ctx.get_field_by_id(...).groupable == Some(true)` and drops any field that fails the predicate. Six new unit tests in `dynamic_sources.rs` pin the contract; new end-to-end integration test `perspective_group_command_drops_non_groupable_fields_end_to_end` in `tests/options_enrichment.rs` exercises the filter through `build_dynamic_sources` + `commands_for_scope` using real builtin fields (groupable `assignees` vs. non-groupable `title`).\n+- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-  - **Resolution:** Rewrote the paragraph to describe both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n-- [x] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-  - **Resolution:** File-level JSDoc rewritten to describe both Filter and Group leaves as `<CommandButton>`s with their parallel `perspective_tab.perspective.{filter.focus,group}:{id}` monikers.\n-- [x] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n-  - **Resolution:** Adopted via a new reusable YAML annotation `clear_command: \"<command-id>\"` on `ParamDef` (both Rust and TypeScript sides). When set, `<CommandPopover>` renders \"(none)\" as the first option in the enum select and treats the empty-string slot as a submittable value; `<CommandButton>.handleCommit` intercepts the empty-string sentinel and dispatches the `clear_command` target (with the sentinel stripped from the args bag) instead of the parent command. The Group YAML now carries `clear_command: \"perspective.clearGroup\"` on its `group` param, restoring the legacy single-popover \"None to clear\" affordance. The Sort migration can reuse the same annotation if it lands a similar UX need — the redirection stays out of the YAML body and stays consistent across surfaces. Three new frontend tests pin the contract: \"(none)\" renders when `clear_command` is set; picking it dispatches `perspective.clearGroup` (not `perspective.group`) with no `group` arg; the \"Pick…\" placeholder is preserved when `clear_command` is absent.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRGSYMBWPJVYH1N29MQTZH7A","timestamp":"2026-05-13T13:55:49.756086Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -7,21 +7,27 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n-  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n+  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n+  - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n   → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\", clear_command: \"perspective.clearGroup\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n ### Files modified\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `clear_command: perspective.clearGroup`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `swissarmyhammer-commands/src/types.rs` — added `ParamDef.clear_command: Option<String>` plus full docstring; updated two struct-literal sites in tests.\n+- `swissarmyhammer-kanban/src/dynamic_sources.rs` — `denormalize_perspective_fields` now filters perspective fields against `FieldDef.groupable == Some(true)`; six new unit tests pin the contract (groupable kept; non-groupable dropped; caption does not bypass the filter; unknown ids dropped; empty FieldsContext path).\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm. Rewrote the `ScopedPerspectiveTab` JSDoc paragraph to reflect both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n+- `kanban-app/ui/src/components/command-popover.tsx` — `<EnumField>` renders \"(none)\" as the first option when the param declares `clear_command`; submit-disabled rule relaxed so the clear sentinel is a legitimate submission.\n+- `kanban-app/ui/src/components/command-button.tsx` — `handleCommit` intercepts empty-string values for params with `clear_command` and dispatches the redirection target instead of the parent command (with the sentinel stripped from the args bag).\n+- `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` — file-level JSDoc updated to describe the Group leaf as a `<CommandButton>`, mirroring the Filter sentence.\n+- `kanban-app/ui/src/types/kanban.ts` — added `ParamDef.clear_command?: string` mirroring the Rust field; full JSDoc.\n - `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n - `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope` (positive case) and `perspective_group_command_drops_non_groupable_fields_end_to_end` (negative case, end-to-end through `build_dynamic_sources`).\n\n ### Files deleted\n\n@@ -31,7 +37,7 @@\n\n ### Files created\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — seven cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set, \"(none)\" affordance renders when `clear_command` is present, picking \"(none)\" dispatches `perspective.clearGroup`, placeholder stays \"Pick…\" when no `clear_command`.\n\n ## Acceptance Criteria\n\n@@ -50,9 +56,13 @@\n   - [x] `group_popover_renders_field_options_from_command_emission`\n   - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n   - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n+  - [x] `group_popover_renders_none_option_when_clear_command_present`\n+  - [x] `picking_none_in_group_popover_dispatches_perspective_clearGroup`\n+  - [x] `group_popover_keeps_pick_placeholder_when_no_clear_command`\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`, plus end-to-end negative case `perspective_group_command_drops_non_groupable_fields_end_to_end` that exercises the `groupable` filter through `build_dynamic_sources`.\n+- [x] Unit tests in `swissarmyhammer-kanban/src/dynamic_sources.rs` pin `denormalize_perspective_fields`: groupable kept in order; non-groupable dropped; caption does not bypass the filter; caption survives for groupable fields; unknown ids dropped; empty FieldsContext returns empty.\n - [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `swissarmyhammer-cli` doctor `test_lsp_servers_check` test fails only under workspace parallelism and passes in isolation — unrelated to this task, environment-dependent) and `pnpm -C kanban-app/ui test` (2141 tests across 228 files pass).\n\n ## Workflow\n\n@@ -64,10 +74,14 @@\n\n ### Warnings\n\n-- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+- [x] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n+  - **Resolution:** `denormalize_perspective_fields` now consults `fields_ctx.get_field_by_id(...).groupable == Some(true)` and drops any field that fails the predicate. Six new unit tests in `dynamic_sources.rs` pin the contract; new end-to-end integration test `perspective_group_command_drops_non_groupable_fields_end_to_end` in `tests/options_enrichment.rs` exercises the filter through `build_dynamic_sources` + `commands_for_scope` using real builtin fields (groupable `assignees` vs. non-groupable `title`).\n\n ### Nits\n\n-- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+  - **Resolution:** Rewrote the paragraph to describe both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n+- [x] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+  - **Resolution:** File-level JSDoc rewritten to describe both Filter and Group leaves as `<CommandButton>`s with their parallel `perspective_tab.perspective.{filter.focus,group}:{id}` monikers.\n+- [x] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n+  - **Resolution:** Adopted via a new reusable YAML annotation `clear_command: \"<command-id>\"` on `ParamDef` (both Rust and TypeScript sides). When set, `<CommandPopover>` renders \"(none)\" as the first option in the enum select and treats the empty-string slot as a submittable value; `<CommandButton>.handleCommit` intercepts the empty-string sentinel and dispatches the `clear_command` target (with the sentinel stripped from the args bag) instead of the parent command. The Group YAML now carries `clear_command: \"perspective.clearGroup\"` on its `group` param, restoring the legacy single-popover \"None to clear\" affordance. The Sort migration can reuse the same annotation if it lands a similar UX need — the redirection stays out of the YAML body and stays consistent across surfaces. Three new frontend tests pin the contract: \"(none)\" renders when `clear_command` is set; picking it dispatches `perspective.clearGroup` (not `perspective.group`) with no `group` arg; the \"Pick…\" placeholder is preserved when `clear_command` is absent.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -7,27 +7,21 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n-  - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n+  - Field filtering is owned by the backend: `PerspectiveFieldsResolver` projects whatever `PerspectiveFieldInfo` list the gather-time code produces. If a future ask requires limiting the list to `groupable: true` fields, that filter lives in `gather_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` — not in a frontend renderer override.\n+  - The \"None\" affordance is out of scope (per the task's own out-of-scope note) — `perspective.clearGroup` already lives in the right-click context menu.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n   → Deleted `kanban-app/ui/src/components/group-selector.tsx` and its unit test.\n\n-- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\", clear_command: \"perspective.clearGroup\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n+- **YAML annotation only.** The dispatcher path (`SetGroupCmd::execute`, `available()`, `resolve_and_persist_perspective_id`) is unchanged. The migration is a pure surface change: the YAML now tags the command with `tab_button: { icon: \"group\" }` and changes `params[0]` from `from: args` to `from: args, shape: enum, options_from: \"perspective.fields\"`. `perspective_id` switches to `from: scope_chain` with `entity_type: perspective`, matching the Filter migration's pattern. Existing palette and right-click tests continue to pass because the dispatcher still resolves `group` and `perspective_id` from any source.\n\n ### Files modified\n\n-- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `clear_command: perspective.clearGroup`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n-- `swissarmyhammer-commands/src/types.rs` — added `ParamDef.clear_command: Option<String>` plus full docstring; updated two struct-literal sites in tests.\n-- `swissarmyhammer-kanban/src/dynamic_sources.rs` — `denormalize_perspective_fields` now filters perspective fields against `FieldDef.groupable == Some(true)`; six new unit tests pin the contract (groupable kept; non-groupable dropped; caption does not bypass the filter; unknown ids dropped; empty FieldsContext path).\n-- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm. Rewrote the `ScopedPerspectiveTab` JSDoc paragraph to reflect both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n-- `kanban-app/ui/src/components/command-popover.tsx` — `<EnumField>` renders \"(none)\" as the first option when the param declares `clear_command`; submit-disabled rule relaxed so the clear sentinel is a legitimate submission.\n-- `kanban-app/ui/src/components/command-button.tsx` — `handleCommit` intercepts empty-string values for params with `clear_command` and dispatches the redirection target instead of the parent command (with the sentinel stripped from the args bag).\n-- `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` — file-level JSDoc updated to describe the Group leaf as a `<CommandButton>`, mirroring the Filter sentence.\n-- `kanban-app/ui/src/types/kanban.ts` — added `ParamDef.clear_command?: string` mirroring the Rust field; full JSDoc.\n+- `swissarmyhammer-kanban/builtin/commands/perspective.yaml` — annotated `perspective.group` with `tab_button: { icon: group }`, `shape: enum, options_from: perspective.fields`, `from: scope_chain` for perspective_id. Name changed from \"Set Group\" to \"Group By\".\n+- `kanban-app/ui/src/components/perspective-tab-bar.tsx` — deleted `<GroupPopoverButton>` definition, its JSX invocation, the local `groupOpen`/`schemaFields` state, and the no-longer-needed `Group` lucide / `useSchema` / `FieldDef` / `Popover*` / `GroupSelector` imports. Extended `isCommandActiveForPerspective` with a `case \"perspective.group\": return Boolean(perspective.group)` arm.\n - `kanban-app/ui/src/components/perspective-tab-bar.registry-driven.test.tsx` — replaced the legacy `screen.getByRole(\"button\", { name: \"Group\" })` assertion with explicit \"is now gone\" checks for both the legacy label and the new label.\n - `kanban-app/ui/src/components/perspective-tab-bar.test.tsx` — updated two comments that referenced `<GroupPopoverButton>`.\n-- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope` (positive case) and `perspective_group_command_drops_non_groupable_fields_end_to_end` (negative case, end-to-end through `build_dynamic_sources`).\n+- `swissarmyhammer-kanban/tests/options_enrichment.rs` — added `perspective_group_command_carries_field_options_when_perspective_in_scope`.\n\n ### Files deleted\n\n@@ -37,7 +31,7 @@\n\n ### Files created\n\n-- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — seven cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set, \"(none)\" affordance renders when `clear_command` is present, picking \"(none)\" dispatches `perspective.clearGroup`, placeholder stays \"Pick…\" when no `clear_command`.\n+- `kanban-app/ui/src/components/perspective-tab-bar.group-migration.test.tsx` — four cases: icon, popover renders backend options, picking dispatches with the right args, active highlight when perspective.group is set.\n\n ## Acceptance Criteria\n\n@@ -56,13 +50,9 @@\n   - [x] `group_popover_renders_field_options_from_command_emission`\n   - [x] `picking_a_group_field_dispatches_perspective_group_with_field_arg`\n   - [x] `group_button_is_active_when_perspective_has_a_group_set`\n-  - [x] `group_popover_renders_none_option_when_clear_command_present`\n-  - [x] `picking_none_in_group_popover_dispatches_perspective_clearGroup`\n-  - [x] `group_popover_keeps_pick_placeholder_when_no_clear_command`\n-- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`, plus end-to-end negative case `perspective_group_command_drops_non_groupable_fields_end_to_end` that exercises the `groupable` filter through `build_dynamic_sources`.\n-- [x] Unit tests in `swissarmyhammer-kanban/src/dynamic_sources.rs` pin `denormalize_perspective_fields`: groupable kept in order; non-groupable dropped; caption does not bypass the filter; caption survives for groupable fields; unknown ids dropped; empty FieldsContext returns empty.\n+- [x] Backend integration test `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs`.\n - [x] Update / remove the existing `group-popover-button.test.tsx` (no such file existed — covered by deletion of `<GroupPopoverButton>` in `perspective-tab-bar.tsx`) and `perspective-tab-bar.group-enter.spatial.test.tsx` (deleted — superseded by `<CommandButton>` generic test coverage).\n-- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `swissarmyhammer-cli` doctor `test_lsp_servers_check` test fails only under workspace parallelism and passes in isolation — unrelated to this task, environment-dependent) and `pnpm -C kanban-app/ui test` (2141 tests across 228 files pass).\n+- [x] Run: `cargo test --workspace` (passes; one pre-existing flaky `shelltool-cli` test fails only under workspace parallelism and passes in isolation — unrelated to this task) and `pnpm -C kanban-app/ui test perspective-tab-bar` (2138 tests across 228 files pass).\n\n ## Workflow\n\n@@ -74,14 +64,10 @@\n\n ### Warnings\n\n-- [x] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n-  - **Resolution:** `denormalize_perspective_fields` now consults `fields_ctx.get_field_by_id(...).groupable == Some(true)` and drops any field that fails the predicate. Six new unit tests in `dynamic_sources.rs` pin the contract; new end-to-end integration test `perspective_group_command_drops_non_groupable_fields_end_to_end` in `tests/options_enrichment.rs` exercises the filter through `build_dynamic_sources` + `commands_for_scope` using real builtin fields (groupable `assignees` vs. non-groupable `title`).\n+- [ ] `swissarmyhammer-kanban/src/dynamic_sources.rs::denormalize_perspective_fields` and `swissarmyhammer-perspectives/src/options_resolvers.rs::PerspectiveFieldsResolver::resolve` — **the `groupable` filter does not exist anywhere on the new path.** The legacy `<GroupSelector>` filtered to `f.groupable === true` so users only saw fields they could actually group by. The task description states this filter \"moved to backend `PerspectiveFieldsResolver`/`gather_perspective_fields`\"; that is not what the code does. `denormalize_perspective_fields` returns every perspective field with a resolvable display name, and `PerspectiveFieldsResolver` passes the list through unchanged. `FieldDef.groupable` exists on the entity schema (see `swissarmyhammer-kanban/src/defaults.rs`) but is not consulted on the gather path. Net effect: the new Group By popover surfaces fields the user can't usefully group on (e.g. text-heavy fields, IDs). Fix: filter `perspective.fields[].field` against `fields_ctx.get_field_by_id(...).groupable == Some(true)` inside `denormalize_perspective_fields`, then update the resolver test fixture so it can pin the filter (e.g. add a non-groupable field and assert it is dropped). The frontend `group-migration.test.tsx` does not need to change because the test fixtures already pass groupable-shaped fields, but the backend `perspective_group_command_carries_field_options_when_perspective_in_scope` test should be extended to cover the negative case.\n\n ### Nits\n\n-- [x] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n-  - **Resolution:** Rewrote the paragraph to describe both Filter and Group as `<CommandButton>` leaves at `perspective_tab.perspective.{filter.focus,group}:{id}`.\n-- [x] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n-  - **Resolution:** File-level JSDoc rewritten to describe both Filter and Group leaves as `<CommandButton>`s with their parallel `perspective_tab.perspective.{filter.focus,group}:{id}` monikers.\n-- [x] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n-  - **Resolution:** Adopted via a new reusable YAML annotation `clear_command: \"<command-id>\"` on `ParamDef` (both Rust and TypeScript sides). When set, `<CommandPopover>` renders \"(none)\" as the first option in the enum select and treats the empty-string slot as a submittable value; `<CommandButton>.handleCommit` intercepts the empty-string sentinel and dispatches the `clear_command` target (with the sentinel stripped from the args bag) instead of the parent command. The Group YAML now carries `clear_command: \"perspective.clearGroup\"` on its `group` param, restoring the legacy single-popover \"None to clear\" affordance. The Sort migration can reuse the same annotation if it lands a similar UX need — the redirection stays out of the YAML body and stays consistent across surfaces. Three new frontend tests pin the contract: \"(none)\" renders when `clear_command` is set; picking it dispatches `perspective.clearGroup` (not `perspective.group`) with no `group` arg; the \"Pick…\" placeholder is preserved when `clear_command` is absent.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/components/perspective-tab-bar.tsx` JSDoc on `ScopedPerspectiveTab` — \"while the group icon remains a Pressable leaf (`perspective_tab.group:{id}`)\". Stale after this migration; the group icon is now a `<CommandButton>` and the moniker is `perspective_tab.perspective.group:{id}`. Rewrite as a parallel of the filter-affordance sentence above it (\"the group affordance is now a `<CommandButton>` leaf (`perspective_tab.perspective.group:{id}`)\").\n+- [ ] `kanban-app/ui/src/components/perspective-spatial-nav.guards.node.test.ts` file-level JSDoc — \"(and, when rendered, the `perspective_tab.perspective.filter.focus:{id}` `<CommandButton>` leaf … and the `perspective_tab.group:{id}` Pressable icon button) FocusScope leaves.\" Stale after this migration: the group icon is no longer a Pressable. Mirror the filter wording: \"and the `perspective_tab.perspective.group:{id}` `<CommandButton>` leaf\". The actual `it()` assertions in the file are unaffected — only the file-level descriptive comment.\n+- [ ] \"None\" / clear-group UX — the legacy `<GroupSelector>` had a \"None\" entry that dispatched `perspective.clearGroup` inline. After the migration the only way to clear a group is right-click → \"Clear Group\" (context-menu entry). The task description marks this as intentional and out of scope, and the end state is reachable, but the discoverability is materially lower than the legacy single-popover flow. Consider adding a \"None\" / \"(no grouping)\" entry as the first `<option>` in the enum picker (value `\"\"`, label `\"None\"`) that, when selected and submitted, dispatches `perspective.clearGroup` instead of `perspective.group` — handled at the `<CommandPopover>` commit boundary so the dispatch redirection stays out of the YAML. If not adopted now, capture as a follow-up so the regression doesn't bake in across the rest of the epic's migrations.\n\\ No newline at end of file\n"}]]}
{"id":"01KRGT07PHVZXN25W3VXPBVCNX","timestamp":"2026-05-13T13:56:42.321281Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGT07PHWF0NQMJ427BJBCH4","timestamp":"2026-05-13T13:56:42.321934Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KRGTB01PNY506JMBDWFVXN87","timestamp":"2026-05-13T14:02:34.934254Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGTB01QMSB0D7WACP5DWHS2","timestamp":"2026-05-13T14:02:34.935136Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KRGTJMJ7D8H0TRTJPDN080R2","timestamp":"2026-05-13T14:06:45.319381Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -108,5 +108,7 @@\n\n ### Nits\n\n-- [ ] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n-- [ ] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n\\ No newline at end of file\n+- [x] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n+  - **Resolution:** Added `command_def_with_param_clear_command_round_trips` in `swissarmyhammer-commands/src/types.rs` — builds a `ParamDef` with `clear_command: Some(\"perspective.clearGroup\".into())`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts both the full struct equality and the explicit `clear_command.as_deref()` value survive. Mirrors the existing `command_def_with_param_shape_and_options_round_trips` naming/style so the Sort migration's reviewer finds both halves of the contract together.\n+- [x] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n+  - **Resolution:** Extended `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs` with the requested assertion — after the existing `options_from` check, the test now asserts `group_param.clear_command.as_deref() == Some(\"perspective.clearGroup\")`. Pins the full builtin-YAML → `CommandsRegistry::from_yaml_sources` → `commands_for_scope` → emitted-`ParamDef` plumbing for the annotation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -108,7 +108,5 @@\n\n ### Nits\n\n-- [x] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n-  - **Resolution:** Added `command_def_with_param_clear_command_round_trips` in `swissarmyhammer-commands/src/types.rs` — builds a `ParamDef` with `clear_command: Some(\"perspective.clearGroup\".into())`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts both the full struct equality and the explicit `clear_command.as_deref()` value survive. Mirrors the existing `command_def_with_param_shape_and_options_round_trips` naming/style so the Sort migration's reviewer finds both halves of the contract together.\n-- [x] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n-  - **Resolution:** Extended `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs` with the requested assertion — after the existing `options_from` check, the test now asserts `group_param.clear_command.as_deref() == Some(\"perspective.clearGroup\")`. Pins the full builtin-YAML → `CommandsRegistry::from_yaml_sources` → `commands_for_scope` → emitted-`ParamDef` plumbing for the annotation.\n\\ No newline at end of file\n+- [ ] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n+- [ ] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KRGTJMJ7NR4XZ8YS4S6ATHT2","timestamp":"2026-05-13T14:06:45.319899Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -98,5 +98,7 @@\n\n ### Nits\n\n-- [ ] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n-- [ ] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n\\ No newline at end of file\n+- [x] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n+  - **Resolution:** Added `command_def_with_param_clear_command_round_trips` in `swissarmyhammer-commands/src/types.rs` — builds a `ParamDef` with `clear_command: Some(\"perspective.clearGroup\".into())`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts both the full struct equality and the explicit `clear_command.as_deref()` value survive. Mirrors the existing `command_def_with_param_shape_and_options_round_trips` naming/style so the Sort migration's reviewer finds both halves of the contract together.\n+- [x] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n+  - **Resolution:** Extended `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs` with the requested assertion — after the existing `options_from` check, the test now asserts `group_param.clear_command.as_deref() == Some(\"perspective.clearGroup\")`. Pins the full builtin-YAML → `CommandsRegistry::from_yaml_sources` → `commands_for_scope` → emitted-`ParamDef` plumbing for the annotation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -98,7 +98,5 @@\n\n ### Nits\n\n-- [x] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n-  - **Resolution:** Added `command_def_with_param_clear_command_round_trips` in `swissarmyhammer-commands/src/types.rs` — builds a `ParamDef` with `clear_command: Some(\"perspective.clearGroup\".into())`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts both the full struct equality and the explicit `clear_command.as_deref()` value survive. Mirrors the existing `command_def_with_param_shape_and_options_round_trips` naming/style so the Sort migration's reviewer finds both halves of the contract together.\n-- [x] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n-  - **Resolution:** Extended `perspective_group_command_carries_field_options_when_perspective_in_scope` in `swissarmyhammer-kanban/tests/options_enrichment.rs` with the requested assertion — after the existing `options_from` check, the test now asserts `group_param.clear_command.as_deref() == Some(\"perspective.clearGroup\")`. Pins the full builtin-YAML → `CommandsRegistry::from_yaml_sources` → `commands_for_scope` → emitted-`ParamDef` plumbing for the annotation.\n\\ No newline at end of file\n+- [ ] **YAML round-trip for `clear_command: Some(...)` is not pinned.** `swissarmyhammer-commands/src/types.rs::command_def_with_param_shape_and_options_round_trips` only round-trips `clear_command: None`, so the serialization-with-value path is untested. The serde attributes are correct (`#[serde(default, skip_serializing_if = \"Option::is_none\")]` + plain `Option<String>`) and the field works end-to-end, but the contract isn't pinned. Add a tiny round-trip test that builds a `ParamDef { clear_command: Some(\"perspective.clearGroup\".into()), .. }`, runs it through `serde_yaml_ng::to_string` → `from_str`, and asserts the field survives. The Sort migration will silently regress here if it lands while this field is invariant-free.\n+- [ ] **YAML-to-component plumbing for `clear_command` is not pinned with the real YAML value.** The three new `group-migration.test.tsx` tests inject a fixture object literal carrying `clear_command: \"perspective.clearGroup\"` — they don't exercise the path that loads `perspective.yaml` via `CommandsRegistry::from_yaml_sources`, runs `commands_for_scope`, and asserts the emitted `perspective.group` ParamDef carries `clear_command == Some(\"perspective.clearGroup\")`. The existing `perspective_group_command_carries_field_options_when_perspective_in_scope` test in `tests/options_enrichment.rs` is the right place to add the assertion — it already loads the registry and finds the command. One extra line (`assert_eq!(group_param.clear_command.as_deref(), Some(\"perspective.clearGroup\"));`) is enough to pin the YAML-to-emission plumbing.\n\\ No newline at end of file\n"}]]}
{"id":"01KRGTM5JTT914T5DPGF0XY80X","timestamp":"2026-05-13T14:07:35.514245Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8180'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGTM5JTM54P2ZX3NH42YVQY","timestamp":"2026-05-13T14:07:35.514968Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KRGTQD5XXKCK6JAAPQTZGEV9","timestamp":"2026-05-13T14:09:21.597589Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffdc80\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KRE1WT72MJWNGQBVAD4V5VKM\n - 01KRE7VDF7RXHV39VPEVH23NN4\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffdc80\n+position_column: review\n+position_ordinal: '80'\n title: Migrate Group tab button to command-driven rendering with field picker\n ---\n ## What\n","transaction_id":null}
{"id":"01KRGTQD5YKKFCTM48Q235AK3A","timestamp":"2026-05-13T14:09:21.598112Z","entity_type":"task","entity_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffffdc80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffffdc80\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01M129VPXT7PW1G4CQDV8TPSWN","timestamp":"2026-08-27T19:06:31.226177Z","op":"update","item_id":"01KRE1ZTYJ5PPTQ29K72KE88B5","forward_patch":"--- original\n+++ modified\n@@ -17,7 +17,7 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n+  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding `#1` below — and is now in place with full unit-test coverage.)\n   - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -17,7 +17,7 @@\n ### Implementation notes\n\n - **`<GroupSelector>` decision: DELETED.** The legacy selector did three things `<CommandPopover>`'s generic enum renderer can't do natively: filter to `groupable === true` fields, include a \"None\" option dispatching `perspective.clearGroup`, and render each field as a dedicated button. The right replacements:\n-  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding `#1` below — and is now in place with full unit-test coverage.)\n+  - Field filtering is owned by the backend: `denormalize_perspective_fields` in `swissarmyhammer-kanban/src/dynamic_sources.rs` filters perspective fields against `FieldDef.groupable == Some(true)` so the Group By popover only surfaces fields the user can actually group on. (This filter was missing on the first pass — see review-finding #1 below — and is now in place with full unit-test coverage.)\n   - The \"None\" affordance is restored via a `clear_command: \"perspective.clearGroup\"` annotation on the YAML param. `<CommandPopover>` renders \"(none)\" as the first option whenever a param carries `clear_command`, and `<CommandButton>`'s commit handler intercepts the empty-string sentinel to dispatch the clear command instead of the parent command.\n   - The plain `<select>` from `<CommandPopover>` covers the field-list UX without any virtualization, search, or icon needs.\n\n","transaction_id":null}
