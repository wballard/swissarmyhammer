{"id":"01M0MVM2VZAQPKA1QVK1GPKFBH","timestamp":"2026-08-22T13:47:33.631973Z","op":"create","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,51 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ffe980\n+project: swift-validator\n+title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n+---\n+We already drive real `swiftlint` from four `code-hygiene` rules. We use it for function-length, magic-numbers and missing-docs only. SwiftLint decides six more things our prompt rules currently judge, and Airbnb ships three custom regex rules that convert judgment into a deterministic check.\n+\n+Source: https://raw.githubusercontent.com/airbnb/swift/master/Sources/AirbnbSwiftFormatTool/swiftlint.yml\n+\n+## Stock rules to turn on\n+\n+| SwiftLint rule | Supersedes (prompt rule) |\n+|---|---|\n+| `implicitly_unwrapped_optional` | `optionals.md` no IUO |\n+| `force_unwrapping` | `optionals.md` no force unwrap in non-test code |\n+| `force_try` | `error-handling.md` no `try!` in non-test code |\n+| `force_cast` | `error-handling.md` no `as!` in non-test code |\n+| `unused_optional_binding` | `optionals.md` check for nil, do not bind an unused value |\n+| `unowned_variable_capture` | (new — we have no rule) |\n+| `legacy_constructor`, `legacy_nsgeometry_functions`, `legacy_cggeometry_functions` | (new — we have no rule) |\n+\n+Keep our existing test-code carve-outs. `force_unwrapping`, `force_try` and `force_cast` must stay off inside test targets — our rules say \"in non-test code\" and mean it. Airbnb solves this with `noForceUnwrapInTests` / `noForceTryInTests`, which apply the opposite polarity in tests. Honour the same split.\n+\n+## The three custom regex rules — copy these\n+\n+Each one turns an LLM judgment into a regex plus an escape hatch:\n+\n+1. `no_direct_standard_out_logs` — bans `print`, `debugPrint`, `dump`, `Swift.print`, `_printChanges`, scoped by `match_kinds: [identifier]`. We have NO rule for this.\n+2. `no_file_literal` — bans `#file`, scoped by `match_kinds: [keyword]`. We have NO rule for this.\n+3. `no_unchecked_sendable` — bans `@unchecked Sendable`, scoped by `match_kinds: [attribute.builtin, typeidentifier]`.\n+\n+## The mechanism that matters most\n+\n+Read Airbnb's `no_unchecked_sendable` message:\n+\n+> \"If you really must use `@unchecked Sendable`, you can add a `// swiftlint:disable:next no_unchecked_sendable` annotation with an explanation for how we know the type is thread-safe…\"\n+\n+The escape hatch IS the documentation. Our `concurrency.md` states the same requirement as pure judgment: \"`@unchecked Sendable` requires a documented synchronization invariant. The smell is the *absence* of a lock/isolation mechanism and a comment.\" A tool cannot judge \"is there a documented invariant\" — but it CAN require an explicit annotation, and the annotation text is the invariant.\n+\n+This is the `objectivity-over-judgment` standing order working in production: exemptions become annotations. Write the finding message so it tells the author exactly what to write, as Airbnb's does.\n+\n+## Acceptance\n+\n+- Pass and fail fixtures per rule group, in `builtin/validators/code-hygiene/fixtures/`.\n+- A shipped acceptance test driving REAL `swiftlint`. Model it on `missing_docs.rs:2049-2165` and `magic_numbers.rs:243-330`.\n+- A test proving the annotation escape hatch works: a `@unchecked Sendable` with the disable comment produces NO finding; the same code without it produces one.\n+- A test proving the test-target polarity split: a force unwrap in `Sources/` reports; the same in `Tests/` does not.\n+- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,51 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ffe980\n-project: swift-validator\n-title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n----\n-We already drive real `swiftlint` from four `code-hygiene` rules. We use it for function-length, magic-numbers and missing-docs only. SwiftLint decides six more things our prompt rules currently judge, and Airbnb ships three custom regex rules that convert judgment into a deterministic check.\n-\n-Source: https://raw.githubusercontent.com/airbnb/swift/master/Sources/AirbnbSwiftFormatTool/swiftlint.yml\n-\n-## Stock rules to turn on\n-\n-| SwiftLint rule | Supersedes (prompt rule) |\n-|---|---|\n-| `implicitly_unwrapped_optional` | `optionals.md` no IUO |\n-| `force_unwrapping` | `optionals.md` no force unwrap in non-test code |\n-| `force_try` | `error-handling.md` no `try!` in non-test code |\n-| `force_cast` | `error-handling.md` no `as!` in non-test code |\n-| `unused_optional_binding` | `optionals.md` check for nil, do not bind an unused value |\n-| `unowned_variable_capture` | (new — we have no rule) |\n-| `legacy_constructor`, `legacy_nsgeometry_functions`, `legacy_cggeometry_functions` | (new — we have no rule) |\n-\n-Keep our existing test-code carve-outs. `force_unwrapping`, `force_try` and `force_cast` must stay off inside test targets — our rules say \"in non-test code\" and mean it. Airbnb solves this with `noForceUnwrapInTests` / `noForceTryInTests`, which apply the opposite polarity in tests. Honour the same split.\n-\n-## The three custom regex rules — copy these\n-\n-Each one turns an LLM judgment into a regex plus an escape hatch:\n-\n-1. `no_direct_standard_out_logs` — bans `print`, `debugPrint`, `dump`, `Swift.print`, `_printChanges`, scoped by `match_kinds: [identifier]`. We have NO rule for this.\n-2. `no_file_literal` — bans `#file`, scoped by `match_kinds: [keyword]`. We have NO rule for this.\n-3. `no_unchecked_sendable` — bans `@unchecked Sendable`, scoped by `match_kinds: [attribute.builtin, typeidentifier]`.\n-\n-## The mechanism that matters most\n-\n-Read Airbnb's `no_unchecked_sendable` message:\n-\n-> \"If you really must use `@unchecked Sendable`, you can add a `// swiftlint:disable:next no_unchecked_sendable` annotation with an explanation for how we know the type is thread-safe…\"\n-\n-The escape hatch IS the documentation. Our `concurrency.md` states the same requirement as pure judgment: \"`@unchecked Sendable` requires a documented synchronization invariant. The smell is the *absence* of a lock/isolation mechanism and a comment.\" A tool cannot judge \"is there a documented invariant\" — but it CAN require an explicit annotation, and the annotation text is the invariant.\n-\n-This is the `objectivity-over-judgment` standing order working in production: exemptions become annotations. Write the finding message so it tells the author exactly what to write, as Airbnb's does.\n-\n-## Acceptance\n-\n-- Pass and fail fixtures per rule group, in `builtin/validators/code-hygiene/fixtures/`.\n-- A shipped acceptance test driving REAL `swiftlint`. Model it on `missing_docs.rs:2049-2165` and `magic_numbers.rs:243-330`.\n-- A test proving the annotation escape hatch works: a `@unchecked Sendable` with the disable comment produces NO finding; the same code without it produces one.\n-- A test proving the test-target polarity split: a force unwrap in `Sources/` reports; the same in `Tests/` does not.\n-- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0MYS47934M9NZCM2WN47A5X","timestamp":"2026-08-22T14:42:44.585841Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ffe980\n+position_column: doing\n+position_ordinal: '8280'\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: ffe980\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","transaction_id":null}
{"id":"01M0MZ7F6ZYMF5G8XMBCKMMS5P","timestamp":"2026-08-22T14:50:34.591945Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,28 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m0mz7f6zshwwvym4v8m2dp4z\n+  text: |-\n+    Research done. Measurements taken with swiftlint 0.65.0 before any edit.\n+\n+    **Every rule name the card lists is PRESENT in swiftlint 0.65.0.** Checked each of the nine against `swiftlint rules`: `implicitly_unwrapped_optional`, `force_unwrapping`, `force_try`, `force_cast`, `unused_optional_binding`, `unowned_variable_capture`, `legacy_constructor`, `legacy_nsgeometry_functions`, `legacy_cggeometry_functions`. No name has to be dropped.\n+\n+    **`force_try` and `force_cast` default to `severity: error`.** An error-severity finding makes swiftlint exit 2. The child config states `severity: warning` for every rule, so the run exits 0.\n+\n+    **A per-rule `excluded:` key does NOT work for a stock rule, and swiftlint only WARNS about it.** Measured over one force unwrap in `Sources/` and the same in `Tests/`, with `force_unwrapping: {excluded: \".*/Tests/.*\"}`: swiftlint reported BOTH files, exit 0, and wrote `warning: Configuration for 'force_unwrapping' rule contains the invalid key(s) 'excluded'.` So the test-target split cannot live in the configuration. It has to be the script's own partition.\n+\n+    **Airbnb's own swiftlint.yml does NOT name the three force rules.** Fetched from master. Their `only_rules` names `fatal_error_message`, `implicitly_unwrapped_optional`, the three legacy rules, `legacy_constant`, `unused_optional_binding`, `unowned_variable_capture`, `superfluous_disable_command`, `invalid_swiftlint_command`, `custom_rules`. The force rules are handled on the swiftformat side by `noForceUnwrapInTests` / `noForceTryInTests`, which is the polarity the card names. The three custom regex rules were copied verbatim from that file.\n+\n+    **The three custom rules work, and `match_kinds` does the scoping the card claims.** Measured over one probe file: `print`, `debugPrint`, `dump` and `Swift.print` each report `no_direct_standard_out_logs`; `#file` reports `no_file_literal`; `@unchecked Sendable` reports `no_unchecked_sendable`. The string literal `\"print(this is a string, not a call)\"` and the comment `// print(this is a comment)` each report NOTHING — `match_kinds: [identifier]` is what does that. `rule_id` in the JSON report is the custom rule's own key.\n+\n+    **The annotation escape hatch works.** The same `@unchecked Sendable` class under `// swiftlint:disable:next no_unchecked_sendable  the value is immutable` reported nothing, while the class without the comment reported.\n+\n+    **swiftlint reports the SYMLINK-RESOLVED absolute path in `.file`.** Measured: `cd \"$(dirname \"$f\")\" &amp;&amp; pwd -P` plus the basename reproduces it exactly, through a `./` prefix and through a symlinked working directory. That is what lets the test-file partition be an exact string match rather than a regex.\n+\n+    **`swift package describe` gives the authoritative test target paths and costs 0.19-0.36 s.** It reports a custom `path:` target (`Custom/OddTests`) that no convention would find, it does NOT resolve dependencies (measured: exit 0 beside a dependency at `https://example.invalid/nope.git`), and `swift package --scratch-path &lt;tmp&gt; describe` keeps the `.build` directory out of the workspace.\n+  timestamp: 2026-08-22T14:50:34.591691+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -1,28 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m0mz7f6zshwwvym4v8m2dp4z\n-  text: |-\n-    Research done. Measurements taken with swiftlint 0.65.0 before any edit.\n-\n-    **Every rule name the card lists is PRESENT in swiftlint 0.65.0.** Checked each of the nine against `swiftlint rules`: `implicitly_unwrapped_optional`, `force_unwrapping`, `force_try`, `force_cast`, `unused_optional_binding`, `unowned_variable_capture`, `legacy_constructor`, `legacy_nsgeometry_functions`, `legacy_cggeometry_functions`. No name has to be dropped.\n-\n-    **`force_try` and `force_cast` default to `severity: error`.** An error-severity finding makes swiftlint exit 2. The child config states `severity: warning` for every rule, so the run exits 0.\n-\n-    **A per-rule `excluded:` key does NOT work for a stock rule, and swiftlint only WARNS about it.** Measured over one force unwrap in `Sources/` and the same in `Tests/`, with `force_unwrapping: {excluded: \".*/Tests/.*\"}`: swiftlint reported BOTH files, exit 0, and wrote `warning: Configuration for 'force_unwrapping' rule contains the invalid key(s) 'excluded'.` So the test-target split cannot live in the configuration. It has to be the script's own partition.\n-\n-    **Airbnb's own swiftlint.yml does NOT name the three force rules.** Fetched from master. Their `only_rules` names `fatal_error_message`, `implicitly_unwrapped_optional`, the three legacy rules, `legacy_constant`, `unused_optional_binding`, `unowned_variable_capture`, `superfluous_disable_command`, `invalid_swiftlint_command`, `custom_rules`. The force rules are handled on the swiftformat side by `noForceUnwrapInTests` / `noForceTryInTests`, which is the polarity the card names. The three custom regex rules were copied verbatim from that file.\n-\n-    **The three custom rules work, and `match_kinds` does the scoping the card claims.** Measured over one probe file: `print`, `debugPrint`, `dump` and `Swift.print` each report `no_direct_standard_out_logs`; `#file` reports `no_file_literal`; `@unchecked Sendable` reports `no_unchecked_sendable`. The string literal `\"print(this is a string, not a call)\"` and the comment `// print(this is a comment)` each report NOTHING — `match_kinds: [identifier]` is what does that. `rule_id` in the JSON report is the custom rule's own key.\n-\n-    **The annotation escape hatch works.** The same `@unchecked Sendable` class under `// swiftlint:disable:next no_unchecked_sendable  the value is immutable` reported nothing, while the class without the comment reported.\n-\n-    **swiftlint reports the SYMLINK-RESOLVED absolute path in `.file`.** Measured: `cd \"$(dirname \"$f\")\" &amp;&amp; pwd -P` plus the basename reproduces it exactly, through a `./` prefix and through a symlinked working directory. That is what lets the test-file partition be an exact string match rather than a regex.\n-\n-    **`swift package describe` gives the authoritative test target paths and costs 0.19-0.36 s.** It reports a custom `path:` target (`Custom/OddTests`) that no convention would find, it does NOT resolve dependencies (measured: exit 0 beside a dependency at `https://example.invalid/nope.git`), and `swift package --scratch-path &lt;tmp&gt; describe` keeps the `.build` directory out of the workspace.\n-  timestamp: 2026-08-22T14:50:34.591691+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N0J6KWE8DZD38D90B337AB","timestamp":"2026-08-22T15:13:54.812480Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -23,6 +23,37 @@\n\n     **`swift package describe` gives the authoritative test target paths and costs 0.19-0.36 s.** It reports a custom `path:` target (`Custom/OddTests`) that no convention would find, it does NOT resolve dependencies (measured: exit 0 beside a dependency at `https://example.invalid/nope.git`), and `swift package --scratch-path &lt;tmp&gt; describe` keeps the `.build` directory out of the workspace.\n   timestamp: 2026-08-22T14:50:34.591691+00:00\n+- actor: claude-code\n+  id: 01m0n0j6kw3s2tasj32d23g2b1\n+  text: |-\n+    Implementation landed as `disallowed-constructs-swift`, a `files`-scope tool rule in `code-hygiene`.\n+\n+    **Design decisions, and what each was measured against.**\n+\n+    The rule runs ONE swiftlint invocation over every path it is given, with a child config that states `only_rules` (nine stock names plus `custom_rules`) and every option of every rule. It reuses the whole parent/child, status-gate and three-stderr-reading skeleton of `magic-numbers-swift`, `missing-docs-swift` and `function-length-swift`, so the shipped swiftlint rules are now four and share one shape.\n+\n+    **The test-target polarity split is the script's own partition, because it cannot be anything else.** swiftlint has no per-rule path filter for a stock rule and it does not refuse the key: measured, `excluded:` under `force_unwrapping:` is answered with a WARNING and both files still report. So the script builds a list of test-file paths and the `jq` filter drops a `force_unwrapping`, `force_try` or `force_cast` finding whose `file` is in that list. Two facts feed the list, as a UNION:\n+\n+    - the `Tests/` convention — a path whose first component is `Tests`, or which holds a `Tests` component anywhere;\n+    - the test targets `swift package describe --type json` names, when the workspace root holds a `Package.swift`.\n+\n+    The manifest read is GUARDED, never required: `[ -f Package.swift ]` stands first, `2>/dev/null` swallows a manifest the tool cannot read, and a nonzero status leaves the convention standing alone — so `swift` is absent from `check_command` and a machine without it keeps the whole gate. Measured with `swift` off the PATH: `Tests/ProbeTests` keeps the carve-out and `Custom/OddTests` loses it. `swift package --scratch-path <tmp> describe` keeps `.build` out of the workspace; measured, the probe package holds no `.build` after the run.\n+\n+    The partition compares WHOLE resolved absolute paths rather than a regex, because a directory name holding a regex metacharacter would silently stop matching.\n+\n+    **A dead end worth recording.** The first probe for the manifest test staged `Package.swift` naming three targets and a file for only two of them. SPM refuses to describe such a package — `error: Source files for target ProbeTests should be located under 'Tests/ProbeTests'` at exit 1 — so the run fell back to the convention and the test failed for the right reason. The probe now stages a file under `Tests/ProbeTests` as well, and the test doc records the trap.\n+\n+    **Findings the card's table did not match, corrected against the rule files.**\n+\n+    - `unused_optional_binding` decides NO shipped prompt bullet. `builtin/validators/swift/rules/optionals.md` holds four bullets — force unwrap, `guard let` early exit, IUO, and no `Optional` where a default fits — and none of them is \"check for nil, do not bind an unused value\". The rule body puts `unused_optional_binding` in the \"no shipped prompt rule asks\" group. The gate therefore decides FIVE bullets across three prompt rules, not six.\n+    - `optionals.md`'s IUO bullet sanctions two exceptions, `@IBOutlet` and \"test fixtures set in `setUp()`\". `mode: all_except_iboutlets` expresses the first; measured, no swiftlint option expresses the second, and `swiftlint rules implicitly_unwrapped_optional` names only `severity` and `mode`. `error-handling.md`'s `try!` bullet has the same shape, and `swiftlint rules force_try` names only `severity`. Both gaps are documented in the rule body under \"Two prompt carve-outs no option expresses\", with the directive as the recourse, and card ^c8fj21y was created to decide whether `implicitly_unwrapped_optional` should join the test-target partition.\n+\n+    **`supersedes` is empty**, per the card. `builtin/validators/README.md` said only two shipped tool rules declare no `supersedes`; that sentence was already stale for `idioms-swift` and is now corrected to name all four, with the reason the two Swift rules leave it empty stated separately from the reason the other two do.\n+\n+    **A gap in the sibling card's work, closed here.** `CODE_HYGIENE_IDIOMS_TOOL_RULES` was added to `expected_rules` and to the length sum of `test_code_hygiene_loads_its_rule_roster_and_the_callers_probe`, and NOT to `expected_supersedes` — the chain that actually asserts `supersedes.names()`. So nothing held `idioms-swift` to superseding nothing. Both idioms and disallowed-constructs are now in that chain.\n+\n+    **Counts re-measured over `builtin/validators/*/rules/*.md`**, not derived by arithmetic: 27 rules carry a `run` script, 16 state `scope: files`, 11 state `scope: workspace`, 24 call `mktemp`. Of the 16 files-scope scripts, 4 write the zero-argument guard on the first line and 12 write it under `set -e` alone.\n+  timestamp: 2026-08-22T15:13:54.812168+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -23,37 +23,6 @@\n\n     **`swift package describe` gives the authoritative test target paths and costs 0.19-0.36 s.** It reports a custom `path:` target (`Custom/OddTests`) that no convention would find, it does NOT resolve dependencies (measured: exit 0 beside a dependency at `https://example.invalid/nope.git`), and `swift package --scratch-path &lt;tmp&gt; describe` keeps the `.build` directory out of the workspace.\n   timestamp: 2026-08-22T14:50:34.591691+00:00\n-- actor: claude-code\n-  id: 01m0n0j6kw3s2tasj32d23g2b1\n-  text: |-\n-    Implementation landed as `disallowed-constructs-swift`, a `files`-scope tool rule in `code-hygiene`.\n-\n-    **Design decisions, and what each was measured against.**\n-\n-    The rule runs ONE swiftlint invocation over every path it is given, with a child config that states `only_rules` (nine stock names plus `custom_rules`) and every option of every rule. It reuses the whole parent/child, status-gate and three-stderr-reading skeleton of `magic-numbers-swift`, `missing-docs-swift` and `function-length-swift`, so the shipped swiftlint rules are now four and share one shape.\n-\n-    **The test-target polarity split is the script's own partition, because it cannot be anything else.** swiftlint has no per-rule path filter for a stock rule and it does not refuse the key: measured, `excluded:` under `force_unwrapping:` is answered with a WARNING and both files still report. So the script builds a list of test-file paths and the `jq` filter drops a `force_unwrapping`, `force_try` or `force_cast` finding whose `file` is in that list. Two facts feed the list, as a UNION:\n-\n-    - the `Tests/` convention — a path whose first component is `Tests`, or which holds a `Tests` component anywhere;\n-    - the test targets `swift package describe --type json` names, when the workspace root holds a `Package.swift`.\n-\n-    The manifest read is GUARDED, never required: `[ -f Package.swift ]` stands first, `2>/dev/null` swallows a manifest the tool cannot read, and a nonzero status leaves the convention standing alone — so `swift` is absent from `check_command` and a machine without it keeps the whole gate. Measured with `swift` off the PATH: `Tests/ProbeTests` keeps the carve-out and `Custom/OddTests` loses it. `swift package --scratch-path <tmp> describe` keeps `.build` out of the workspace; measured, the probe package holds no `.build` after the run.\n-\n-    The partition compares WHOLE resolved absolute paths rather than a regex, because a directory name holding a regex metacharacter would silently stop matching.\n-\n-    **A dead end worth recording.** The first probe for the manifest test staged `Package.swift` naming three targets and a file for only two of them. SPM refuses to describe such a package — `error: Source files for target ProbeTests should be located under 'Tests/ProbeTests'` at exit 1 — so the run fell back to the convention and the test failed for the right reason. The probe now stages a file under `Tests/ProbeTests` as well, and the test doc records the trap.\n-\n-    **Findings the card's table did not match, corrected against the rule files.**\n-\n-    - `unused_optional_binding` decides NO shipped prompt bullet. `builtin/validators/swift/rules/optionals.md` holds four bullets — force unwrap, `guard let` early exit, IUO, and no `Optional` where a default fits — and none of them is \"check for nil, do not bind an unused value\". The rule body puts `unused_optional_binding` in the \"no shipped prompt rule asks\" group. The gate therefore decides FIVE bullets across three prompt rules, not six.\n-    - `optionals.md`'s IUO bullet sanctions two exceptions, `@IBOutlet` and \"test fixtures set in `setUp()`\". `mode: all_except_iboutlets` expresses the first; measured, no swiftlint option expresses the second, and `swiftlint rules implicitly_unwrapped_optional` names only `severity` and `mode`. `error-handling.md`'s `try!` bullet has the same shape, and `swiftlint rules force_try` names only `severity`. Both gaps are documented in the rule body under \"Two prompt carve-outs no option expresses\", with the directive as the recourse, and card ^c8fj21y was created to decide whether `implicitly_unwrapped_optional` should join the test-target partition.\n-\n-    **`supersedes` is empty**, per the card. `builtin/validators/README.md` said only two shipped tool rules declare no `supersedes`; that sentence was already stale for `idioms-swift` and is now corrected to name all four, with the reason the two Swift rules leave it empty stated separately from the reason the other two do.\n-\n-    **A gap in the sibling card's work, closed here.** `CODE_HYGIENE_IDIOMS_TOOL_RULES` was added to `expected_rules` and to the length sum of `test_code_hygiene_loads_its_rule_roster_and_the_callers_probe`, and NOT to `expected_supersedes` — the chain that actually asserts `supersedes.names()`. So nothing held `idioms-swift` to superseding nothing. Both idioms and disallowed-constructs are now in that chain.\n-\n-    **Counts re-measured over `builtin/validators/*/rules/*.md`**, not derived by arithmetic: 27 rules carry a `run` script, 16 state `scope: files`, 11 state `scope: workspace`, 24 call `mktemp`. Of the 16 files-scope scripts, 4 write the zero-argument guard on the first line and 12 write it under `set -e` alone.\n-  timestamp: 2026-08-22T15:13:54.812168+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N0JK4HQM33E7W3VP7H04BT","timestamp":"2026-08-22T15:14:07.633890Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -54,6 +54,16 @@\n\n     **Counts re-measured over `builtin/validators/*/rules/*.md`**, not derived by arithmetic: 27 rules carry a `run` script, 16 state `scope: files`, 11 state `scope: workspace`, 24 call `mktemp`. Of the 16 files-scope scripts, 4 write the zero-argument guard on the first line and 12 write it under `set -e` alone.\n   timestamp: 2026-08-22T15:13:54.812168+00:00\n+- actor: claude-code\n+  id: 01m0n0jk4h26gtmd26qrnyw7n5\n+  text: |-\n+    ### implement — changed\n+    - evidence: 19 files. New: `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md`, `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.{fail,pass}.swift.tmpl`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs`. Registration: `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs`, `.../review/tool_rules/tests.rs`, `.../tests/shipped.rs`, `.../tests/shipped/{scope_roster,zero_argument,temp_directory,idioms_swift}.rs`. Docs: `builtin/validators/README.md`, `builtin/validators/code-hygiene/VALIDATOR.md`, `doc/src/concepts/validators.md`, and the \"three shipped swiftlint rules\" sentence in `dead-code-typescript.md`, `function-length-rust.md`, `function-length-swift.md`, `magic-numbers-swift.md`.\n+    - tests: `cargo nextest run -p swissarmyhammer-validators -p mirdan -p swissarmyhammer-doctor` — 1376 run, 1376 passed, 0 failed, 0 skipped. `cargo fmt --all -- --check` clean. `cargo clippy -p swissarmyhammer-validators -p mirdan --all-targets` — 0 warnings.\n+    - both card-critical acceptance tests pass: `..._reads_the_unchecked_sendable_directive` (the `// swiftlint:disable:next no_unchecked_sendable` escape hatch) and `..._keeps_the_force_rules_out_of_a_test_target` (a force unwrap in `Sources/` reports, the same bytes in `Tests/` do not).\n+    - new work: card ^c8fj21y — decide whether `implicitly_unwrapped_optional` joins the test-target carve-out, for the `setUp()` fixture exception `optionals.md` sanctions and no swiftlint option expresses.\n+    - next: ready for `/review`.\n+  timestamp: 2026-08-22T15:14:07.633535+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -54,16 +54,6 @@\n\n     **Counts re-measured over `builtin/validators/*/rules/*.md`**, not derived by arithmetic: 27 rules carry a `run` script, 16 state `scope: files`, 11 state `scope: workspace`, 24 call `mktemp`. Of the 16 files-scope scripts, 4 write the zero-argument guard on the first line and 12 write it under `set -e` alone.\n   timestamp: 2026-08-22T15:13:54.812168+00:00\n-- actor: claude-code\n-  id: 01m0n0jk4h26gtmd26qrnyw7n5\n-  text: |-\n-    ### implement — changed\n-    - evidence: 19 files. New: `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md`, `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.{fail,pass}.swift.tmpl`, `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs`. Registration: `crates/mirdan/src/builtin_validators.rs`, `crates/swissarmyhammer-validators/src/builtin/mod.rs`, `.../review/tool_rules/tests.rs`, `.../tests/shipped.rs`, `.../tests/shipped/{scope_roster,zero_argument,temp_directory,idioms_swift}.rs`. Docs: `builtin/validators/README.md`, `builtin/validators/code-hygiene/VALIDATOR.md`, `doc/src/concepts/validators.md`, and the \"three shipped swiftlint rules\" sentence in `dead-code-typescript.md`, `function-length-rust.md`, `function-length-swift.md`, `magic-numbers-swift.md`.\n-    - tests: `cargo nextest run -p swissarmyhammer-validators -p mirdan -p swissarmyhammer-doctor` — 1376 run, 1376 passed, 0 failed, 0 skipped. `cargo fmt --all -- --check` clean. `cargo clippy -p swissarmyhammer-validators -p mirdan --all-targets` — 0 warnings.\n-    - both card-critical acceptance tests pass: `..._reads_the_unchecked_sendable_directive` (the `// swiftlint:disable:next no_unchecked_sendable` escape hatch) and `..._keeps_the_force_rules_out_of_a_test_target` (a force unwrap in `Sources/` reports, the same bytes in `Tests/` do not).\n-    - new work: card ^c8fj21y — decide whether `implicitly_unwrapped_optional` joins the test-target carve-out, for the `setUp()` fixture exception `optionals.md` sanctions and no swiftlint option expresses.\n-    - next: ready for `/review`.\n-  timestamp: 2026-08-22T15:14:07.633535+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N0TCPSN2ZSRJ2JHA7X6HX2","timestamp":"2026-08-22T15:18:23.193736Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -64,6 +64,14 @@\n     - new work: card ^c8fj21y — decide whether `implicitly_unwrapped_optional` joins the test-target carve-out, for the `setUp()` fixture exception `optionals.md` sanctions and no swiftlint option expresses.\n     - next: ready for `/review`.\n   timestamp: 2026-08-22T15:14:07.633535+00:00\n+- actor: claude-code\n+  id: 01m0n0tcpsw0gn5zsc3ambx3aj\n+  text: |-\n+    ### test — green\n+    - evidence: `cargo nextest run --workspace` — 14176 passed, 0 failed, 0 skipped (29 slow, 2 leaky), 128.069s. `cargo fmt --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean, 0 warnings.\n+    - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n+    - next: ready for review.\n+  timestamp: 2026-08-22T15:18:23.193369+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -64,14 +64,6 @@\n     - new work: card ^c8fj21y — decide whether `implicitly_unwrapped_optional` joins the test-target carve-out, for the `setUp()` fixture exception `optionals.md` sanctions and no swiftlint option expresses.\n     - next: ready for `/review`.\n   timestamp: 2026-08-22T15:14:07.633535+00:00\n-- actor: claude-code\n-  id: 01m0n0tcpsw0gn5zsc3ambx3aj\n-  text: |-\n-    ### test — green\n-    - evidence: `cargo nextest run --workspace` — 14176 passed, 0 failed, 0 skipped (29 slow, 2 leaky), 128.069s. `cargo fmt --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean, 0 warnings.\n-    - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n-    - next: ready for review.\n-  timestamp: 2026-08-22T15:18:23.193369+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N0WV8AGT9A8T6Q0VF2W8SY","timestamp":"2026-08-22T15:19:43.626897Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -72,8 +72,8 @@\n     - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n     - next: ready for review.\n   timestamp: 2026-08-22T15:18:23.193369+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '80'\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -72,8 +72,8 @@\n     - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n     - next: ready for review.\n   timestamp: 2026-08-22T15:18:23.193369+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8280'\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","transaction_id":null}
{"id":"01M0N1F2JHFWBTJ7700KRT03T7","timestamp":"2026-08-22T15:29:40.945592Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -119,4 +119,18 @@\n - A shipped acceptance test driving REAL `swiftlint`. Model it on `missing_docs.rs:2049-2165` and `magic_numbers.rs:243-330`.\n - A test proving the annotation escape hatch works: a `@unchecked Sendable` with the disable comment produces NO finding; the same code without it produces one.\n - A test proving the test-target polarity split: a force unwrap in `Sources/` reports; the same in `Tests/` does not.\n-- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n\\ No newline at end of file\n+- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n+\n+## Review Findings (2026-08-22 10:19)\n+\n+> Scope: `review sha HEAD~1..HEAD` — reviewed the diffs only — lines this change added or modified. 9 file(s) reviewed, 10 not reviewed.\n+\n+> 8 file(s) not reviewed — excluded by an ignore rule:\n+> - `.kanban/ (from .reviewignore)` — 8 file(s)\n+\n+> 2 file(s) not reviewed — excluded from the review scope:\n+> - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.fail.swift.tmpl` — validator fixture\n+> - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.pass.swift.tmpl` — validator fixture\n+\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n","reverse_patch":"--- original\n+++ modified\n@@ -119,18 +119,4 @@\n - A shipped acceptance test driving REAL `swiftlint`. Model it on `missing_docs.rs:2049-2165` and `magic_numbers.rs:243-330`.\n - A test proving the annotation escape hatch works: a `@unchecked Sendable` with the disable comment produces NO finding; the same code without it produces one.\n - A test proving the test-target polarity split: a force unwrap in `Sources/` reports; the same in `Tests/` does not.\n-- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n-\n-## Review Findings (2026-08-22 10:19)\n-\n-> Scope: `review sha HEAD~1..HEAD` — reviewed the diffs only — lines this change added or modified. 9 file(s) reviewed, 10 not reviewed.\n-\n-> 8 file(s) not reviewed — excluded by an ignore rule:\n-> - `.kanban/ (from .reviewignore)` — 8 file(s)\n-\n-> 2 file(s) not reviewed — excluded from the review scope:\n-> - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.fail.swift.tmpl` — validator fixture\n-> - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.pass.swift.tmpl` — validator fixture\n-\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n+- Update the `mirdan` fixture roster at `crates/mirdan/src/builtin_validators.rs`. #tool-validators\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0N1F8Z0JT67NZ1HJV465EB7","timestamp":"2026-08-22T15:29:47.488452Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -72,6 +72,14 @@\n     - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n     - next: ready for review.\n   timestamp: 2026-08-22T15:18:23.193369+00:00\n+- actor: claude-code\n+  id: 01m0n1f8z0p35r2eewhqyw63bf\n+  text: |\n+    ### review — findings\n+    - evidence: 2 findings (2 confirmed, 0 refuted, 14 attempted) — crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432\n+    - scope: review sha HEAD~1..HEAD (commit 4aa9eec0b)\n+    - next: extend the acceptance tests per the two checklist items, then re-run review\n+  timestamp: 2026-08-22T15:29:47.488061+00:00\n position_column: review\n position_ordinal: '80'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -72,14 +72,6 @@\n     - baseline comparison: previous card baseline was 14169 passed. New count 14176 is higher (+7), consistent with the added `disallowed-constructs-swift` rule tests and roster/count-guard updates.\n     - next: ready for review.\n   timestamp: 2026-08-22T15:18:23.193369+00:00\n-- actor: claude-code\n-  id: 01m0n1f8z0p35r2eewhqyw63bf\n-  text: |\n-    ### review — findings\n-    - evidence: 2 findings (2 confirmed, 0 refuted, 14 attempted) — crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347, crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432\n-    - scope: review sha HEAD~1..HEAD (commit 4aa9eec0b)\n-    - next: extend the acceptance tests per the two checklist items, then re-run review\n-  timestamp: 2026-08-22T15:29:47.488061+00:00\n position_column: review\n position_ordinal: '80'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N1G3M2NENY0W4SM93APQHN","timestamp":"2026-08-22T15:30:14.786929Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -80,6 +80,19 @@\n     - scope: review sha HEAD~1..HEAD (commit 4aa9eec0b)\n     - next: extend the acceptance tests per the two checklist items, then re-run review\n   timestamp: 2026-08-22T15:29:47.488061+00:00\n+- actor: claude-code\n+  id: 01m0n1g3m210y67tc0afn0w1ce\n+  text: |-\n+    ### finish iteration 1 — findings\n+    - implement: changed — new `disallowed-constructs-swift` rule, 12 swiftlint rules (9 stock + 3 custom regex), fixtures, acceptance tests, roster/count guards, README note; also closed a `supersedes` assertion-chain hole left by ^3fx5bny\n+    - test: green — cargo nextest run --workspace, 14176 passed, 0 failed, 0 skipped (up 7 from the 14169 baseline, so nothing was dropped); fmt clean; clippy clean\n+    - commit: 4aa9eec0b\n+    - review: findings — disallowed_constructs_swift.rs:347, disallowed_constructs_swift.rs:432\n+\n+    Both findings are the same defect in two places: a comment states a property, and no test proves it. That is the declared-equals-proven rule. Neither is a defect in the rule itself.\n+\n+    Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n+  timestamp: 2026-08-22T15:30:14.786510+00:00\n position_column: review\n position_ordinal: '80'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -80,19 +80,6 @@\n     - scope: review sha HEAD~1..HEAD (commit 4aa9eec0b)\n     - next: extend the acceptance tests per the two checklist items, then re-run review\n   timestamp: 2026-08-22T15:29:47.488061+00:00\n-- actor: claude-code\n-  id: 01m0n1g3m210y67tc0afn0w1ce\n-  text: |-\n-    ### finish iteration 1 — findings\n-    - implement: changed — new `disallowed-constructs-swift` rule, 12 swiftlint rules (9 stock + 3 custom regex), fixtures, acceptance tests, roster/count guards, README note; also closed a `supersedes` assertion-chain hole left by ^3fx5bny\n-    - test: green — cargo nextest run --workspace, 14176 passed, 0 failed, 0 skipped (up 7 from the 14169 baseline, so nothing was dropped); fmt clean; clippy clean\n-    - commit: 4aa9eec0b\n-    - review: findings — disallowed_constructs_swift.rs:347, disallowed_constructs_swift.rs:432\n-\n-    Both findings are the same defect in two places: a comment states a property, and no test proves it. That is the declared-equals-proven rule. Neither is a defect in the rule itself.\n-\n-    Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n-  timestamp: 2026-08-22T15:30:14.786510+00:00\n position_column: review\n position_ordinal: '80'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N1GQPBPNX4T3XX22DF1Z6K","timestamp":"2026-08-22T15:30:35.339729Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -93,8 +93,8 @@\n\n     Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n   timestamp: 2026-08-22T15:30:14.786510+00:00\n-position_column: review\n-position_ordinal: '80'\n+position_column: doing\n+position_ordinal: '8280'\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -93,8 +93,8 @@\n\n     Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n   timestamp: 2026-08-22T15:30:14.786510+00:00\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: review\n+position_ordinal: '80'\n project: swift-validator\n title: 'swift: expand the swiftlint tool rules, and copy Airbnb''s three custom regex rules'\n ---\n","transaction_id":null}
{"id":"01M0N285F56Q9WZW250626QYF5","timestamp":"2026-08-22T15:43:23.109478Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -153,5 +153,5 @@\n > - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.fail.swift.tmpl` — validator fixture\n > - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.pass.swift.tmpl` — validator fixture\n\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n-- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n+- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n","reverse_patch":"--- original\n+++ modified\n@@ -153,5 +153,5 @@\n > - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.fail.swift.tmpl` — validator fixture\n > - `builtin/validators/code-hygiene/fixtures/disallowed-constructs-swift.pass.swift.tmpl` — validator fixture\n\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n-- [x] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:347` `completeness/invariant-propagation` — The module comment (lines 6-14) states that all three custom regex rules 'read an identifier alone, so a comment and a string literal holding the same words stay silent.' Only `no_direct_standard_out_logs` is tested for this identifier-only property (line 347). The other two custom rules (`no_file_literal` at line 170 and `no_unchecked_sendable` at line 171 in the fixture list) also appear in the fixture but lack tests verifying they do not match rule tokens in comments or string literals. Add test cases similar to the test at line 347 for `no_file_literal` and `no_unchecked_sendable` to verify they also read only identifiers and do not match tokens appearing in comments or string literals.\n+- [ ] `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs:432` `completeness/invariant-propagation` — Three force-related rules (force_unwrapping, force_try, force_cast) should all be excluded from test targets per the comment 'Three of the twelve stay off inside a test target', but only force_unwrapping is explicitly tested for this exclusion. If the script has per-rule handling or a misconfiguration affects one rule differently, it would not be detected. Extend the test at line 432-449 to verify that all three force rules (force_unwrapping, force_try, force_cast) are excluded from test targets. Add probe source code for force_try and force_cast (using `try!` and `as!` operators) and stage them alongside the force_unwrap code to ensure all three are properly excluded.\n","transaction_id":null}
{"id":"01M0N28XCN1DWKRQ1AY690G730","timestamp":"2026-08-22T15:43:47.605786Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -93,6 +93,48 @@\n\n     Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n   timestamp: 2026-08-22T15:30:14.786510+00:00\n+- actor: claude-code\n+  id: 01m0n28xcne6cn0jkyg81pdszd\n+  text: |-\n+    Both findings were the same defect: a comment declared a property of THREE rules and one test measured ONE of them. Neither claim was false — both were proved true against real swiftlint 0.65.0 — so nothing in the shipped rule had to change. The comments were not softened.\n+\n+    **Finding at `disallowed_constructs_swift.rs:347` — the three custom regex rules.**\n+\n+    Each of the two unprobed rules now carries a probe of its own, built the same way the standard-out probe is: ONE real construct beside three copies of the same words, in a string literal, in a line comment and in a doc comment.\n+\n+    - `no_file_literal` — `#file` as a default argument, plus `\"#file …\"`, `// #file …` and `/// #file …`.\n+    - `no_unchecked_sendable` — an `@unchecked Sendable` conformance, plus the same words in a string, a line comment and a doc comment.\n+\n+    Measured with the shipped configuration: each reports its rule ONCE. Measured with the same regexes and every `match_kinds` line stripped: each reports FOUR times. So the probes are discriminating rather than passing by luck, and `match_kinds` is what the test measures.\n+\n+    The three rules name three DIFFERENT kinds — `identifier`, `keyword`, and `attribute.builtin`+`typeidentifier` — which is why one rule's answer never stood for the other two. The module comment said \"read an identifier alone\", which is inaccurate for two of the three; it now says each matches a token of the KIND its `match_kinds` names. That corrects a wrong description; the load-bearing half (a comment and a string literal stay silent) is unchanged and now proved for all three.\n+\n+    **Finding at `disallowed_constructs_swift.rs:432` — the three force rules.**\n+\n+    The probe source now holds all three constructs in ONE file — `value!`, `try! make()` and `value as! Int` — staged at the product path and at the test path with identical bytes. Both the `Tests/` convention test and the manifest-named-target test run over it, so the carve-out is measured for every rule it names in both partitions.\n+\n+    Line numbers are no longer typed: `declaration_line` reads each construct's line off the source, so an edit to the probe cannot silently move the expectation.\n+\n+    The rows a probe reads now carry the rule name after `path:line`, through a new `finding_rule_rows` reader. A row naming only a path and a line could not tell a rule the partition dropped from a rule that never fired — which is exactly the hole the finding named.\n+\n+    Rows are sorted on both sides. swiftlint groups a file's findings by RULE, not by line: measured over the three-construct file, the report reads `force_cast`, `force_try`, `force_unwrapping` while the lines run 10, 6, 2. Asserting that order would be asserting swiftlint's grouping.\n+\n+    **The cause was removed rather than the two lines.** Both counts of \"three\" were themselves untested numbers, so both are now read off the shipped script instead of written in the test:\n+\n+    - `swift_custom_rule_names` reads the `custom_rules:` keys out of the script — the only two-space `printf` entry that ends on its own colon.\n+    - `swift_product_only_rules` reads the names the script writes to `$work/product-only`.\n+\n+    Each is asserted equal to the set of rules the test probes. A fourth rule added to either set now FAILS these tests until it carries a probe. Verified by renaming `no_file_literal` and by dropping `force_cast` from the product-only list: each guard failed and named the offending set.\n+\n+    One more declared-but-unproved property was found and closed in the same file: the doc of `SWIFT_UNCHECKED_SENDABLE_ANNOTATED` says the annotated and plain constants \"differ in that one line and in nothing else\". The directive test now asserts that of the constants before it drives either one.\n+\n+    **RED verified for every new assertion, not just GREEN.** Two mutations were applied to the shipped rule and reverted:\n+\n+    1. `match_kinds: [keyword]` deleted from `no_file_literal` — the custom-rule test failed, reporting `[\"no_file_literal\" x4]`.\n+    2. the `jq` filter narrowed from `IN($product_only[])` to `IN($product_only[0])`, so only the FIRST named rule keeps its carve-out — BOTH force tests failed, with `force_try` and `force_cast` reporting from `Tests/ProbeTests` and from `Custom/OddTests`. Under the old single-construct probe that mutation would have passed. That is the blind spot the finding named, now closed.\n+\n+    The rule file `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` is byte-identical to its committed state; `git status` shows only the test file changed.\n+  timestamp: 2026-08-22T15:43:47.605332+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -93,48 +93,6 @@\n\n     Three deliberate decisions were challenged and survived: the empty `supersedes`, the batched swiftlint invocation (which differs from `idioms-swift` on measured grounds), and the twelve verified rule names. The `@unchecked Sendable` escape-hatch test also survived — it proves both polarities as claimed.\n   timestamp: 2026-08-22T15:30:14.786510+00:00\n-- actor: claude-code\n-  id: 01m0n28xcne6cn0jkyg81pdszd\n-  text: |-\n-    Both findings were the same defect: a comment declared a property of THREE rules and one test measured ONE of them. Neither claim was false — both were proved true against real swiftlint 0.65.0 — so nothing in the shipped rule had to change. The comments were not softened.\n-\n-    **Finding at `disallowed_constructs_swift.rs:347` — the three custom regex rules.**\n-\n-    Each of the two unprobed rules now carries a probe of its own, built the same way the standard-out probe is: ONE real construct beside three copies of the same words, in a string literal, in a line comment and in a doc comment.\n-\n-    - `no_file_literal` — `#file` as a default argument, plus `\"#file …\"`, `// #file …` and `/// #file …`.\n-    - `no_unchecked_sendable` — an `@unchecked Sendable` conformance, plus the same words in a string, a line comment and a doc comment.\n-\n-    Measured with the shipped configuration: each reports its rule ONCE. Measured with the same regexes and every `match_kinds` line stripped: each reports FOUR times. So the probes are discriminating rather than passing by luck, and `match_kinds` is what the test measures.\n-\n-    The three rules name three DIFFERENT kinds — `identifier`, `keyword`, and `attribute.builtin`+`typeidentifier` — which is why one rule's answer never stood for the other two. The module comment said \"read an identifier alone\", which is inaccurate for two of the three; it now says each matches a token of the KIND its `match_kinds` names. That corrects a wrong description; the load-bearing half (a comment and a string literal stay silent) is unchanged and now proved for all three.\n-\n-    **Finding at `disallowed_constructs_swift.rs:432` — the three force rules.**\n-\n-    The probe source now holds all three constructs in ONE file — `value!`, `try! make()` and `value as! Int` — staged at the product path and at the test path with identical bytes. Both the `Tests/` convention test and the manifest-named-target test run over it, so the carve-out is measured for every rule it names in both partitions.\n-\n-    Line numbers are no longer typed: `declaration_line` reads each construct's line off the source, so an edit to the probe cannot silently move the expectation.\n-\n-    The rows a probe reads now carry the rule name after `path:line`, through a new `finding_rule_rows` reader. A row naming only a path and a line could not tell a rule the partition dropped from a rule that never fired — which is exactly the hole the finding named.\n-\n-    Rows are sorted on both sides. swiftlint groups a file's findings by RULE, not by line: measured over the three-construct file, the report reads `force_cast`, `force_try`, `force_unwrapping` while the lines run 10, 6, 2. Asserting that order would be asserting swiftlint's grouping.\n-\n-    **The cause was removed rather than the two lines.** Both counts of \"three\" were themselves untested numbers, so both are now read off the shipped script instead of written in the test:\n-\n-    - `swift_custom_rule_names` reads the `custom_rules:` keys out of the script — the only two-space `printf` entry that ends on its own colon.\n-    - `swift_product_only_rules` reads the names the script writes to `$work/product-only`.\n-\n-    Each is asserted equal to the set of rules the test probes. A fourth rule added to either set now FAILS these tests until it carries a probe. Verified by renaming `no_file_literal` and by dropping `force_cast` from the product-only list: each guard failed and named the offending set.\n-\n-    One more declared-but-unproved property was found and closed in the same file: the doc of `SWIFT_UNCHECKED_SENDABLE_ANNOTATED` says the annotated and plain constants \"differ in that one line and in nothing else\". The directive test now asserts that of the constants before it drives either one.\n-\n-    **RED verified for every new assertion, not just GREEN.** Two mutations were applied to the shipped rule and reverted:\n-\n-    1. `match_kinds: [keyword]` deleted from `no_file_literal` — the custom-rule test failed, reporting `[\"no_file_literal\" x4]`.\n-    2. the `jq` filter narrowed from `IN($product_only[])` to `IN($product_only[0])`, so only the FIRST named rule keeps its carve-out — BOTH force tests failed, with `force_try` and `force_cast` reporting from `Tests/ProbeTests` and from `Custom/OddTests`. Under the old single-construct probe that mutation would have passed. That is the blind spot the finding named, now closed.\n-\n-    The rule file `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` is byte-identical to its committed state; `git status` shows only the test file changed.\n-  timestamp: 2026-08-22T15:43:47.605332+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0N298VT36V8EC5WQ83APHA5","timestamp":"2026-08-22T15:43:59.354162Z","op":"update","item_id":"01M0MVM2VZ71SBQ95754S2N0RX","forward_patch":"--- original\n+++ modified\n@@ -135,6 +135,16 @@\n\n     The rule file `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` is byte-identical to its committed state; `git status` shows only the test file changed.\n   timestamp: 2026-08-22T15:43:47.605332+00:00\n+- actor: claude-code\n+  id: 01m0n298vsf0cs2pw27s5r4j1v\n+  text: |-\n+    ### implement — changed\n+    - evidence: 1 file — `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs`. Both review findings checked off. New probes `SWIFT_FILE_LITERAL_PROBE` and `SWIFT_UNCHECKED_SENDABLE_PROBE` beside the existing standard-out probe, walked by a `SWIFT_CUSTOM_RULE_PROBES` table; new `SWIFT_FORCE_CONSTRUCTS_SOURCE` holding `value!`, `try! make()` and `value as! Int` in one file, with `SWIFT_FORCE_CONSTRUCTS` naming the rule and construct of each; new readers `swift_custom_rule_names`, `swift_product_only_rules`, `finding_rule_rows`, `swift_force_rows_of`.\n+    - tests: `cargo nextest run --workspace` — 14176 run, 14176 passed, 0 failed, 0 skipped (29 slow), 126.9s. Same count as the prior baseline, as expected: existing tests were strengthened, none added or dropped. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean, 0 warnings.\n+    - RED proved for the new assertions by two mutations of the shipped rule, both reverted: stripping `match_kinds: [keyword]` fails the custom-rule test; narrowing the `jq` carve-out to the first named rule fails both force tests. The old single-construct probe would have passed that second mutation.\n+    - no change to `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` — both declared properties were TRUE, so the fix is the missing tests, not a weaker claim.\n+    - next: ready for `/review`.\n+  timestamp: 2026-08-22T15:43:59.353659+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -135,16 +135,6 @@\n\n     The rule file `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` is byte-identical to its committed state; `git status` shows only the test file changed.\n   timestamp: 2026-08-22T15:43:47.605332+00:00\n-- actor: claude-code\n-  id: 01m0n298vsf0cs2pw27s5r4j1v\n-  text: |-\n-    ### implement — changed\n-    - evidence: 1 file — `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/disallowed_constructs_swift.rs`. Both review findings checked off. New probes `SWIFT_FILE_LITERAL_PROBE` and `SWIFT_UNCHECKED_SENDABLE_PROBE` beside the existing standard-out probe, walked by a `SWIFT_CUSTOM_RULE_PROBES` table; new `SWIFT_FORCE_CONSTRUCTS_SOURCE` holding `value!`, `try! make()` and `value as! Int` in one file, with `SWIFT_FORCE_CONSTRUCTS` naming the rule and construct of each; new readers `swift_custom_rule_names`, `swift_product_only_rules`, `finding_rule_rows`, `swift_force_rows_of`.\n-    - tests: `cargo nextest run --workspace` — 14176 run, 14176 passed, 0 failed, 0 skipped (29 slow), 126.9s. Same count as the prior baseline, as expected: existing tests were strengthened, none added or dropped. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean, 0 warnings.\n-    - RED proved for the new assertions by two mutations of the shipped rule, both reverted: stripping `match_kinds: [keyword]` fails the custom-rule test; narrowing the `jq` carve-out to the first named rule fails both force tests. The old single-construct probe would have passed that second mutation.\n-    - no change to `builtin/validators/code-hygiene/rules/disallowed-constructs-swift.md` — both declared properties were TRUE, so the fix is the missing tests, not a weaker claim.\n-    - next: ready for `/review`.\n-  timestamp: 2026-08-22T15:43:59.353659+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
