name: CI

on:
  push:
    branches:
      - "*"
    tags-ignore:
      - "**[0-9]+.[0-9]+.[0-9]+*"
  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always
  # NOTE: the self-hosted runner is a Mac WITH a Metal GPU, so the Test job runs
  # on the GPU (the embedding stack defaults to offloading all layers). This is
  # required by the Metal-offload proof test and keeps the real-model tests fast
  # enough to stay well under their hang-guards. Do NOT set LLAMA_N_GPU_LAYERS=0
  # here — CPU-forced real-model inference is dramatically slower.

jobs:
  test:
    name: Test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
        with:
          # The `missing-docs-rust` and `function-length-rust` tool rules check
          # `which cargo-clippy jq`, and their tests state that tool as a
          # precondition rather than installing it. A job states the components
          # it needs, the same way the Clippy and Rustfmt jobs do.
          components: clippy
      - name: Install rust-analyzer
        run: rustup component add rust-analyzer
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "swissarmyhammer-ci"
      - uses: taiki-e/install-action@nextest
      - name: Ensure Chromium is available
        run: |
          if ! command -v chromium &>/dev/null && ! [ -d "/Applications/Chromium.app" ] && ! [ -d "/Applications/Google Chrome.app" ]; then
            brew install --cask chromium --no-quarantine
          fi
      - name: Ensure jq is available
        # The second half of the `which cargo-clippy jq` doctor check above.
        run: |
          if ! command -v jq &>/dev/null; then
            brew install jq
          fi
      - name: Install vulture
        # The tool `dead-code-python` requires, at the version that rule pins.
        # `uv` is not part of this runner's contract, so install it the way the
        # step above installs Chromium. `uv tool install` links executables into
        # a bin directory that is not on the runner PATH by default, so state it
        # for the steps that follow. Both commands are no-ops when the pinned
        # version is already there.
        run: |
          if ! command -v uv &>/dev/null; then
            brew install uv
          fi
          uv tool install vulture==2.14
          uv tool dir --bin >> "$GITHUB_PATH"
      - name: Install cargo-machete
        # The tool `unused-dependencies-rust` requires, installed by the command
        # that rule declares. A plain `cargo install` fails on this runner when
        # the binary is present but cargo's install metadata does not list it,
        # so skip the step when the pinned version is already on PATH and
        # overwrite the binary when it is not.
        run: |
          if [ "$(cargo-machete --version 2>/dev/null)" != "0.9.2" ]; then
            cargo install cargo-machete@0.9.2 --locked --force
          fi
      - name: Put the user bin directory on PATH
        # `uv tool install`, `pipx install` and the Go rules' `GOBIN` all land
        # their binaries here, and a doctor check finds a tool only on PATH.
        run: |
          mkdir -p "$HOME/.local/bin"
          echo "$HOME/.local/bin" >> "$GITHUB_PATH"
      - uses: actions/setup-node@v4
        # The TypeScript rules install eslint and ts-prune with
        # `npm install -g`, so the roster tests need npm on this job too.
        with:
          node-version: 22
      - name: Install the tools the TypeScript rules declare
        # The exact packages `function-length-typescript`, `magic-numbers-typescript`,
        # `missing-docs-typescript` and `dead-code-typescript` state under
        # `install.commands`. The step above gives this job its own global
        # package prefix, so a plugin an earlier run installed is not there.
        # Without the plugins the rules' eslint config cannot load, eslint
        # writes nothing, and a fail fixture that must report findings reports
        # none. Each version is pinned, so the command is a no-op the second
        # time.
        run: |
          npm install -g eslint@10.8.0 eslint-plugin-jsdoc@63.3.3 \
            eslint-plugin-sonarjs@4.2.0 typescript-eslint@8.66.0 \
            typescript@5.9.3 ts-prune@0.10.3
      - name: Ensure the language toolchains the roster tests need are available
        # `go` and `swift` are the toolchains the Go and Swift rules run, and
        # the roster tests require every row. Each command is a no-op when the
        # toolchain is already there.
        run: |
          if ! command -v go &>/dev/null; then
            brew install go
          fi
          swift --version
      - name: Ensure the tools that declare no install command are available
        # `swiftlint`, `periphery` and `dart` ship only through Homebrew, which
        # installs the current version and therefore cannot pin one, so their
        # rules state a `doctor.fix_hint` instead of an install command. A rule
        # with no install command cannot install itself, so this job does it.
        run: |
          if ! command -v swiftlint &>/dev/null; then
            brew install swiftlint
          fi
          if ! command -v swiftformat &>/dev/null; then
            brew install swiftformat
          fi
          # `periphery` comes from homebrew-core, not from the vendor tap. The
          # tap ships 2.21.2, whose scan output `dead-code-swift` cannot read,
          # so the rule reports nothing on a fixture that must report. Drop a
          # tap copy an earlier run left behind before installing the formula.
          brew uninstall --cask peripheryapp/periphery/periphery &>/dev/null || true
          if ! command -v periphery &>/dev/null; then
            brew install periphery
          fi
          # `magic-numbers-dart` builds a probe package that depends on
          # `solid_lints`, which depends on the Flutter SDK, so `dart` alone
          # cannot resolve it. The Flutter cask carries both, and it links its
          # own `dart`, so remove the formula of that name first and let the
          # `dart` check below find the one Flutter ships.
          if ! command -v flutter &>/dev/null; then
            brew uninstall --formula dart-sdk &>/dev/null || true
            brew install --cask flutter
          fi
          if ! command -v dart &>/dev/null; then
            brew install dart-sdk
          fi
      - name: Build the sah command line interface
        # A tool rule whose tool IS sah runs `"$SAH_BIN"`. Under `cargo nextest`
        # the running executable is a test binary rather than the command line
        # interface, so the resolver falls back to the bare name on PATH, which
        # this runner does not carry. Point every such script at the binary
        # built from this commit.
        run: |
          cargo build -p swissarmyhammer-cli
          target="$(cargo metadata --format-version 1 --no-deps | jq -r '.target_directory')"
          echo "SAH_BIN=$target/debug/sah" >> "$GITHUB_ENV"
      - name: Run tests
        run: cargo nextest run --no-fail-fast

  frontend:
    name: Frontend
    needs: test
    runs-on: self-hosted
    strategy:
      matrix:
        app: [apps/kanban-app, apps/mirdan-app]
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "swissarmyhammer-ci"
      - name: Build kanban CLI (needed by browser integration tests)
        run: cargo build -p kanban-cli
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install dependencies
        working-directory: ${{ matrix.app }}/ui
        # `npm ci` installs the exact committed lockfile — deterministic, immune
        # to a freshly-published transitive dep breaking a green build.
        run: |
          npm ci
          rm -rf node_modules/.vite
      - name: Install Playwright browsers
        working-directory: ${{ matrix.app }}/ui
        run: npx playwright install --with-deps chromium
      - name: Typecheck
        working-directory: ${{ matrix.app }}/ui
        run: npx tsc --noEmit
      - name: Test
        working-directory: ${{ matrix.app }}/ui
        run: npm test --if-present

  apps:
    name: Tauri Apps
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "swissarmyhammer-ci"
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Install Tauri CLI
        run: cargo install tauri-cli --version "^2" --locked || true
      - uses: taiki-e/install-action@just
      - name: Build Tauri apps
        # Runs the same just recipe developers use, so a broken app build
        # (e.g. mismatched tauri / @tauri-apps/api versions) fails CI instead
        # of slipping through to release. The justfile owns the app list --
        # adding an app there is enough, no CI edit needed.
        #
        # APPLE_SIGNING_IDENTITY="-" forces ad-hoc signing, which needs no
        # keychain identity. It overrides the real Developer ID in each app's
        # tauri.conf.json so this job verifies the build compiles and bundles
        # without depending on signing certs. Release signing is handled by
        # the separate Release App workflow.
        env:
          APPLE_SIGNING_IDENTITY: "-"
        run: just build

  fmt:
    name: Rustfmt
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - name: Enforce formatting
        run: cargo fmt --check

  clippy:
    name: Clippy
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "swissarmyhammer-ci"
      - name: Linting
        run: cargo clippy --all-targets --all-features -- -D warnings

  docs:
    name: Docs Guard
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Guard against removed prompts feature in docs
        run: ./scripts/docs_no_prompts_guard.sh

  examples:
    name: Run all examples
    needs: test
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          shared-key: "swissarmyhammer-ci"
      - name: Build examples
        run: |
          cargo metadata --format-version 1 \
            | jq -r '
                .workspace_members as $members
                | .packages[]
                | select(.id as $id | $members | index($id))
                | .name as $pkg
                | .targets[]
                | select(.kind[] == "example")
                | "\($pkg) \(.name)"
              ' \
            | while read -r pkg name; do
                echo "::group::Running example: $pkg/$name"
                cargo build --example "$name" -p "$pkg"
                echo "::endgroup::"
              done
