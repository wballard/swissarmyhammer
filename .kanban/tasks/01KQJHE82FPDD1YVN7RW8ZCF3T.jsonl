{"id":"01KQJHE82FVY5DWM4HAKKFV55C","timestamp":"2026-05-01T19:49:51.311862Z","op":"create","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,51 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: b280\n+project: spatial-nav\n+title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n+---\n+## What\n+\n+`<FocusDebugOverlay>` (`kanban-app/ui/src/components/focus-debug-overlay.tsx`) currently paints a colored dashed border AND an always-visible top-left label that reads `${kind}:${label} (x,y)` (lines 192–228). With overlays mounted on every Layer / Zone / Scope, the screen is wallpapered with overlapping label badges that obscure the actual UI being debugged.\n+\n+The dashed border is the part that's actually load-bearing — it shows where each primitive's rect lives. The text (kind, moniker, coordinates) is reference info you only need on demand. Move that text into a hover-revealed tooltip.\n+\n+### Approach\n+\n+Edit `kanban-app/ui/src/components/focus-debug-overlay.tsx` only.\n+\n+1. Replace the always-visible label `<span>` (lines 220–228) with a small hoverable handle: a 10–12px square dot/chip pinned to the top-left, color-matched to the kind via `KIND_CLASSES`. The handle is the only part of the overlay that takes pointer events (`pointer-events: auto`); everything else (border span and the wrapping span) stays `pointer-events: none` so click / right-click / hover routing on the host primitive is unchanged.\n+2. Wrap the handle in `<Tooltip>` / `<TooltipTrigger asChild>` / `<TooltipContent>` from `@/components/ui/tooltip`. Tooltip content text matches the current `labelText` exactly: `${kind}:${label}` for layers, `${kind}:${label} (x,y)` for zones / scopes when a rect is known.\n+3. Keep the `data-debug={kind}` attribute on the wrapping span so the existing test selectors continue to find the overlay. The label text the tests assert on must move with the tooltip — when the tooltip is closed, the label substring is in `TooltipContent`'s aria/`data-content` (Radix renders it into an offscreen portal even when closed for accessibility), or the test must hover the handle to open it. Either is acceptable; the chosen approach must be deterministic in jsdom **and** in browser-mode tests.\n+4. Preserve the layer-aware `zIndex: tier + OVERLAY_OFFSET_ABOVE_TIER` on the wrapper. The handle inherits via the wrapper's stacking context.\n+5. Preserve the rAF rect-tracking loop (lines 138–185) — coordinates are still computed; they just live in the tooltip now. The TODO referencing `01KQ9XBAG5P9W3JREQYNGAYM8Y` (rects-on-scroll subscription) stays unchanged; this task is independent.\n+\n+### Visual outcome\n+\n+  - Overlay-on: dashed colored border around each primitive (unchanged), plus a tiny color-coded dot in the top-left corner. Hover the dot → tooltip pops up with `kind:moniker (x,y)`.\n+  - Overlay-off: zero DOM cost (already true via `useFocusDebug()` short-circuit at the call sites in `focus-layer.tsx`, `focus-zone.tsx`, `focus-scope.tsx`).\n+\n+### Why a hover handle and not just `title=`\n+\n+Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n+\n+## Acceptance Criteria\n+- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n+- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n+- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n+- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n+\n+## Tests\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n+  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n+  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n+  - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n+  - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n+\n+## Workflow\n+- Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,51 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: b280\n-project: spatial-nav\n-title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n----\n-## What\n-\n-`<FocusDebugOverlay>` (`kanban-app/ui/src/components/focus-debug-overlay.tsx`) currently paints a colored dashed border AND an always-visible top-left label that reads `${kind}:${label} (x,y)` (lines 192–228). With overlays mounted on every Layer / Zone / Scope, the screen is wallpapered with overlapping label badges that obscure the actual UI being debugged.\n-\n-The dashed border is the part that's actually load-bearing — it shows where each primitive's rect lives. The text (kind, moniker, coordinates) is reference info you only need on demand. Move that text into a hover-revealed tooltip.\n-\n-### Approach\n-\n-Edit `kanban-app/ui/src/components/focus-debug-overlay.tsx` only.\n-\n-1. Replace the always-visible label `<span>` (lines 220–228) with a small hoverable handle: a 10–12px square dot/chip pinned to the top-left, color-matched to the kind via `KIND_CLASSES`. The handle is the only part of the overlay that takes pointer events (`pointer-events: auto`); everything else (border span and the wrapping span) stays `pointer-events: none` so click / right-click / hover routing on the host primitive is unchanged.\n-2. Wrap the handle in `<Tooltip>` / `<TooltipTrigger asChild>` / `<TooltipContent>` from `@/components/ui/tooltip`. Tooltip content text matches the current `labelText` exactly: `${kind}:${label}` for layers, `${kind}:${label} (x,y)` for zones / scopes when a rect is known.\n-3. Keep the `data-debug={kind}` attribute on the wrapping span so the existing test selectors continue to find the overlay. The label text the tests assert on must move with the tooltip — when the tooltip is closed, the label substring is in `TooltipContent`'s aria/`data-content` (Radix renders it into an offscreen portal even when closed for accessibility), or the test must hover the handle to open it. Either is acceptable; the chosen approach must be deterministic in jsdom **and** in browser-mode tests.\n-4. Preserve the layer-aware `zIndex: tier + OVERLAY_OFFSET_ABOVE_TIER` on the wrapper. The handle inherits via the wrapper's stacking context.\n-5. Preserve the rAF rect-tracking loop (lines 138–185) — coordinates are still computed; they just live in the tooltip now. The TODO referencing `01KQ9XBAG5P9W3JREQYNGAYM8Y` (rects-on-scroll subscription) stays unchanged; this task is independent.\n-\n-### Visual outcome\n-\n-  - Overlay-on: dashed colored border around each primitive (unchanged), plus a tiny color-coded dot in the top-left corner. Hover the dot → tooltip pops up with `kind:moniker (x,y)`.\n-  - Overlay-off: zero DOM cost (already true via `useFocusDebug()` short-circuit at the call sites in `focus-layer.tsx`, `focus-zone.tsx`, `focus-scope.tsx`).\n-\n-### Why a hover handle and not just `title=`\n-\n-Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n-\n-## Acceptance Criteria\n-- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n-- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n-- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n-- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n-\n-## Tests\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n-  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n-  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n-  - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n-  - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n-\n-## Workflow\n-- Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n","transaction_id":null}
{"id":"01KQJHE82G4KG0D06ZHX4MKQ82","timestamp":"2026-05-01T19:49:51.312310Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"create","changes":[["project",{"kind":"set","value":"spatial-nav"}],["position_ordinal",{"kind":"set","value":"b280"}],["position_column",{"kind":"set","value":"todo"}],["title",{"kind":"set","value":"Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle"}],["assignees",{"kind":"set","value":["claude-code"]}],["body",{"kind":"set","value":"## What\n\n`<FocusDebugOverlay>` (`kanban-app/ui/src/components/focus-debug-overlay.tsx`) currently paints a colored dashed border AND an always-visible top-left label that reads `${kind}:${label} (x,y)` (lines 192–228). With overlays mounted on every Layer / Zone / Scope, the screen is wallpapered with overlapping label badges that obscure the actual UI being debugged.\n\nThe dashed border is the part that's actually load-bearing — it shows where each primitive's rect lives. The text (kind, moniker, coordinates) is reference info you only need on demand. Move that text into a hover-revealed tooltip.\n\n### Approach\n\nEdit `kanban-app/ui/src/components/focus-debug-overlay.tsx` only.\n\n1. Replace the always-visible label `<span>` (lines 220–228) with a small hoverable handle: a 10–12px square dot/chip pinned to the top-left, color-matched to the kind via `KIND_CLASSES`. The handle is the only part of the overlay that takes pointer events (`pointer-events: auto`); everything else (border span and the wrapping span) stays `pointer-events: none` so click / right-click / hover routing on the host primitive is unchanged.\n2. Wrap the handle in `<Tooltip>` / `<TooltipTrigger asChild>` / `<TooltipContent>` from `@/components/ui/tooltip`. Tooltip content text matches the current `labelText` exactly: `${kind}:${label}` for layers, `${kind}:${label} (x,y)` for zones / scopes when a rect is known.\n3. Keep the `data-debug={kind}` attribute on the wrapping span so the existing test selectors continue to find the overlay. The label text the tests assert on must move with the tooltip — when the tooltip is closed, the label substring is in `TooltipContent`'s aria/`data-content` (Radix renders it into an offscreen portal even when closed for accessibility), or the test must hover the handle to open it. Either is acceptable; the chosen approach must be deterministic in jsdom **and** in browser-mode tests.\n4. Preserve the layer-aware `zIndex: tier + OVERLAY_OFFSET_ABOVE_TIER` on the wrapper. The handle inherits via the wrapper's stacking context.\n5. Preserve the rAF rect-tracking loop (lines 138–185) — coordinates are still computed; they just live in the tooltip now. The TODO referencing `01KQ9XBAG5P9W3JREQYNGAYM8Y` (rects-on-scroll subscription) stays unchanged; this task is independent.\n\n### Visual outcome\n\n  - Overlay-on: dashed colored border around each primitive (unchanged), plus a tiny color-coded dot in the top-left corner. Hover the dot → tooltip pops up with `kind:moniker (x,y)`.\n  - Overlay-off: zero DOM cost (already true via `useFocusDebug()` short-circuit at the call sites in `focus-layer.tsx`, `focus-zone.tsx`, `focus-scope.tsx`).\n\n### Why a hover handle and not just `title=`\n\nRadix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n\n## Acceptance Criteria\n- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n## Tests\n- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n  - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n  - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n## Workflow\n- Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n"}]]}
{"id":"01KQM4M9NJPTM39TGW4KEWEJ4W","timestamp":"2026-05-02T10:44:26.930083Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: b280\n+position_ordinal: '7e80'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -2,7 +2,7 @@\n assignees:\n - claude-code\n position_column: todo\n-position_ordinal: '7e80'\n+position_ordinal: b280\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","transaction_id":null}
{"id":"01KQM4M9NJT046PXAMXH13YZNY","timestamp":"2026-05-02T10:44:26.930784Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-b280\n\\ No newline at end of file\n+7e80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7e80\n\\ No newline at end of file\n+b280\n\\ No newline at end of file\n"}]]}
{"id":"01KQM5HZWTYNR72X41MMAWEJQZ","timestamp":"2026-05-02T11:00:39.962516Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '7e80'\n+position_column: doing\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8180'\n+position_column: todo\n+position_ordinal: '7e80'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","transaction_id":null}
{"id":"01KQM5HZWTT8D9KJP8WWRW5E07","timestamp":"2026-05-02T11:00:39.962966Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-7e80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+7e80\n\\ No newline at end of file\n"}]]}
{"id":"01KQM6MQ1ZHZ4CDS4HHFK25VX0","timestamp":"2026-05-02T11:19:37.791684Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -32,20 +32,27 @@\n Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n\n ## Acceptance Criteria\n-- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n-- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n-- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n-- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n+- [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n+- [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n+- [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n+- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n+- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n+- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n+- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n ## Workflow\n - Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n+\n+## Implementation Notes\n+\n+- **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n+- **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n+- **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n","reverse_patch":"--- original\n+++ modified\n@@ -32,27 +32,20 @@\n Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n\n ## Acceptance Criteria\n-- [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n-- [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n-- [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n-- [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n+- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n+- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n+- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n+- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n-- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n-- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n-- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n ## Workflow\n - Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n-\n-## Implementation Notes\n-\n-- **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n-- **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n-- **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n","transaction_id":null}
{"id":"01KQM6MQ202R9NAK0CMXCPXB8B","timestamp":"2026-05-02T11:19:37.792256Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -24,20 +24,27 @@\n Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n\n ## Acceptance Criteria\n-- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n-- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n-- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n-- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n+- [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n+- [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n+- [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n+- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n+- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n-- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n-- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n+- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n+- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n ## Workflow\n - Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n+\n+## Implementation Notes\n+\n+- **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n+- **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n+- **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n","reverse_patch":"--- original\n+++ modified\n@@ -24,27 +24,20 @@\n Radix Tooltip is the project's tooltip primitive (used in `nav-bar.tsx`, `entity-card.tsx`, etc.). HTML `title=` would render after a long delay, can't be styled, and is invisible to e2e tests. Use the existing Tooltip component for consistency.\n\n ## Acceptance Criteria\n-- [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n-- [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n-- [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n-- [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n+- [ ] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n+- [ ] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n+- [ ] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n+- [ ] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [ ] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n-- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n-- [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n-- [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n+- [ ] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n+- [ ] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n ## Workflow\n - Use `/tdd` — flip the existing browser test's \"label always visible\" expectation to \"label visible only after hovering the handle\"; watch it fail; replace the visible `<span>` with the Tooltip-wrapped handle; watch it pass. Click-passthrough assertion stays green throughout.\n-\n-## Implementation Notes\n-\n-- **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n-- **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n-- **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n"}]]}
{"id":"01KQM6NR66DGZK63KAWQZWE793","timestamp":"2026-05-02T11:20:11.718552Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n","transaction_id":null}
{"id":"01KQM6NR66RQ1625C8DFR5YJA6","timestamp":"2026-05-02T11:20:11.718927Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQM7JN7F2R0FQCWMDZJ99AA0","timestamp":"2026-05-02T11:35:58.959825Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '8180'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffec80\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffec80\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Focus debug overlay: hide id/coords behind a tooltip on a small hoverable handle'\n ---\n","transaction_id":null}
{"id":"01KQM7JN7GTC5FYB44950Q7TX2","timestamp":"2026-05-02T11:35:58.960539Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffec80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffec80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQZ9BVG4GSK6VPCGJJ3R5RK3","timestamp":"2026-05-06T18:38:51.908286Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n - **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n - **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n - **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -55,4 +55,4 @@\n - **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n - **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n - **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n\\ No newline at end of file\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n","transaction_id":null}
{"id":"01KQZ9BVG75A7VXZYWM709ZWTH","timestamp":"2026-05-06T18:38:51.911116Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n - **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n - **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n - **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n - **Self-contained TooltipProvider:** `<FocusDebugOverlay>` includes its own `<TooltipProvider delayDuration={0}>` wrapping the Tooltip. Production mounts a `<TooltipProvider>` at `<WindowContainer>` for chrome tooltips, but the *window-layer* `<FocusLayer>` in `App.tsx` sits *outside* `<WindowContainer>` — its layer-kind overlay would have no provider in scope. A local provider keeps the overlay self-contained; nested Radix providers are explicitly supported.\n - **Inline width/height on the handle:** the handle uses `style={{ width: 12, height: 12, position: \"absolute\" }}` in addition to Tailwind classes (`h-3 w-3`) so it has a deterministic 12×12 hit area in the vitest browser project, which mounts components without the `tailwindcss()` plugin.\n - **Tests assert via `aria-label`:** the handle's `aria-label` mirrors the tooltip content verbatim, so most label-text assertions read `getAttribute(\"aria-label\")` on the handle (deterministic, no hover round-trip needed). Two new tests (`tooltip_opens_on_handle_hover`, `tooltip_for_layer_kind_shows_kind_and_label`) use `userEvent.hover()` from `vitest/browser` to exercise the real hover→portal path end-to-end.\n-- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n\\ No newline at end of file\n+- **`readOpenTooltipText` strips the `[role=\"tooltip\"]` clone:** Radix renders both the visible `Slottable` and an offscreen `<VisuallyHidden role=\"tooltip\">` clone of the same children; the helper subtracts the latter so `textContent` matches what the user sees, not the doubled accessibility shadow.\n"}]]}
{"id":"01KRE1S55A4BZWPRSCM2BP3516","timestamp":"2026-05-12T12:14:55.658759Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -41,7 +41,7 @@\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n-  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n+  - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n","reverse_patch":"--- original\n+++ modified\n@@ -41,7 +41,7 @@\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n-  - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n+  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n","transaction_id":null}
{"id":"01KRE1S55BRWG1KM5EC1ZSMEV3","timestamp":"2026-05-12T12:14:55.659591Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -33,7 +33,7 @@\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n-  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n+  - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n","reverse_patch":"--- original\n+++ modified\n@@ -33,7 +33,7 @@\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n-  - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n+  - Existing \"(x,y) coordinates\" assertion (#3): hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n"}]]}
{"id":"01KRE1WFYK1RZNYBMBJJKAGHV6","timestamp":"2026-05-12T12:16:45.011326Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -40,7 +40,7 @@\n\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n-  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n+  - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n","reverse_patch":"--- original\n+++ modified\n@@ -40,7 +40,7 @@\n\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n-  - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n+  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n","transaction_id":null}
{"id":"01KRE1WFYMJM218F2W16X6XN1G","timestamp":"2026-05-12T12:16:45.012973Z","entity_type":"task","entity_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -32,7 +32,7 @@\n\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n-  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n+  - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n","reverse_patch":"--- original\n+++ modified\n@@ -32,7 +32,7 @@\n\n ## Tests\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n-  - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n+  - Existing \"label mentions primitive's name / moniker\" assertion (#1): change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n   - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n   - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n"}]]}
{"id":"01M129V1KC9656Y39E82GAJX0Q","timestamp":"2026-08-27T19:06:09.388585Z","op":"update","item_id":"01KQJHE82FPDD1YVN7RW8ZCF3T","forward_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n - [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n - [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n - [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n+- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion `#4`.)\n - [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n@@ -42,8 +42,8 @@\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n-  - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n-  - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n+  - Existing \"no overlay when provider disabled\" assertion (`#2`): unchanged — still verifies no `[data-debug=…]` mounts.\n+  - Existing \"click passthrough\" assertion (`#4`): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n - [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -35,7 +35,7 @@\n - [x] `<FocusDebugOverlay>` renders a small (~10–12px) color-matched handle pinned to the top-left of its host's content box. The handle carries `pointer-events: auto`; the surrounding wrapper and dashed border keep `pointer-events: none`.\n - [x] The handle is wrapped in a Radix Tooltip. Hovering it opens a `TooltipContent` whose text is exactly the current `labelText` value (`${kind}:${label}` for layers, `${kind}:${label} (x,y)` otherwise).\n - [x] The dashed border, color coding, layer-aware z-index, and `data-debug={kind}` attribute are unchanged.\n-- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion `#4`.)\n+- [x] Click routing on the host element is unchanged: clicking on the host's content (NOT on the handle) still reaches the host's click handler, not the overlay. (Existing click-passthrough invariant in `focus-debug-overlay.browser.test.tsx` assertion #4.)\n - [x] When `useFocusDebug()` returns `false`, no overlay or handle DOM is mounted (already enforced upstream — verify nothing in this change makes it conditional on the new tooltip subtree).\n\n ## Tests\n@@ -42,8 +42,8 @@\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.browser.test.tsx`:\n   - Existing \"label mentions primitive's name / moniker\" assertion (change to fire a hover on the handle and assert the tooltip content text matches `kind:moniker`. Use `@testing-library/user-event`'s `hover()` against `[data-debug=…] [data-tooltip-trigger]` (or whatever stable selector the handle exposes).\n   - Existing \"(x,y) coordinates\" assertion (hover the handle and assert tooltip text contains the `\"x,y\"` substring.\n-  - Existing \"no overlay when provider disabled\" assertion (`#2`): unchanged — still verifies no `[data-debug=…]` mounts.\n-  - Existing \"click passthrough\" assertion (`#4`): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n+  - Existing \"no overlay when provider disabled\" assertion (#2): unchanged — still verifies no `[data-debug=…]` mounts.\n+  - Existing \"click passthrough\" assertion (#4): unchanged — clicks on host content still reach the host. Add a sub-assertion: clicking on the *handle itself* must NOT reach the host (the handle is the only `pointer-events: auto` region; this is the explicit affordance for hover).\n - [x] Update `kanban-app/ui/src/components/focus-debug-overlay.layer-z.browser.test.tsx` only if the z-tier read needs to move — preserve the existing layer-z assertions.\n - [x] Run `cd kanban-app/ui && pnpm vitest run src/components/focus-debug-overlay` and confirm green.\n\n","transaction_id":null}
