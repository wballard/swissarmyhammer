{"id":"01KSJ48QSEM91F7BHH6NYR6SWS","timestamp":"2026-05-26T12:31:02.958399Z","op":"create","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,42 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: '8480'\n+title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n+---\n+## What\n+\n+`apps/swissarmyhammer-cli/src/commands/install/components/mod.rs` has store-management helpers that duplicate or belong next to existing mirdan store code:\n+\n+- `is_safe_name(name: &str) -> bool` — single-segment path-traversal sanitizer.\n+- `is_safe_relative_path(path: &str) -> bool` — multi-segment sibling.\n+- `remove_store_entries(...)` — removes named entries from a store dir and their symlinks across agent link dirs.\n+- `remove_single_store_entry(...)` — per-entry helper.\n+- `remove_if_symlink(path)` — safety-critical: only removes a path if it is a symlink.\n+\n+These have no CLI-specific concerns; they are mirdan store mechanics. `mirdan::store` already owns `sanitize_dir_name`, `symlink_name`, `create_skill_link`, `remove_if_exists`, `store_entry_still_referenced`.\n+\n+Also: `McpRegistration` (same file) still carries a legacy fallback path:\n+- `install_project_legacy(reporter)` and `uninstall_project_legacy(reporter)` — only triggered when **no** detected agent has an `mcp_config` populated. With the current `agents_default.yaml` that is unreachable for any real install: the fallback only fires when `installed_count == 0`, which means zero detected agents have an MCP config, in which case writing `.mcp.json` doesn't serve any agent anyway. The cleanup helper `cleanup_empty_mcp_servers` only exists for this path.\n+\n+Move + delete:\n+\n+- Move `is_safe_name`, `is_safe_relative_path` into `crates/mirdan/src/store.rs` (or a `mirdan::names` module) and re-export from `mirdan::store`. Update all call sites in `apps/swissarmyhammer-cli/src/commands/{install/components/mod.rs,skill.rs}`.\n+- Move `remove_store_entries`, `remove_single_store_entry`, `remove_if_symlink` into `mirdan::store`. Update call sites in `SkillDeployment::deinit`, `AgentDeployment::deinit`, and the components helper.\n+- Delete `install_project_legacy`, `uninstall_project_legacy`, `cleanup_empty_mcp_servers` and the `installed_count == 0` fallback branches in `McpRegistration::init`/`deinit`. The component's success path already iterates detected agents via mirdan; no fallback is needed once mirdan handles every supported agent.\n+- Keep all existing tests; relocate the helper unit tests to mirdan's test module.\n+\n+## Acceptance Criteria\n+- [ ] No `is_safe_*` / `remove_store_*` / `remove_if_symlink` definitions remain in `apps/swissarmyhammer-cli/`; the CLI consumes them via `mirdan::store::…`.\n+- [ ] `install_project_legacy` / `uninstall_project_legacy` / `cleanup_empty_mcp_servers` are gone; `McpRegistration` reports zero-installed agents as a Warning (with a fix hint) instead of writing a non-agent-bound `.mcp.json`.\n+- [ ] `cargo build -p mirdan -p swissarmyhammer-cli` green; clippy clean with `-D warnings`.\n+- [ ] All previously-passing install/skill/agent tests still pass.\n+\n+## Tests\n+- [ ] Move the existing `test_is_safe_name` / `test_is_safe_relative_path` unit tests to `mirdan::store`'s test module.\n+- [ ] Add a `McpRegistration::init` test asserting the no-detected-agents path emits a Warning result rather than touching `.mcp.json` (replacing the deleted legacy behavior).\n+- [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n+\n+## Workflow\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,42 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: '8480'\n-title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n----\n-## What\n-\n-`apps/swissarmyhammer-cli/src/commands/install/components/mod.rs` has store-management helpers that duplicate or belong next to existing mirdan store code:\n-\n-- `is_safe_name(name: &str) -> bool` — single-segment path-traversal sanitizer.\n-- `is_safe_relative_path(path: &str) -> bool` — multi-segment sibling.\n-- `remove_store_entries(...)` — removes named entries from a store dir and their symlinks across agent link dirs.\n-- `remove_single_store_entry(...)` — per-entry helper.\n-- `remove_if_symlink(path)` — safety-critical: only removes a path if it is a symlink.\n-\n-These have no CLI-specific concerns; they are mirdan store mechanics. `mirdan::store` already owns `sanitize_dir_name`, `symlink_name`, `create_skill_link`, `remove_if_exists`, `store_entry_still_referenced`.\n-\n-Also: `McpRegistration` (same file) still carries a legacy fallback path:\n-- `install_project_legacy(reporter)` and `uninstall_project_legacy(reporter)` — only triggered when **no** detected agent has an `mcp_config` populated. With the current `agents_default.yaml` that is unreachable for any real install: the fallback only fires when `installed_count == 0`, which means zero detected agents have an MCP config, in which case writing `.mcp.json` doesn't serve any agent anyway. The cleanup helper `cleanup_empty_mcp_servers` only exists for this path.\n-\n-Move + delete:\n-\n-- Move `is_safe_name`, `is_safe_relative_path` into `crates/mirdan/src/store.rs` (or a `mirdan::names` module) and re-export from `mirdan::store`. Update all call sites in `apps/swissarmyhammer-cli/src/commands/{install/components/mod.rs,skill.rs}`.\n-- Move `remove_store_entries`, `remove_single_store_entry`, `remove_if_symlink` into `mirdan::store`. Update call sites in `SkillDeployment::deinit`, `AgentDeployment::deinit`, and the components helper.\n-- Delete `install_project_legacy`, `uninstall_project_legacy`, `cleanup_empty_mcp_servers` and the `installed_count == 0` fallback branches in `McpRegistration::init`/`deinit`. The component's success path already iterates detected agents via mirdan; no fallback is needed once mirdan handles every supported agent.\n-- Keep all existing tests; relocate the helper unit tests to mirdan's test module.\n-\n-## Acceptance Criteria\n-- [ ] No `is_safe_*` / `remove_store_*` / `remove_if_symlink` definitions remain in `apps/swissarmyhammer-cli/`; the CLI consumes them via `mirdan::store::…`.\n-- [ ] `install_project_legacy` / `uninstall_project_legacy` / `cleanup_empty_mcp_servers` are gone; `McpRegistration` reports zero-installed agents as a Warning (with a fix hint) instead of writing a non-agent-bound `.mcp.json`.\n-- [ ] `cargo build -p mirdan -p swissarmyhammer-cli` green; clippy clean with `-D warnings`.\n-- [ ] All previously-passing install/skill/agent tests still pass.\n-\n-## Tests\n-- [ ] Move the existing `test_is_safe_name` / `test_is_safe_relative_path` unit tests to `mirdan::store`'s test module.\n-- [ ] Add a `McpRegistration::init` test asserting the no-detected-agents path emits a Warning result rather than touching `.mcp.json` (replacing the deleted legacy behavior).\n-- [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n-\n-## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJ6VQCQZN53TJ2YXWM06NQW","timestamp":"2026-05-26T13:16:22.295984Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: '8480'\n+position_column: doing\n+position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: '8480'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n ## What\n","transaction_id":null}
{"id":"01KSJ7TMZ31EBG1K0W0JCE4J7P","timestamp":"2026-05-26T13:33:15.619044Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","transaction_id":null}
{"id":"01KSJ83CSXACZ526PB8ZQ09RFT","timestamp":"2026-05-26T13:38:02.173924Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -39,4 +39,9 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\\ No newline at end of file\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n+\n+## Review Findings (2026-05-26 13:50)\n+\n+### Nits\n+- [ ] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -39,9 +39,4 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n-\n-## Review Findings (2026-05-26 13:50)\n-\n-### Nits\n-- [ ] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n\\ No newline at end of file\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJ8GJCY59B1S90QXQSM8C4F","timestamp":"2026-05-26T13:45:13.886493Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","transaction_id":null}
{"id":"01KSJ8MN6N8SSXJ3V4898XB20Q","timestamp":"2026-05-26T13:47:27.829091Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -44,4 +44,7 @@\n ## Review Findings (2026-05-26 13:50)\n\n ### Nits\n-- [ ] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n\\ No newline at end of file\n+- [x] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n+\n+### Resolution (2026-05-26)\n+Added a local `EnvGuard(&'static str)` RAII struct in the `tests` module of `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs`. `EnvGuard::set` calls `std::env::set_var` on construction; `Drop` calls `std::env::remove_var`. The `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` test now binds the guard with `let _env_guard = EnvGuard::set(\"MIRDAN_AGENTS_CONFIG\", &custom_config);` so the env var is cleared even if an assertion panics. `cargo test -p swissarmyhammer-cli --lib commands::install::components` — 20/20 pass; `cargo clippy -p swissarmyhammer-cli --tests -- -D warnings` clean.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -44,7 +44,4 @@\n ## Review Findings (2026-05-26 13:50)\n\n ### Nits\n-- [x] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n-\n-### Resolution (2026-05-26)\n-Added a local `EnvGuard(&'static str)` RAII struct in the `tests` module of `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs`. `EnvGuard::set` calls `std::env::set_var` on construction; `Drop` calls `std::env::remove_var`. The `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` test now binds the guard with `let _env_guard = EnvGuard::set(\"MIRDAN_AGENTS_CONFIG\", &custom_config);` so the env var is cleared even if an assertion panics. `cargo test -p swissarmyhammer-cli --lib commands::install::components` — 20/20 pass; `cargo clippy -p swissarmyhammer-cli --tests -- -D warnings` clean.\n\\ No newline at end of file\n+- [ ] `apps/swissarmyhammer-cli/src/commands/install/components/mod.rs:1949-1957` — `test_mcp_registration_init_warns_when_no_agent_has_mcp_config` sets `MIRDAN_AGENTS_CONFIG` with `std::env::set_var` and only clears it after the assertion block. If any assert above `std::env::remove_var` panics, the env var leaks to subsequent in-process tests that read `MIRDAN_AGENTS_CONFIG`. `#[serial_test::serial(home_env)]` serializes against home-env tests but doesn't cover this variable. Consider a small RAII guard (e.g. set in a `struct EnvGuard(&'static str)` whose `Drop` calls `remove_var`) so cleanup happens on panic too. Same pattern already exists for `CurrentDirGuard` / `IsolatedTestEnvironment` elsewhere.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KSJ8MVJBRSGB69G2WDGXSBQ4","timestamp":"2026-05-26T13:47:34.347028Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n","transaction_id":null}
{"id":"01KSJ8PFWV6HP3SJ5W9YWBJARG","timestamp":"2026-05-26T13:48:27.931909Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffff8a80\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n ## What\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffff8a80\n+position_column: review\n+position_ordinal: '80'\n title: Move path-safety + store-cleanup helpers to mirdan::store; delete dead MCP legacy fallback\n ---\n ## What\n","transaction_id":null}
{"id":"01M12AQDZG99DG1WWMXY8WZWZJ","timestamp":"2026-08-27T19:21:39.568014Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -39,7 +39,7 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor-sweep-tmp\n\n ## Review Findings (2026-05-26 13:50)\n\n","reverse_patch":"--- original\n+++ modified\n@@ -39,7 +39,7 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor-sweep-tmp\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\n ## Review Findings (2026-05-26 13:50)\n\n","transaction_id":null}
{"id":"01M12AR3Q7ZBPDJ3PNDNYEDY01","timestamp":"2026-08-27T19:22:01.831564Z","op":"update","item_id":"01KSJ48QSEWK7N68TM4Z3QYH9G","forward_patch":"--- original\n+++ modified\n@@ -39,7 +39,7 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor-sweep-tmp\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n\n ## Review Findings (2026-05-26 13:50)\n\n","reverse_patch":"--- original\n+++ modified\n@@ -39,7 +39,7 @@\n - [ ] `cargo test -p mirdan -p swissarmyhammer-cli` green.\n\n ## Workflow\n-- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor\n+- Use `/tdd` — write the moved-helpers tests in mirdan first, then move and delete. #init-doctor-sweep-tmp\n\n ## Review Findings (2026-05-26 13:50)\n\n","transaction_id":null}
