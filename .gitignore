# Rust
target
**/*.rs.bk
*.rcgu.o
rust_out

# Editor
*.swp
*.swo
*~
*.bak
*.backup
*.new
*.orig

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.iml

# Debug
*.pdb
semantic.db
semantic.db.wal
semantic.db-wal
search.db

# Logs
*.log
coverage_output.log
test_output.log
test_output.txt
mcp.log

# Coverage
lcov.info

# Scratch files
CODE_REVIEW.md
TEST_FAILURES.md
TEST_RESULTS.md
TEST_RESULTS_SUMMARY.md
DOCUMENTATION_REVIEW.md
scratch/


# Test issue files
swissarmyhammer/issues/
tests/test-home

# Test files with special characters created by file tools tests
swissarmyhammer-tools/path*
swissarmyhammer-tools/relative/
swissarmyhammer-tools/*path*
swissarmyhammer-tools/*.test


# Per-subcrate tool dirs (tools self-manage their own .gitignore inside)
swissarmyhammer/.sah
swissarmyhammer-agent-executor/.sah
swissarmyhammer-cli/.sah
swissarmyhammer-workflow/.sah
swissarmyhammer-tools/.sah
kanban-app/.sah
claude-agent/.sah
acp-conformance/.sah

.work


doc/book
**/doc/book


# ACP transcript recordings
.acp/
.hence/

# AVP runtime state (self-manages its own .gitignore)
.avp/

# Validators runtime store (self-manages its own .gitignore)
.validators/

# Kanban perspective trash (local cleanup, not source)
.kanban/perspectives/.trash/

# Stray kanban boards under crates and apps. The only board this repository
# tracks is the one at the root; a board anywhere else is state a tool wrote
# into its working directory, and committing it is what happened to
# crates/swissarmyhammer-mcp-proxy. These patterns are anchored to the crate
# and app directories so the tracked root board stays tracked.
/crates/*/.kanban/
/apps/*/.kanban/

# Stray fixture output at workspace root.
# `agent-client-protocol-extras::get_fixture_path_for()` resolves to the
# per-crate `<crate>/.fixtures/` layout (acp-conformance,
# agent-client-protocol-extras, avp-common — all intentionally tracked). The
# workspace-root `.fixtures/` directory should never appear in a clean tree;
# this ignore line keeps any historical/accidental drop quiet.
/.fixtures/

# Test workflow files (not part of the project source)
builtin/workflows/*-test.md

# Python bytecode caches (helper scripts under scripts/)
__pycache__/

# Node.js — ignore lockfiles by default, but COMMIT the app UI lockfiles so CI
# (`npm ci`) installs a deterministic dep set. Unpinned `npm install` lets a
# freshly-published transitive dep silently break a green build with no code
# change (see the radix dismissable-layer regression, 2026-06).
package-lock.json
!apps/kanban-app/ui/package-lock.json
!apps/mirdan-app/ui/package-lock.json
node_modules/
# Reject pnpm lockfiles entirely — Tauri apps enforce npm via `only-allow npm`.
pnpm-lock.yaml

# Output scratch files
output_*.txt

test_validation

# Claude Code usage tracking files
cc.json
.llama-sessions

.acp


# Generated man pages (built at compile time, included in release archives)
docs/*.1

# AVP and shell tool dirs self-manage their own .gitignore

# Lockfiles (generated during install, project-specific)
avp-lock.json
mirdan-lock.json



# Frontend build artifacts
**/ui/dist/
**/.vite/

# Vitest browser-mode artifacts (failure screenshots, attachments)
**/.vitest-attachments/
**/__screenshots__/


.github/copilot/

# Model conversion output (large .mlpackage files, tokenizers, weights)
var/data/models/
ane-embedding/convert/output/

# Claude Code local settings
.claude/settings.local.json
**/.claude/settings.local.json
.claude/plans/
.claude/scheduled_tasks.lock

# Ralph runtime state
.ralph/
**/.ralph/

# SAH runtime state
**/.sah/

# Validators runtime store (definitions live in builtin/validators/)
/.validators/
**/.validators/


# Shell tool runtime state
**/.shell/
.claude/worktrees
.swissarmyhammer
.code-context/

# Isolated CARGO_TARGET_DIR scratch dirs used by implement/verify runs
target-implement-verify/

# Generated skill/agent deploy targets (source of truth lives in builtin/).
# Unanchored (no leading `/`, `**/` on the multi-segment ones) so a deploy run
# from inside any `apps/*-cli/` directory is also ignored, not just one at the
# repository root -- see ^d525k4k.
.skills/
.agents/
**/.claude/skills/
**/.claude/agents/
**/.zed/skills/
