{"id":"01KV2YD5HDKJCN23N80G755QM7","timestamp":"2026-06-14T11:31:23.821166Z","op":"create","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,28 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: a180\n+project: claude-model-select\n+title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n+---\n+## What\n+The review tool's `backend: local` path bypasses the `review.model → claude-code-haiku` mapping: a review run invoked with `{\"backend\":\"local\"}` does not use the resolved review `ModelConfig`, so the configured/default review model (e.g. claude-code-haiku) is NOT actually used. This defeats the whole review-scope-default feature for the local backend.\n+\n+Investigate and fix:\n+- Review tool / orchestrator in `swissarmyhammer-tools/src/mcp/tools/review/` (`review_op.rs` defines `AgentFactory`/`AgentHandle`; backend is selected via the `backend` arg, `session` | `local`).\n+- The review factories are wired in `apps/swissarmyhammer-cli/src/commands/serve/mod.rs::wire_review_factories` / `set_review_factories` from the resolved review `ModelConfig` (via `review_agent_factory`). Determine why the `local` backend does not consume that factory/config and route the resolved review `ModelConfig` (which for unset config + no overall model is `claude-code-haiku`) into the local backend's agent creation.\n+- The resolved name precedence is already centralized in `ModelManager::review_agent_name_from` / `resolve_review_agent_config` — reuse it; do not re-implement the fallback.\n+\n+## Acceptance Criteria\n+- [ ] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n+- [ ] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n+- [ ] `backend: session` behavior unchanged.\n+\n+## Tests\n+- [ ] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n+- [ ] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n+- [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n+\n+## Workflow\n+- Use `/tdd` — write the failing parity/real-path test first, then implement.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,28 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: a180\n-project: claude-model-select\n-title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n----\n-## What\n-The review tool's `backend: local` path bypasses the `review.model → claude-code-haiku` mapping: a review run invoked with `{\"backend\":\"local\"}` does not use the resolved review `ModelConfig`, so the configured/default review model (e.g. claude-code-haiku) is NOT actually used. This defeats the whole review-scope-default feature for the local backend.\n-\n-Investigate and fix:\n-- Review tool / orchestrator in `swissarmyhammer-tools/src/mcp/tools/review/` (`review_op.rs` defines `AgentFactory`/`AgentHandle`; backend is selected via the `backend` arg, `session` | `local`).\n-- The review factories are wired in `apps/swissarmyhammer-cli/src/commands/serve/mod.rs::wire_review_factories` / `set_review_factories` from the resolved review `ModelConfig` (via `review_agent_factory`). Determine why the `local` backend does not consume that factory/config and route the resolved review `ModelConfig` (which for unset config + no overall model is `claude-code-haiku`) into the local backend's agent creation.\n-- The resolved name precedence is already centralized in `ModelManager::review_agent_name_from` / `resolve_review_agent_config` — reuse it; do not re-implement the fallback.\n-\n-## Acceptance Criteria\n-- [ ] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n-- [ ] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n-- [ ] `backend: session` behavior unchanged.\n-\n-## Tests\n-- [ ] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n-- [ ] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n-- [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n-\n-## Workflow\n-- Use `/tdd` — write the failing parity/real-path test first, then implement.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV2YDB5WMCCCF1XD2W1E3833","timestamp":"2026-06-14T11:31:29.596991Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -25,4 +25,4 @@\n - [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n-- Use `/tdd` — write the failing parity/real-path test first, then implement.\n\\ No newline at end of file\n+- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -25,4 +25,4 @@\n - [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n-- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n\\ No newline at end of file\n+- Use `/tdd` — write the failing parity/real-path test first, then implement.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV2YFW85BDACXFBT5ER02G6G","timestamp":"2026-06-14T11:32:52.613477Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: a180\n+position_column: doing\n+position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: a180\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n ---\n","transaction_id":null}
{"id":"01KV30E862VN2GP5RSMB1N4YNV","timestamp":"2026-06-14T12:06:56.450363Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","transaction_id":null}
{"id":"01KV30WMNA3YJDSJ42WN3BJWTN","timestamp":"2026-06-14T12:14:47.978712Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -25,4 +25,23 @@\n - [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n-- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n\\ No newline at end of file\n+- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n+\n+## Review Findings (2026-06-14 13:20)\n+\n+Reconciliation review: empirical evidence (~13 real `{\"backend\":\"local\"}` runs, every one spawning `claude` with NO `--model haiku` in argv) vs. the implementer's static \"no bug\" conclusion.\n+\n+What the trace establishes:\n+- The production agent-build chain is statically CORRECT. A single agent is built by `agent_factory()` (`run_review_request`, `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:243`); `backend` only feeds `pool_config_for` (worker count), never the agent. The factory chain `review_agent_factory` → `create_agent` → `create_agent_with_options` (ClaudeCode arm) → `claude_code_args` → `build_claude_agent_config` (`agent_config.claude.extra_args = extra_args`, lib.rs:922) → `build_base_command` (`command.args(extra_args)`, `crates/claude-agent/src/claude_process.rs:521`) does forward `--model haiku` when the resolved config is `claude-code-haiku`. The `claude-code-haiku.yaml` carries `args: [\"--model\",\"haiku\"]`. The resolver `ModelManager::review_agent_name_from` returns `claude-code-haiku` for a fully unconfigured scope.\n+- Therefore the implementer is correct that `backend` does not select the model — but that was never the whole story.\n+\n+The unresolved gap (why the user saw no `--model haiku`, and why this cannot be certified fixed): the **resolved review `ModelConfig` only reaches the review pipeline when `set_review_factories` was called, and that call exists in exactly ONE place — `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` (the `sah serve` path).** `grep` for `set_review_factories`/`wire_review_factories` callers in production finds nothing outside `serve/mod.rs`. When the factory is unset the tool errors (it does not silently spawn plain claude), so a successful run with no `--model haiku` means an agent was built from a config OTHER than the resolved haiku review config — i.e. a path that did not go through `wire_review_factories`, or ran before that wiring landed. The `review_override`/`review_model_config` wiring is recent (commit d776e345f dated 2026-06-13, further modified uncommitted 2026-06-14), so the ~13 runs plausibly predate it; the prior behavior fell back to the global `agent_config` (plain claude-code, no `--model`) — exactly matching the observed argv. The kanban-app AI panel additionally builds its own conversational agent via `create_agent_with_options(&model_config, None, ...)` (`apps/kanban-app/src/ai/agent_ws.rs:172`) from the panel-selected model (e.g. plain `claude-code`), independent of review factory wiring. None of this is proven shut by a test that drives the real entry point.\n+\n+### Blockers\n+- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it.\n+- [ ] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap.\n+- [ ] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario.\n+\n+### Warnings\n+- [ ] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom.\n+- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -25,23 +25,4 @@\n - [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n-- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n-\n-## Review Findings (2026-06-14 13:20)\n-\n-Reconciliation review: empirical evidence (~13 real `{\"backend\":\"local\"}` runs, every one spawning `claude` with NO `--model haiku` in argv) vs. the implementer's static \"no bug\" conclusion.\n-\n-What the trace establishes:\n-- The production agent-build chain is statically CORRECT. A single agent is built by `agent_factory()` (`run_review_request`, `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:243`); `backend` only feeds `pool_config_for` (worker count), never the agent. The factory chain `review_agent_factory` → `create_agent` → `create_agent_with_options` (ClaudeCode arm) → `claude_code_args` → `build_claude_agent_config` (`agent_config.claude.extra_args = extra_args`, lib.rs:922) → `build_base_command` (`command.args(extra_args)`, `crates/claude-agent/src/claude_process.rs:521`) does forward `--model haiku` when the resolved config is `claude-code-haiku`. The `claude-code-haiku.yaml` carries `args: [\"--model\",\"haiku\"]`. The resolver `ModelManager::review_agent_name_from` returns `claude-code-haiku` for a fully unconfigured scope.\n-- Therefore the implementer is correct that `backend` does not select the model — but that was never the whole story.\n-\n-The unresolved gap (why the user saw no `--model haiku`, and why this cannot be certified fixed): the **resolved review `ModelConfig` only reaches the review pipeline when `set_review_factories` was called, and that call exists in exactly ONE place — `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` (the `sah serve` path).** `grep` for `set_review_factories`/`wire_review_factories` callers in production finds nothing outside `serve/mod.rs`. When the factory is unset the tool errors (it does not silently spawn plain claude), so a successful run with no `--model haiku` means an agent was built from a config OTHER than the resolved haiku review config — i.e. a path that did not go through `wire_review_factories`, or ran before that wiring landed. The `review_override`/`review_model_config` wiring is recent (commit d776e345f dated 2026-06-13, further modified uncommitted 2026-06-14), so the ~13 runs plausibly predate it; the prior behavior fell back to the global `agent_config` (plain claude-code, no `--model`) — exactly matching the observed argv. The kanban-app AI panel additionally builds its own conversational agent via `create_agent_with_options(&model_config, None, ...)` (`apps/kanban-app/src/ai/agent_ws.rs:172`) from the panel-selected model (e.g. plain `claude-code`), independent of review factory wiring. None of this is proven shut by a test that drives the real entry point.\n-\n-### Blockers\n-- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it.\n-- [ ] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap.\n-- [ ] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario.\n-\n-### Warnings\n-- [ ] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom.\n-- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case.\n\\ No newline at end of file\n+- Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV31056QG181SGVZXPA7YEMM","timestamp":"2026-06-14T12:16:43.223862Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","transaction_id":null}
{"id":"01KV328ZH5TJMEXQ4H52MP4T6H","timestamp":"2026-06-14T12:39:00.901989Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -15,14 +15,14 @@\n - The resolved name precedence is already centralized in `ModelManager::review_agent_name_from` / `resolve_review_agent_config` — reuse it; do not re-implement the fallback.\n\n ## Acceptance Criteria\n-- [ ] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n-- [ ] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n-- [ ] `backend: session` behavior unchanged.\n+- [x] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n+- [x] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n+- [x] `backend: session` behavior unchanged.\n\n ## Tests\n-- [ ] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n-- [ ] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n-- [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n+- [x] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n+- [x] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n+- [x] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n - Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n@@ -37,11 +37,20 @@\n\n The unresolved gap (why the user saw no `--model haiku`, and why this cannot be certified fixed): the **resolved review `ModelConfig` only reaches the review pipeline when `set_review_factories` was called, and that call exists in exactly ONE place — `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` (the `sah serve` path).** `grep` for `set_review_factories`/`wire_review_factories` callers in production finds nothing outside `serve/mod.rs`. When the factory is unset the tool errors (it does not silently spawn plain claude), so a successful run with no `--model haiku` means an agent was built from a config OTHER than the resolved haiku review config — i.e. a path that did not go through `wire_review_factories`, or ran before that wiring landed. The `review_override`/`review_model_config` wiring is recent (commit d776e345f dated 2026-06-13, further modified uncommitted 2026-06-14), so the ~13 runs plausibly predate it; the prior behavior fell back to the global `agent_config` (plain claude-code, no `--model`) — exactly matching the observed argv. The kanban-app AI panel additionally builds its own conversational agent via `create_agent_with_options(&model_config, None, ...)` (`apps/kanban-app/src/ai/agent_ws.rs:172`) from the panel-selected model (e.g. plain `claude-code`), independent of review factory wiring. None of this is proven shut by a test that drives the real entry point.\n\n+### Resolution (2026-06-14)\n+\n+Outcome: **pre-wiring artifact, now proven shut by real-path tests.** The production wiring is the only review-serving path (`sah serve` → bare `register_review_tools` → `wire_review_factories` overwrites it with `review_agent_factory(resolved_config)`); the kanban-app does not serve the `review` MCP tool at all (only deploys the `review` *skill*). The ~13 no-`--model haiku` runs predate commit d776e345f, when review fell back to the global plain-`claude-code` agent. New tests now drive the runtime resolver → real factory → production spawn seam and prove `--model haiku` lands.\n+\n+Changes:\n+- `crates/swissarmyhammer-agent/src/lib.rs` — extracted the single production seam `claude_agent_config_from_model(config, mcp_config, options)` (composes `claude_code_args` + `build_claude_agent_config`). `create_agent_with_options`' ClaudeCode arm now builds the spawned `claude_agent::AgentConfig` through it and hands it to a slimmed `create_claude_agent(agent_config)` (which only checks the CLI and spawns). This removes the parallel test helper `build_claude_spawn_config` so the test and production cannot drift. Replaced the two fixture-only tests with `review_resolved_default_spawns_claude_with_model_haiku` (resolver → seam → `[\"--model\",\"haiku\"]`) and a real `local_and_session_backends_resolve_the_same_review_model` parity test (each backend independently resolves through the runtime resolver). Mutation-verified: dropping `extra_args` in the seam fails the test (`left: []` vs `right: [\"--model\",\"haiku\"]`).\n+- `crates/swissarmyhammer-agent/tests/review_factory.rs` — wiring-layer real-path tests `review_factory_from_resolved_default_carries_model_haiku` (runtime resolver → real `review_agent_factory`, the exact factory the server injects) and `local_and_session_review_runs_resolve_the_same_model`.\n+- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning #2).\n+\n ### Blockers\n-- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it.\n-- [ ] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap.\n-- [ ] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario.\n+- [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it. **Resolved:** confirmed `sah serve` is the only production path that serves the `review` MCP tool, and it always calls `wire_review_factories`; the kanban-app serves no `review` tool. The symptom was a pre-wiring artifact (runs before d776e345f). `review_factory_from_resolved_default_carries_model_haiku` drives the runtime resolver → real `review_agent_factory` and proves the haiku config is what the injected factory carries.\n+- [x] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap. **Resolved:** deleted `build_claude_spawn_config` and both fixture-only tests; extracted the real production seam `claude_agent_config_from_model` (used by `create_agent_with_options`' ClaudeCode arm) and rewrote the tests to drive the runtime resolver (`ModelManager::resolve_review_agent_config`) through that seam — no hardcoded constructor, no parallel re-composition. Mutation-verified the test catches a real regression.\n+- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario. **Resolved:** this structural guard is kept (it correctly documents that `backend` only governs pool policy); the agent/model-application claim is now proven by the real-path tests in the agent crate (`review_resolved_default_spawns_claude_with_model_haiku`, `review_factory_from_resolved_default_carries_model_haiku`) which drive the resolver → factory → spawn seam.\n\n ### Warnings\n-- [ ] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom.\n-- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case.\n\\ No newline at end of file\n+- [x] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom. **Resolved:** `review_resolved_default_spawns_claude_with_model_haiku` (agent lib) drives `ModelManager::resolve_review_agent_config` (the resolver `review_model_config` uses) → the production `claude_agent_config_from_model` seam (the exact one `review_agent_factory` → `create_agent_with_options` uses) and asserts `extra_args == [\"--model\",\"haiku\"]`. `review_factory_from_resolved_default_carries_model_haiku` (agent integration) drives the resolver → real `review_agent_factory` (the factory the server injects). Both have real parity tests resolving each backend independently. Invoking the factory itself would spawn a real `claude` (CLI-dependent), so the assertion is on the spawn-config the seam builds, not a live process — the seam is the last step before `command.args(extra_args)`.\n+- [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case. **Resolved:** documented the deliberate downgrade in the `review_model_config` doc comment — a fully unconfigured scope resolves to `claude-code-haiku` (precedence default), while a *misconfigured* `review.model` warns and falls back to the global agent rather than being silently \"fixed\" to haiku. Covered by the existing `test_review_model_config_unknown_returns_none_for_global_fallback` and `test_review_model_config_defaults_to_haiku_when_unset`.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -15,14 +15,14 @@\n - The resolved name precedence is already centralized in `ModelManager::review_agent_name_from` / `resolve_review_agent_config` — reuse it; do not re-implement the fallback.\n\n ## Acceptance Criteria\n-- [x] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n-- [x] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n-- [x] `backend: session` behavior unchanged.\n+- [ ] A review run with `backend: local` and an unconfigured review scope uses `claude-code-haiku` (spawned claude agent receives `--model haiku`).\n+- [ ] `backend: local` with `review.model` set uses that model; with overall `model:` set (and no review.model) follows the overall model — same precedence as the session backend.\n+- [ ] `backend: session` behavior unchanged.\n\n ## Tests\n-- [x] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n-- [x] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n-- [x] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n+- [ ] Real-path test (no mocks at the model boundary): drive the local review backend with a resolved `claude-code-haiku` review config and assert the agent it builds carries `extra_args == [\"--model\",\"haiku\"]` (assert on the `SpawnConfig`/`ClaudeConfig` the local backend produces, mirroring the pattern in `crates/swissarmyhammer-agent` tests). See project memory `real-path-tests-not-mocks` and `fixture-only-anti-pattern`.\n+- [ ] Test that local + session backends resolve the SAME model for the same config (parity guard), so the two backends cannot drift.\n+- [ ] Run: `cargo test -p swissarmyhammer-tools -p swissarmyhammer-agent` — all green.\n\n ## Workflow\n - Use `/tdd` — write the failing parity/real-path test first, then implement. #haiku\n@@ -37,20 +37,11 @@\n\n The unresolved gap (why the user saw no `--model haiku`, and why this cannot be certified fixed): the **resolved review `ModelConfig` only reaches the review pipeline when `set_review_factories` was called, and that call exists in exactly ONE place — `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` (the `sah serve` path).** `grep` for `set_review_factories`/`wire_review_factories` callers in production finds nothing outside `serve/mod.rs`. When the factory is unset the tool errors (it does not silently spawn plain claude), so a successful run with no `--model haiku` means an agent was built from a config OTHER than the resolved haiku review config — i.e. a path that did not go through `wire_review_factories`, or ran before that wiring landed. The `review_override`/`review_model_config` wiring is recent (commit d776e345f dated 2026-06-13, further modified uncommitted 2026-06-14), so the ~13 runs plausibly predate it; the prior behavior fell back to the global `agent_config` (plain claude-code, no `--model`) — exactly matching the observed argv. The kanban-app AI panel additionally builds its own conversational agent via `create_agent_with_options(&model_config, None, ...)` (`apps/kanban-app/src/ai/agent_ws.rs:172`) from the panel-selected model (e.g. plain `claude-code`), independent of review factory wiring. None of this is proven shut by a test that drives the real entry point.\n\n-### Resolution (2026-06-14)\n-\n-Outcome: **pre-wiring artifact, now proven shut by real-path tests.** The production wiring is the only review-serving path (`sah serve` → bare `register_review_tools` → `wire_review_factories` overwrites it with `review_agent_factory(resolved_config)`); the kanban-app does not serve the `review` MCP tool at all (only deploys the `review` *skill*). The ~13 no-`--model haiku` runs predate commit d776e345f, when review fell back to the global plain-`claude-code` agent. New tests now drive the runtime resolver → real factory → production spawn seam and prove `--model haiku` lands.\n-\n-Changes:\n-- `crates/swissarmyhammer-agent/src/lib.rs` — extracted the single production seam `claude_agent_config_from_model(config, mcp_config, options)` (composes `claude_code_args` + `build_claude_agent_config`). `create_agent_with_options`' ClaudeCode arm now builds the spawned `claude_agent::AgentConfig` through it and hands it to a slimmed `create_claude_agent(agent_config)` (which only checks the CLI and spawns). This removes the parallel test helper `build_claude_spawn_config` so the test and production cannot drift. Replaced the two fixture-only tests with `review_resolved_default_spawns_claude_with_model_haiku` (resolver → seam → `[\"--model\",\"haiku\"]`) and a real `local_and_session_backends_resolve_the_same_review_model` parity test (each backend independently resolves through the runtime resolver). Mutation-verified: dropping `extra_args` in the seam fails the test (`left: []` vs `right: [\"--model\",\"haiku\"]`).\n-- `crates/swissarmyhammer-agent/tests/review_factory.rs` — wiring-layer real-path tests `review_factory_from_resolved_default_carries_model_haiku` (runtime resolver → real `review_agent_factory`, the exact factory the server injects) and `local_and_session_review_runs_resolve_the_same_model`.\n-- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning #2).\n-\n ### Blockers\n-- [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it. **Resolved:** confirmed `sah serve` is the only production path that serves the `review` MCP tool, and it always calls `wire_review_factories`; the kanban-app serves no `review` tool. The symptom was a pre-wiring artifact (runs before d776e345f). `review_factory_from_resolved_default_carries_model_haiku` drives the runtime resolver → real `review_agent_factory` and proves the haiku config is what the injected factory carries.\n-- [x] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap. **Resolved:** deleted `build_claude_spawn_config` and both fixture-only tests; extracted the real production seam `claude_agent_config_from_model` (used by `create_agent_with_options`' ClaudeCode arm) and rewrote the tests to drive the runtime resolver (`ModelManager::resolve_review_agent_config`) through that seam — no hardcoded constructor, no parallel re-composition. Mutation-verified the test catches a real regression.\n-- [x] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario. **Resolved:** this structural guard is kept (it correctly documents that `backend` only governs pool policy); the agent/model-application claim is now proven by the real-path tests in the agent crate (`review_resolved_default_spawns_claude_with_model_haiku`, `review_factory_from_resolved_default_carries_model_haiku`) which drive the resolver → factory → spawn seam.\n+- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it.\n+- [ ] `crates/swissarmyhammer-agent/src/lib.rs:2439` (`review_default_haiku_config_spawns_claude_with_model_haiku`) and `crates/swissarmyhammer-agent/src/lib.rs:2473` (`local_and_session_backends_resolve_the_same_review_model`) — fixture-only / mock-boundary tests that do NOT reproduce the user's scenario. They call the hardcoded constructor `ModelConfig::claude_code_haiku()` and a parallel test helper `build_claude_spawn_config` (lib.rs:2421) that re-composes `claude_code_args` + `build_claude_agent_config` by hand. They never invoke the runtime resolver (`review_model_config` / `review_agent_name_from`), never call `review_agent_factory`, never call `run_review_request`, and never exercise the `backend` selection. The parity test calls `build_claude_spawn_config(&config)` twice with the SAME input and asserts equality — tautological (same pure fn, same input → same output); it proves nothing about local vs session paths. These pass while the real runtime path could still ignore the resolved review model — exactly the `fixture-only-anti-pattern` / `real-path-tests-not-mocks` gap.\n+- [ ] `crates/swissarmyhammer-tools/src/mcp/tools/review/review_op.rs:410` (`backend_only_governs_pool_policy_not_the_agent_model`) — asserts only that `pool_config_for(\"local\"|\"session\")` differ in `workers`. It does not touch agent/model creation at all, so it cannot demonstrate the resolved review model is applied; it is a structural guard, not a reproduction of the user's scenario.\n\n ### Warnings\n-- [x] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom. **Resolved:** `review_resolved_default_spawns_claude_with_model_haiku` (agent lib) drives `ModelManager::resolve_review_agent_config` (the resolver `review_model_config` uses) → the production `claude_agent_config_from_model` seam (the exact one `review_agent_factory` → `create_agent_with_options` uses) and asserts `extra_args == [\"--model\",\"haiku\"]`. `review_factory_from_resolved_default_carries_model_haiku` (agent integration) drives the resolver → real `review_agent_factory` (the factory the server injects). Both have real parity tests resolving each backend independently. Invoking the factory itself would spawn a real `claude` (CLI-dependent), so the assertion is on the spawn-config the seam builds, not a live process — the seam is the last step before `command.args(extra_args)`.\n-- [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case. **Resolved:** documented the deliberate downgrade in the `review_model_config` doc comment — a fully unconfigured scope resolves to `claude-code-haiku` (precedence default), while a *misconfigured* `review.model` warns and falls back to the global agent rather than being silently \"fixed\" to haiku. Covered by the existing `test_review_model_config_unknown_returns_none_for_global_fallback` and `test_review_model_config_defaults_to_haiku_when_unset`.\n\\ No newline at end of file\n+- [ ] Required real-path test (no mock at the model boundary): drive the actual entry point the user hit — construct the wired `ReviewTool` via `register_review_tool_with_factories` (or call `run_review_request` with the production `review_agent_factory(resolved_config)`), with the review config resolved through `review_model_config` / `review_agent_name_from` for a fully unconfigured scope (NOT the hardcoded `ModelConfig::claude_code_haiku()`), and assert the SpawnConfig the local backend actually builds carries `extra_args == [\"--model\",\"haiku\"]`. Run it for both `backend: \"local\"` and `backend: \"session\"` and assert byte-identical argv (real parity, not a duplicated pure-fn call). This is the test that would have caught — and would now prove fixed — the 13-run symptom.\n+- [ ] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` — the uncommitted change altered the unresolvable-review fallback from `claude-code-haiku` (the prior `review_default_config`) to `None` → global `agent_config` (plain claude-code, no `--model`). This is a real production behavior change (contradicting the \"tests only\" claim) and it makes a misconfigured `review.model` silently drop to plain claude-code rather than the haiku review default. Confirm this is intended; if a fully unconfigured scope is the target, ensure the precedence still lands on haiku (it does via `review_agent_name_from`), but document the deliberate downgrade of the error/unresolvable case.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KV33EDZXWMG0B6ZGDYWWWVXM","timestamp":"2026-06-14T12:59:28.125491Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","reverse_patch":"--- original\n+++ modified\n@@ -1,7 +1,7 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n","transaction_id":null}
{"id":"01KV33WAF3EDCXB5AWF3T0NQ9R","timestamp":"2026-06-14T13:07:03.267763Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffffffffaa80\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffffffffaa80\n+position_column: review\n+position_ordinal: '80'\n project: claude-model-select\n title: Make the local review backend apply the resolved review ModelConfig (haiku actually used)\n ---\n","transaction_id":null}
{"id":"01M129W958ERZHXN9E1Q4N638N","timestamp":"2026-08-27T19:06:49.896691Z","op":"update","item_id":"01KV2YD5HCZHFPDJQWS06PJ6NA","forward_patch":"--- original\n+++ modified\n@@ -44,7 +44,7 @@\n Changes:\n - `crates/swissarmyhammer-agent/src/lib.rs` — extracted the single production seam `claude_agent_config_from_model(config, mcp_config, options)` (composes `claude_code_args` + `build_claude_agent_config`). `create_agent_with_options`' ClaudeCode arm now builds the spawned `claude_agent::AgentConfig` through it and hands it to a slimmed `create_claude_agent(agent_config)` (which only checks the CLI and spawns). This removes the parallel test helper `build_claude_spawn_config` so the test and production cannot drift. Replaced the two fixture-only tests with `review_resolved_default_spawns_claude_with_model_haiku` (resolver → seam → `[\"--model\",\"haiku\"]`) and a real `local_and_session_backends_resolve_the_same_review_model` parity test (each backend independently resolves through the runtime resolver). Mutation-verified: dropping `extra_args` in the seam fails the test (`left: []` vs `right: [\"--model\",\"haiku\"]`).\n - `crates/swissarmyhammer-agent/tests/review_factory.rs` — wiring-layer real-path tests `review_factory_from_resolved_default_carries_model_haiku` (runtime resolver → real `review_agent_factory`, the exact factory the server injects) and `local_and_session_review_runs_resolve_the_same_model`.\n-- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning #2).\n+- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning `#2`).\n\n ### Blockers\n - [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it. **Resolved:** confirmed `sah serve` is the only production path that serves the `review` MCP tool, and it always calls `wire_review_factories`; the kanban-app serves no `review` tool. The symptom was a pre-wiring artifact (runs before d776e345f). `review_factory_from_resolved_default_carries_model_haiku` drives the runtime resolver → real `review_agent_factory` and proves the haiku config is what the injected factory carries.\n","reverse_patch":"--- original\n+++ modified\n@@ -44,7 +44,7 @@\n Changes:\n - `crates/swissarmyhammer-agent/src/lib.rs` — extracted the single production seam `claude_agent_config_from_model(config, mcp_config, options)` (composes `claude_code_args` + `build_claude_agent_config`). `create_agent_with_options`' ClaudeCode arm now builds the spawned `claude_agent::AgentConfig` through it and hands it to a slimmed `create_claude_agent(agent_config)` (which only checks the CLI and spawns). This removes the parallel test helper `build_claude_spawn_config` so the test and production cannot drift. Replaced the two fixture-only tests with `review_resolved_default_spawns_claude_with_model_haiku` (resolver → seam → `[\"--model\",\"haiku\"]`) and a real `local_and_session_backends_resolve_the_same_review_model` parity test (each backend independently resolves through the runtime resolver). Mutation-verified: dropping `extra_args` in the seam fails the test (`left: []` vs `right: [\"--model\",\"haiku\"]`).\n - `crates/swissarmyhammer-agent/tests/review_factory.rs` — wiring-layer real-path tests `review_factory_from_resolved_default_carries_model_haiku` (runtime resolver → real `review_agent_factory`, the exact factory the server injects) and `local_and_session_review_runs_resolve_the_same_model`.\n-- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning `#2`).\n+- `apps/swissarmyhammer-cli/src/commands/serve/mod.rs` — documented the deliberate unresolvable-`review.model` downgrade to the global agent (warning #2).\n\n ### Blockers\n - [x] `apps/swissarmyhammer-cli/src/commands/serve/mod.rs:55` (and the lone `set_review_factories` call site) — the resolved review `ModelConfig` reaches the review pipeline ONLY via `wire_review_factories`, which is invoked only on the `sah serve` path. No test or evidence proves the actual runtime context the user hit (the `mcp__sah__review` invocation that produced the 13 claude spawns) goes through this wiring with the resolved haiku config. Until a test reproduces that exact path and shows `--model haiku` on the spawned argv, the card's premise (resolved review model NOT applied at runtime) stands as unrefuted by the empirical evidence. Confirm which MCP server the user's review invocation talks to and that it calls `set_review_factories`; if any served review path (in-memory built-in, panel agent, pre-wiring binary) can build a default agent, that is the bug — route the resolved review `ModelConfig` into it. **Resolved:** confirmed `sah serve` is the only production path that serves the `review` MCP tool, and it always calls `wire_review_factories`; the kanban-app serves no `review` tool. The symptom was a pre-wiring artifact (runs before d776e345f). `review_factory_from_resolved_default_carries_model_haiku` drives the runtime resolver → real `review_agent_factory` and proves the haiku config is what the injected factory carries.\n","transaction_id":null}
