{"id":"01KQQV1FDRCCTTH1MRBJDBWDCQ","timestamp":"2026-05-03T21:13:50.776282Z","op":"create","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,75 @@\n+---\n+assignees:\n+- claude-code\n+depends_on:\n+- 01KQQSXM2PEYR1WAQ7QXW3B8ME\n+- 01KQQTXDHP3XBHZ8G40AC4FG4D\n+position_column: todo\n+position_ordinal: d380\n+project: spatial-nav\n+title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n+---\n+## Reference\n+\n+Part of the spatial-nav redesign. Full design: **`01KQQSXM2PEYR1WAQ7QXW3B8ME`** — read it before starting, especially the \"Virtualization\" section.\n+\n+**This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n+\n+**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n+\n+**Contract (restated from design):**\n+\n+> When the kernel returns stay-put (`result === focusedFq`) AND the focused scope is at the edge of a scrollable ancestor in direction D AND that ancestor can scroll further in D, scroll the ancestor by one item-height in D, wait for the virtualizer to mount the next row, then re-run nav.\n+\n+This rule lives in **React glue, not the Rust kernel.** The kernel doesn't know about scroll containers — those are DOM-only. The kernel returns stay-put; the React side decides what to do next.\n+\n+## What\n+\n+### Files to modify\n+\n+- `kanban-app/ui/src/components/app-shell.tsx`:\n+  - In `buildNavCommands` (the four cardinal command builders that dispatch `spatial_navigate`), wrap the result handling so that when the kernel returns the focused FQM (stay-put):\n+    1. Find the focused scope's nearest scrollable ancestor in direction D (walk DOM ancestors, check `overflow-y` for vertical / `overflow-x` for horizontal, check `scrollHeight > clientHeight` etc.).\n+    2. If that ancestor can scroll further in D (compare scroll position to scroll size), scroll it by one item-height (or some sensible step — `Math.max(focused-rect-height, 64px)` is a reasonable default).\n+    3. Wait for the next animation frame (so the virtualizer has a chance to mount the freshly-revealed row), then re-dispatch the same nav command. This time geometric pick should find a candidate.\n+    4. Cap the retry depth at 1 to avoid infinite loops on weird layouts.\n+\n+- `kanban-app/ui/src/components/use-track-rect-on-ancestor-scroll.ts`:\n+  - No code changes needed (it already updates rects on scroll), but verify the rect refresh runs synchronously enough that the re-dispatched nav sees the new rects.\n+\n+- New file `kanban-app/ui/src/lib/scroll-on-edge.ts` (or similar):\n+  - Extract the \"find scrollable ancestor in direction\" + \"can scroll further?\" logic into a small helper. Pure function over a DOM element + Direction; testable in isolation. ~50 lines.\n+\n+- `swissarmyhammer-focus/README.md`:\n+  - Add a \"## Scrolling\" section describing the rule, where it lives (React glue), and noting that the kernel itself remains scroll-unaware.\n+\n+### Tests\n+\n+- **Unit test in `kanban-app/ui/src/lib/scroll-on-edge.test.ts`** for the helper:\n+  - Given a DOM element inside a `overflow-y: auto` ancestor with content larger than viewport, `scrollableAncestorInDirection(el, \"Down\")` returns the ancestor.\n+  - When scroll position is at max, `canScrollFurther(ancestor, \"Down\")` returns false.\n+  - When scroll position is < max, returns true.\n+  - Walks past `overflow: visible` ancestors.\n+- **End-to-end browser test in `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx`** (new file):\n+  - Mount a column with enough cards that virtualization kicks in (~50 cards).\n+  - Drive focus to the last visible card via simulated focus event.\n+  - Fire keydown ArrowDown.\n+  - Assert (a) the column scrolled (scroll position increased), (b) after one animation frame, focus moved to a card that was previously off-viewport, (c) `data-focused=\"true\"` is on the new card.\n+- **End-to-end browser test for horizontal**: similar but for the column strip — Right from the rightmost card in the rightmost visible column triggers a horizontal scroll of the strip.\n+- **Negative test**: when the ancestor is fully scrolled to the end, the scroll-on-edge fallback does NOT fire (focus stays put genuinely). No infinite loop.\n+- Run `pnpm -C kanban-app/ui test scroll-on-edge column-view.virtualized-nav` and confirm green.\n+\n+## Acceptance Criteria\n+\n+- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n+- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n+- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n+- [ ] No infinite loop: retry depth is capped at 1.\n+- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n+- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n+- [ ] `pnpm -C kanban-app/ui test` passes.\n+\n+## Workflow\n+\n+- Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n+- Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n","reverse_patch":"--- original\n+++ modified\n@@ -1,75 +0,0 @@\n----\n-assignees:\n-- claude-code\n-depends_on:\n-- 01KQQSXM2PEYR1WAQ7QXW3B8ME\n-- 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: todo\n-position_ordinal: d380\n-project: spatial-nav\n-title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n----\n-## Reference\n-\n-Part of the spatial-nav redesign. Full design: **`01KQQSXM2PEYR1WAQ7QXW3B8ME`** — read it before starting, especially the \"Virtualization\" section.\n-\n-**This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n-\n-**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n-\n-**Contract (restated from design):**\n-\n-> When the kernel returns stay-put (`result === focusedFq`) AND the focused scope is at the edge of a scrollable ancestor in direction D AND that ancestor can scroll further in D, scroll the ancestor by one item-height in D, wait for the virtualizer to mount the next row, then re-run nav.\n-\n-This rule lives in **React glue, not the Rust kernel.** The kernel doesn't know about scroll containers — those are DOM-only. The kernel returns stay-put; the React side decides what to do next.\n-\n-## What\n-\n-### Files to modify\n-\n-- `kanban-app/ui/src/components/app-shell.tsx`:\n-  - In `buildNavCommands` (the four cardinal command builders that dispatch `spatial_navigate`), wrap the result handling so that when the kernel returns the focused FQM (stay-put):\n-    1. Find the focused scope's nearest scrollable ancestor in direction D (walk DOM ancestors, check `overflow-y` for vertical / `overflow-x` for horizontal, check `scrollHeight > clientHeight` etc.).\n-    2. If that ancestor can scroll further in D (compare scroll position to scroll size), scroll it by one item-height (or some sensible step — `Math.max(focused-rect-height, 64px)` is a reasonable default).\n-    3. Wait for the next animation frame (so the virtualizer has a chance to mount the freshly-revealed row), then re-dispatch the same nav command. This time geometric pick should find a candidate.\n-    4. Cap the retry depth at 1 to avoid infinite loops on weird layouts.\n-\n-- `kanban-app/ui/src/components/use-track-rect-on-ancestor-scroll.ts`:\n-  - No code changes needed (it already updates rects on scroll), but verify the rect refresh runs synchronously enough that the re-dispatched nav sees the new rects.\n-\n-- New file `kanban-app/ui/src/lib/scroll-on-edge.ts` (or similar):\n-  - Extract the \"find scrollable ancestor in direction\" + \"can scroll further?\" logic into a small helper. Pure function over a DOM element + Direction; testable in isolation. ~50 lines.\n-\n-- `swissarmyhammer-focus/README.md`:\n-  - Add a \"## Scrolling\" section describing the rule, where it lives (React glue), and noting that the kernel itself remains scroll-unaware.\n-\n-### Tests\n-\n-- **Unit test in `kanban-app/ui/src/lib/scroll-on-edge.test.ts`** for the helper:\n-  - Given a DOM element inside a `overflow-y: auto` ancestor with content larger than viewport, `scrollableAncestorInDirection(el, \"Down\")` returns the ancestor.\n-  - When scroll position is at max, `canScrollFurther(ancestor, \"Down\")` returns false.\n-  - When scroll position is < max, returns true.\n-  - Walks past `overflow: visible` ancestors.\n-- **End-to-end browser test in `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx`** (new file):\n-  - Mount a column with enough cards that virtualization kicks in (~50 cards).\n-  - Drive focus to the last visible card via simulated focus event.\n-  - Fire keydown ArrowDown.\n-  - Assert (a) the column scrolled (scroll position increased), (b) after one animation frame, focus moved to a card that was previously off-viewport, (c) `data-focused=\"true\"` is on the new card.\n-- **End-to-end browser test for horizontal**: similar but for the column strip — Right from the rightmost card in the rightmost visible column triggers a horizontal scroll of the strip.\n-- **Negative test**: when the ancestor is fully scrolled to the end, the scroll-on-edge fallback does NOT fire (focus stays put genuinely). No infinite loop.\n-- Run `pnpm -C kanban-app/ui test scroll-on-edge column-view.virtualized-nav` and confirm green.\n-\n-## Acceptance Criteria\n-\n-- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n-- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n-- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n-- [ ] No infinite loop: retry depth is capped at 1.\n-- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n-- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n-- [ ] `pnpm -C kanban-app/ui test` passes.\n-\n-## Workflow\n-\n-- Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n-- Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n","transaction_id":null}
{"id":"01KQQV1FDTTMXV39AK2VQ49QQ2","timestamp":"2026-05-03T21:13:50.778847Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"create","changes":[["assignees",{"kind":"set","value":["claude-code"]}],["position_ordinal",{"kind":"set","value":"d380"}],["title",{"kind":"set","value":"Spatial-nav #5: scroll-on-edge for virtualized regions"}],["body",{"kind":"set","value":"## Reference\n\nPart of the spatial-nav redesign. Full design: **`01KQQSXM2PEYR1WAQ7QXW3B8ME`** — read it before starting, especially the \"Virtualization\" section.\n\n**This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n\n**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n\n**Contract (restated from design):**\n\n> When the kernel returns stay-put (`result === focusedFq`) AND the focused scope is at the edge of a scrollable ancestor in direction D AND that ancestor can scroll further in D, scroll the ancestor by one item-height in D, wait for the virtualizer to mount the next row, then re-run nav.\n\nThis rule lives in **React glue, not the Rust kernel.** The kernel doesn't know about scroll containers — those are DOM-only. The kernel returns stay-put; the React side decides what to do next.\n\n## What\n\n### Files to modify\n\n- `kanban-app/ui/src/components/app-shell.tsx`:\n  - In `buildNavCommands` (the four cardinal command builders that dispatch `spatial_navigate`), wrap the result handling so that when the kernel returns the focused FQM (stay-put):\n    1. Find the focused scope's nearest scrollable ancestor in direction D (walk DOM ancestors, check `overflow-y` for vertical / `overflow-x` for horizontal, check `scrollHeight > clientHeight` etc.).\n    2. If that ancestor can scroll further in D (compare scroll position to scroll size), scroll it by one item-height (or some sensible step — `Math.max(focused-rect-height, 64px)` is a reasonable default).\n    3. Wait for the next animation frame (so the virtualizer has a chance to mount the freshly-revealed row), then re-dispatch the same nav command. This time geometric pick should find a candidate.\n    4. Cap the retry depth at 1 to avoid infinite loops on weird layouts.\n\n- `kanban-app/ui/src/components/use-track-rect-on-ancestor-scroll.ts`:\n  - No code changes needed (it already updates rects on scroll), but verify the rect refresh runs synchronously enough that the re-dispatched nav sees the new rects.\n\n- New file `kanban-app/ui/src/lib/scroll-on-edge.ts` (or similar):\n  - Extract the \"find scrollable ancestor in direction\" + \"can scroll further?\" logic into a small helper. Pure function over a DOM element + Direction; testable in isolation. ~50 lines.\n\n- `swissarmyhammer-focus/README.md`:\n  - Add a \"## Scrolling\" section describing the rule, where it lives (React glue), and noting that the kernel itself remains scroll-unaware.\n\n### Tests\n\n- **Unit test in `kanban-app/ui/src/lib/scroll-on-edge.test.ts`** for the helper:\n  - Given a DOM element inside a `overflow-y: auto` ancestor with content larger than viewport, `scrollableAncestorInDirection(el, \"Down\")` returns the ancestor.\n  - When scroll position is at max, `canScrollFurther(ancestor, \"Down\")` returns false.\n  - When scroll position is < max, returns true.\n  - Walks past `overflow: visible` ancestors.\n- **End-to-end browser test in `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx`** (new file):\n  - Mount a column with enough cards that virtualization kicks in (~50 cards).\n  - Drive focus to the last visible card via simulated focus event.\n  - Fire keydown ArrowDown.\n  - Assert (a) the column scrolled (scroll position increased), (b) after one animation frame, focus moved to a card that was previously off-viewport, (c) `data-focused=\"true\"` is on the new card.\n- **End-to-end browser test for horizontal**: similar but for the column strip — Right from the rightmost card in the rightmost visible column triggers a horizontal scroll of the strip.\n- **Negative test**: when the ancestor is fully scrolled to the end, the scroll-on-edge fallback does NOT fire (focus stays put genuinely). No infinite loop.\n- Run `pnpm -C kanban-app/ui test scroll-on-edge column-view.virtualized-nav` and confirm green.\n\n## Acceptance Criteria\n\n- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n- [ ] No infinite loop: retry depth is capped at 1.\n- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n- [ ] `pnpm -C kanban-app/ui test` passes.\n\n## Workflow\n\n- Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n- Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n"}],["depends_on",{"kind":"set","value":["01KQQSXM2PEYR1WAQ7QXW3B8ME","01KQQTXDHP3XBHZ8G40AC4FG4D"]}],["position_column",{"kind":"set","value":"todo"}],["project",{"kind":"set","value":"spatial-nav"}]]}
{"id":"01KQQV2XYCX5ERWTSTZMWGCNXH","timestamp":"2026-05-03T21:14:38.412759Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -73,3 +73,4 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n+#spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -73,4 +73,3 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQQV2XYDSZAF2YS45DF042FE","timestamp":"2026-05-03T21:14:38.413208Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -62,3 +62,4 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n+#spatial-nav-redesign\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,4 +62,3 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n\\ No newline at end of file\n"}]]}
{"id":"01KQR2ZA03KZRGN6TBTVG48V4Z","timestamp":"2026-05-03T23:32:28.291218Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: todo\n-position_ordinal: d380\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n-position_ordinal: '80'\n+position_column: todo\n+position_ordinal: d380\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR2ZA07VJPEP7E5TQKSN4AP","timestamp":"2026-05-03T23:32:28.295688Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-todo\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+todo\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-d380\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+d380\n\\ No newline at end of file\n"}]]}
{"id":"01KQR3JV0AHC9SRMPHZW7ZJHFT","timestamp":"2026-05-03T23:43:08.298915Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -61,13 +61,13 @@\n\n ## Acceptance Criteria\n\n-- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n-- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n-- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n-- [ ] No infinite loop: retry depth is capped at 1.\n-- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n-- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n-- [ ] `pnpm -C kanban-app/ui test` passes.\n+- [x] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n+- [x] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n+- [x] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n+- [x] No infinite loop: retry depth is capped at 1.\n+- [x] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n+- [x] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n+- [x] `pnpm -C kanban-app/ui test` passes.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -61,13 +61,13 @@\n\n ## Acceptance Criteria\n\n-- [x] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n-- [x] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n-- [x] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n-- [x] No infinite loop: retry depth is capped at 1.\n-- [x] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n-- [x] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n-- [x] `pnpm -C kanban-app/ui test` passes.\n+- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n+- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n+- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n+- [ ] No infinite loop: retry depth is capped at 1.\n+- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n+- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n+- [ ] `pnpm -C kanban-app/ui test` passes.\n\n ## Workflow\n\n","transaction_id":null}
{"id":"01KQR3JV0BY1H8VVTVRXHWV4S0","timestamp":"2026-05-03T23:43:08.299869Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -50,13 +50,13 @@\n\n ## Acceptance Criteria\n\n-- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n-- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n-- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n-- [ ] No infinite loop: retry depth is capped at 1.\n-- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n-- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n-- [ ] `pnpm -C kanban-app/ui test` passes.\n+- [x] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n+- [x] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n+- [x] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n+- [x] No infinite loop: retry depth is capped at 1.\n+- [x] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n+- [x] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n+- [x] `pnpm -C kanban-app/ui test` passes.\n\n ## Workflow\n\n","reverse_patch":"--- original\n+++ modified\n@@ -50,13 +50,13 @@\n\n ## Acceptance Criteria\n\n-- [x] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n-- [x] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n-- [x] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n-- [x] No infinite loop: retry depth is capped at 1.\n-- [x] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n-- [x] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n-- [x] `pnpm -C kanban-app/ui test` passes.\n+- [ ] Pressing ArrowDown from the last visible card in a virtualized column scrolls the column to reveal the next card AND moves focus to it (one keypress, one user-visible action).\n+- [ ] Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column.\n+- [ ] When the scrollable ancestor is fully scrolled in direction D, the fallback does NOT fire — focus stays put as it should at a true visual edge.\n+- [ ] No infinite loop: retry depth is capped at 1.\n+- [ ] The helper in `scroll-on-edge.ts` has unit tests covering the four cases above.\n+- [ ] README \"## Scrolling\" section documents the rule and notes the kernel remains scroll-unaware.\n+- [ ] `pnpm -C kanban-app/ui test` passes.\n\n ## Workflow\n\n"}]]}
{"id":"01KQR46FVH8R3P3QK5RZ26S1Q8","timestamp":"2026-05-03T23:53:52.241277Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR46FVJA03ZK0JYA49MWZAM","timestamp":"2026-05-03T23:53:52.242011Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQR599CW9ACQQK3WMV7K9TE7","timestamp":"2026-05-04T00:12:52.508105Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -73,4 +73,14 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n\\ No newline at end of file\n+#spatial-nav-redesign\n+\n+## Review Findings (2026-05-03 19:12)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -73,14 +73,4 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n-\n-## Review Findings (2026-05-03 19:12)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n+#spatial-nav-redesign\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQR599CW0PMTPB5KYQFPN19F","timestamp":"2026-05-04T00:12:52.508567Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -62,4 +62,14 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n\\ No newline at end of file\n+#spatial-nav-redesign\n+\n+## Review Findings (2026-05-03 19:12)\n+\n+### Warnings\n+- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -62,14 +62,4 @@\n\n - Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n-#spatial-nav-redesign\n-\n-## Review Findings (2026-05-03 19:12)\n-\n-### Warnings\n-- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n+#spatial-nav-redesign\n\\ No newline at end of file\n"}]]}
{"id":"01KQR5CP7H405AKZDBBW271QV6","timestamp":"2026-05-04T00:14:43.953270Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n-position_ordinal: '8280'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8280'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR5CP7JC1G8NWTCR3KQM4QX","timestamp":"2026-05-04T00:14:43.954337Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8280\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8280\n\\ No newline at end of file\n"}]]}
{"id":"01KQR6FRZ04TNNF5ARR1WP00ND","timestamp":"2026-05-04T00:33:53.632528Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -78,9 +78,19 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n+  **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n+\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+\n+  **Addressed:** Adopted the suggested re-read. After `await nextFrame()` post-scroll, the helper now reads `fqAfterScroll = actions.focusedFq()` and bails out (no retry IPC) if it's `null` or differs from the captured `fq`. Existing tests still pass (their stub focus state remains stable across the wait, so `fqAfterScroll === fq` and the retry fires as before). Docstring updated with a \"Why we re-read `focusedFq()` before the retry navigate\" section.\n+\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+\n+  **Addressed:** Dropped `overlay` from the accepted set. `isScrollableOnAxis` now only accepts `auto` and `scroll`. Function-level docstring notes the rationale (modern browsers normalize `overlay` back to `auto`, so accepting it would only matter on obsolete engines).\n+\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n+\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -78,19 +78,9 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n-\n-  **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n+- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-\n-  **Addressed:** Adopted the suggested re-read. After `await nextFrame()` post-scroll, the helper now reads `fqAfterScroll = actions.focusedFq()` and bails out (no retry IPC) if it's `null` or differs from the captured `fq`. Existing tests still pass (their stub focus state remains stable across the wait, so `fqAfterScroll === fq` and the retry fires as before). Docstring updated with a \"Why we re-read `focusedFq()` before the retry navigate\" section.\n-\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-\n-  **Addressed:** Dropped `overlay` from the accepted set. `isScrollableOnAxis` now only accepts `auto` and `scroll`. Function-level docstring notes the rationale (modern browsers normalize `overlay` back to `auto`, so accepting it would only matter on obsolete engines).\n-\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n-\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQR6FRZ0EH45H4V7GDBBTVWR","timestamp":"2026-05-04T00:33:53.632966Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -67,9 +67,19 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n+  **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n+\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+\n+  **Addressed:** Adopted the suggested re-read. After `await nextFrame()` post-scroll, the helper now reads `fqAfterScroll = actions.focusedFq()` and bails out (no retry IPC) if it's `null` or differs from the captured `fq`. Existing tests still pass (their stub focus state remains stable across the wait, so `fqAfterScroll === fq` and the retry fires as before). Docstring updated with a \"Why we re-read `focusedFq()` before the retry navigate\" section.\n+\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+\n+  **Addressed:** Dropped `overlay` from the accepted set. `isScrollableOnAxis` now only accepts `auto` and `scroll`. Function-level docstring notes the rationale (modern browsers normalize `overlay` back to `auto`, so accepting it would only matter on obsolete engines).\n+\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n+\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -67,19 +67,9 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n-\n-  **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n+- [ ] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n-\n-  **Addressed:** Adopted the suggested re-read. After `await nextFrame()` post-scroll, the helper now reads `fqAfterScroll = actions.focusedFq()` and bails out (no retry IPC) if it's `null` or differs from the captured `fq`. Existing tests still pass (their stub focus state remains stable across the wait, so `fqAfterScroll === fq` and the retry fires as before). Docstring updated with a \"Why we re-read `focusedFq()` before the retry navigate\" section.\n-\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n-\n-  **Addressed:** Dropped `overlay` from the accepted set. `isScrollableOnAxis` now only accepts `auto` and `scroll`. Function-level docstring notes the rationale (modern browsers normalize `overlay` back to `auto`, so accepting it would only matter on obsolete engines).\n-\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n-\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:312-329` — `runNavWithScrollOnEdge` captures `fq` once (line 316) and re-uses it on the retry navigate (line 328). Steps 4 (`actions.focusedFq() !== fq`) correctly guards against focus moving during the first `await nextFrame()`, but a focus change during the *second* `await nextFrame()` (between scroll and retry) would dispatch navigate from a now-stale `fq`. The kernel handles unknown fq gracefully (returns stay-put), so the worst case is one wasted IPC — not a correctness bug, but the comment block could note this and explicitly call out that re-reading `focusedFq()` after the scroll wait would be a defensible alternative if a future regression exposes the timing window. Optional: re-read with `const fq2 = actions.focusedFq(); if (fq2 !== fq) return;` before the retry navigate.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:128` — `isScrollableOnAxis` accepts `auto`, `scroll`, and `overlay` overflow values. Only `auto` and `scroll` are tested; `overlay` is not exercised by any unit test (and `overlay` is also a non-standard / Webkit-deprecated value, marked legacy in CSS Overflow spec). Suggested fix: either drop `overlay` from the accepted set (cleaner — modern browsers normalize it back to `auto`) or add a unit test that covers it. Either is fine; the current state silently accepts a legacy value.\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\\ No newline at end of file\n"}]]}
{"id":"01KQR6TFQVPR8ZCY8MV6C2D51P","timestamp":"2026-05-04T00:39:44.635232Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR6TFQW5JXSXAP03HW99KCD","timestamp":"2026-05-04T00:39:44.636083Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KQR6ZTJA3DYSR00NM807RZX8","timestamp":"2026-05-04T00:42:39.562340Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -93,4 +93,9 @@\n\n - [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n+\n+## Review Findings (2026-05-03 21:05)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -93,9 +93,4 @@\n\n - [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n-\n-## Review Findings (2026-05-03 21:05)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQR6ZTJAXP055CK4D1DXMV93","timestamp":"2026-05-04T00:42:39.562779Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -82,4 +82,9 @@\n\n - [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n+\n+## Review Findings (2026-05-03 21:05)\n+\n+### Nits\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -82,9 +82,4 @@\n\n - [x] `kanban-app/ui/src/lib/scroll-on-edge.test.ts` — Unit tests cover `overflow: visible` ancestors at line 146 but no test exercises `overflow: hidden` (which the helper also walks past). The README docstring at lines 19–24 says auto/scroll qualify and visible/hidden are walked past — the implementation is correct, but a one-line test for `hidden` would close the documentation gap.\n\n-  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n-\n-## Review Findings (2026-05-03 21:05)\n-\n-### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n+  **Addressed:** Added a `walks past \\`overflow: hidden\\` ancestors` unit test that mounts the vertical scroll fixture inside a wrapping `overflow: hidden` div and asserts `scrollableAncestorInDirection(leaf, \"down\")` returns the inner scroller (walking past the hidden wrapper).\n\\ No newline at end of file\n"}]]}
{"id":"01KQR7D68VKNCK4DW6N5MNERTG","timestamp":"2026-05-04T00:49:57.531613Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n-position_ordinal: '8180'\n+position_column: doing\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n-position_ordinal: '80'\n+position_column: review\n+position_ordinal: '8180'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR7D68WZCHWTMCET8CRKJ39","timestamp":"2026-05-04T00:49:57.532805Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-8180\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+8180\n\\ No newline at end of file\n"}]]}
{"id":"01KQR7HGY946N3P667D3DQ9X61","timestamp":"2026-05-04T00:52:19.529425Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -98,4 +98,6 @@\n ## Review Findings (2026-05-03 21:05)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n+\n+  **Addressed:** Changed the `scrollableAncestorInDirection` docstring to read `auto` or `scroll` (dropped the stale `, or overlay` reference). The public contract now matches the `isScrollableOnAxis` implementation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -98,6 +98,4 @@\n ## Review Findings (2026-05-03 21:05)\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n-\n-  **Addressed:** Changed the `scrollableAncestorInDirection` docstring to read `auto` or `scroll` (dropped the stale `, or overlay` reference). The public contract now matches the `isScrollableOnAxis` implementation.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01KQR7HGY94T2FN29M525QE44S","timestamp":"2026-05-04T00:52:19.529889Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -87,4 +87,6 @@\n ## Review Findings (2026-05-03 21:05)\n\n ### Nits\n-- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n+- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n+\n+  **Addressed:** Changed the `scrollableAncestorInDirection` docstring to read `auto` or `scroll` (dropped the stale `, or overlay` reference). The public contract now matches the `isScrollableOnAxis` implementation.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -87,6 +87,4 @@\n ## Review Findings (2026-05-03 21:05)\n\n ### Nits\n-- [x] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n-\n-  **Addressed:** Changed the `scrollableAncestorInDirection` docstring to read `auto` or `scroll` (dropped the stale `, or overlay` reference). The public contract now matches the `isScrollableOnAxis` implementation.\n\\ No newline at end of file\n+- [ ] `kanban-app/ui/src/lib/scroll-on-edge.ts:94` — The `scrollableAncestorInDirection` docstring still claims an element qualifies when its `overflow-y` / `overflow-x` is \"`auto`, `scroll`, or `overlay`\". The previous round's nit fix correctly dropped `overlay` from `isScrollableOnAxis` (line 133) and updated *that* function's docstring (lines 121–128) to call out the change, but the upstream caller's contract description was missed. Reading the public-facing docstring of the exported function now contradicts the implementation. Suggested fix: change line 94 to `\\`auto\\` or \\`scroll\\`` (drop `, or \\`overlay\\``) so the public contract matches the implementation.\n\\ No newline at end of file\n"}]]}
{"id":"01KQR7JZ3SCX30M7D1EMVWMHG6","timestamp":"2026-05-04T00:53:06.809508Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: doing\n+position_column: review\n position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n+position_column: doing\n position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n","transaction_id":null}
{"id":"01KQR7JZ3S28Q3TJ9BX2AF7746","timestamp":"2026-05-04T00:53:06.809944Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-doing\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+doing\n\\ No newline at end of file\n"}]]}
{"id":"01KQR85HHPW26591R6MADSRRGW","timestamp":"2026-05-04T01:03:15.510970Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: review\n-position_ordinal: '80'\n+position_column: done\n+position_ordinal: ffffffffffffffffffffffffffffffffff8480\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -4,8 +4,8 @@\n depends_on:\n - 01KQQSXM2PEYR1WAQ7QXW3B8ME\n - 01KQQTXDHP3XBHZ8G40AC4FG4D\n-position_column: done\n-position_ordinal: ffffffffffffffffffffffffffffffffff8480\n+position_column: review\n+position_ordinal: '80'\n project: spatial-nav\n title: 'Spatial-nav #5: scroll-on-edge for virtualized regions'\n ---\n","transaction_id":null}
{"id":"01KQR85HHQDRN7JX913ZDJS0Z3","timestamp":"2026-05-04T01:03:15.511876Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["position_column",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-review\n\\ No newline at end of file\n+done\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-done\n\\ No newline at end of file\n+review\n\\ No newline at end of file\n"}],["position_ordinal",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-80\n\\ No newline at end of file\n+ffffffffffffffffffffffffffffffffff8480\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1 +1 @@\n-ffffffffffffffffffffffffffffffffff8480\n\\ No newline at end of file\n+80\n\\ No newline at end of file\n"}]]}
{"id":"01KRE1X0BMAV1WYQQ5S80WZSHW","timestamp":"2026-05-12T12:17:01.812069Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -15,7 +15,7 @@\n\n **This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n\n-**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n+**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n\n **Contract (restated from design):**\n\n","reverse_patch":"--- original\n+++ modified\n@@ -15,7 +15,7 @@\n\n **This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n\n-**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n+**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n\n **Contract (restated from design):**\n\n","transaction_id":null}
{"id":"01KRE1X0BNERXSJG9BW2D3MBH5","timestamp":"2026-05-12T12:17:01.813564Z","entity_type":"task","entity_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","op":"update","changes":[["body",{"kind":"text_diff","forward_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n\n **This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n\n-**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n+**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n\n **Contract (restated from design):**\n\n","reverse_patch":"--- original\n+++ modified\n@@ -4,7 +4,7 @@\n\n **This component owns:** the scroll-on-edge fall-through that lets cardinal nav cross the boundary of a virtualized scroll container.\n\n-**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n+**Why it's needed:** the app uses *essential* virtualization. Off-viewport rows do not register `<FocusScope>`, so the kernel cannot find them via `geometric_pick` (component #1). When the user is on the last visible row of a virtualized list and presses Down, the kernel returns stay-put. Without this component, the user is stuck.\n\n **Contract (restated from design):**\n\n"}]]}
{"id":"01M129VGCWWQG8VKQE4XZC5Q6J","timestamp":"2026-08-27T19:06:24.540869Z","op":"update","item_id":"01KQQV1FDQXGBJ70ZRMP7AG66J","forward_patch":"--- original\n+++ modified\n@@ -71,7 +71,7 @@\n\n ## Workflow\n\n-- Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n+- Depends on **`#1` (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n #spatial-nav-redesign\n\n@@ -78,7 +78,7 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion `#2` (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n   **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n\n","reverse_patch":"--- original\n+++ modified\n@@ -71,7 +71,7 @@\n\n ## Workflow\n\n-- Depends on **`#1` (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n+- Depends on **#1 (geometric cardinal pick)** because the fallback fires on `result === focusedFq`, which only the geometric algorithm produces reliably (the current cascade also produces stay-put but for different reasons that the fallback would mishandle).\n - Use `/tdd`. Write the helper unit tests first, then the column-view end-to-end test (RED), then implement the helper and wire it into `buildNavCommands`.\n #spatial-nav-redesign\n\n@@ -78,7 +78,7 @@\n ## Review Findings (2026-05-03 19:12)\n\n ### Warnings\n-- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion `#2` (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n+- [x] `kanban-app/ui/src/components/column-view.virtualized-nav.browser.test.tsx` — Acceptance criterion #2 (\"Pressing ArrowRight at the right edge of the visible column strip scrolls the strip horizontally AND moves focus to a card in the newly-visible column\") is checked off but no end-to-end test pins it. The task description's Tests section explicitly calls for a horizontal e2e test in addition to the vertical one (\"End-to-end browser test for horizontal: similar but for the column strip\"). The unit tests in `scroll-on-edge.test.ts` cover horizontal helper math, and `runNavWithScrollOnEdge` is direction-agnostic, so the wiring is very likely to work — but a column-strip-level integration test would catch layout-specific regressions and should exist to honor the acceptance criterion. Suggested fix: add a fourth `it(...)` case that builds a multi-column board, scrolls horizontally with ArrowRight from a card in the rightmost visible column, and asserts both `scrollLeft` advanced and the kernel-simulator received two `spatial_navigate` IPCs.\n\n   **Addressed:** Added a 4th `it(\"scrolls the column strip horizontally and re-dispatches nav from a card in the rightmost visible column\")` test plus a `renderColumnStrip` helper. The test mounts one column inside a 600px-wide wrapper with the strip's intrinsic width forced to 1120px so the wrapper has horizontal scroll travel even when off-screen columns are unmounted (mirroring essential-virtualization in production). Because the kernel-simulator's stay-put surface is sensitive to virtualizer churn (off-screen registrations come and go between focus and navigate), the test installs a per-test `spatial_navigate` override that always emits a stay-put echo for `right` — exactly the case scroll-on-edge is designed to handle. Asserts: two `spatial_navigate` IPCs (initial + retry), `scrollLeft` advanced, and both directions are `right`.\n\n","transaction_id":null}
