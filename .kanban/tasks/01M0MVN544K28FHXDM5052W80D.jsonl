{"id":"01M0MVN545G9CWCJS9A284C6TB","timestamp":"2026-08-22T13:48:08.709228Z","op":"create","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -0,0 +1,50 @@\n+---\n+assignees:\n+- claude-code\n+position_column: todo\n+position_ordinal: ffeb80\n+project: swift-validator\n+title: 'swift: add the ten judgment rules we are missing (init cost, immutability, preconditions, handler naming)'\n+---\n+The Airbnb skill has about ten rules that NO tool can decide and that we do not have. These are real judgment rules. They belong in `builtin/validators/swift/rules/`.\n+\n+Source: https://swift.airbnb.tech/skill\n+\n+## The rules\n+\n+Group them into new files, or add them to the closest existing file. Suggested placement:\n+\n+NEW `initialization.md`\n+- Do no meaningful or slow work in `init()`. No database connections, no network requests, no large disk reads. Move that work to an explicit `start()`/`load()` the caller invokes.\n+- Prefer initializing properties at `init` time over deferring them. (Pairs with the IUO rule that moves to SwiftLint.)\n+- Extract complex property observers (`didSet`/`willSet`) into methods. This reduces nesting and separates side effects from the property declaration.\n+- Extract complex callback blocks into methods, for the same reason.\n+\n+NEW `immutability.md`\n+- Prefer immutable values. Build a collection with `map`/`compactMap`, not by appending to a `var` accumulator.\n+- Prefer stored `static let` or computed `static var` over mutable stored `static var`.\n+- Avoid global functions. Prefer a method inside a type.\n+\n+NEW `preconditions.md` — or fold into `error-handling.md`\n+- Validate preconditions at the start of a scope with `guard`, not `if`. This keeps the happy path unindented. (Our `optionals.md` teaches this for optional binding only; the general precondition form is missing.)\n+- Handle an unexpected but recoverable condition with `assert` plus production logging, not a crash and not silence.\n+\n+ADD to `naming-clarity.md`\n+- Name event-handling functions as past-tense sentences: `didTap`, not `handleTap`. Omit the subject when it is not needed for clarity.\n+\n+ADD to `value-semantics.md` or a new `swiftui.md`\n+- For internal SwiftUI views, declare properties `internal` so the synthesized memberwise init is usable. Keep dynamic properties (`@State`, `@StateObject`) `private`.\n+\n+## Style\n+\n+Match the existing rules in this bundle. Each bullet is a bold claim, then DON'T, then DO, with real Swift. Short sentences. Every rule that fires must be fixable — review is binary pass/fail, with no advisory tier. Do not add a rule you are not willing to block on.\n+\n+## We have no SwiftUI content anywhere\n+\n+Check this before you write: the repo has no SwiftUI guidance in any validator, partial, or rule. The one bullet above is a start, not coverage. If it grows past three bullets, make `swiftui.md` its own file.\n+\n+## Acceptance\n+\n+- Each new rule has a fixture pair proving it fires on the bad form and stays quiet on the good form.\n+- `crates/mirdan/src/builtin_validators.rs` fixture roster updated if new `.tmpl` files appear.\n+- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names.\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -1,50 +0,0 @@\n----\n-assignees:\n-- claude-code\n-position_column: todo\n-position_ordinal: ffeb80\n-project: swift-validator\n-title: 'swift: add the ten judgment rules we are missing (init cost, immutability, preconditions, handler naming)'\n----\n-The Airbnb skill has about ten rules that NO tool can decide and that we do not have. These are real judgment rules. They belong in `builtin/validators/swift/rules/`.\n-\n-Source: https://swift.airbnb.tech/skill\n-\n-## The rules\n-\n-Group them into new files, or add them to the closest existing file. Suggested placement:\n-\n-NEW `initialization.md`\n-- Do no meaningful or slow work in `init()`. No database connections, no network requests, no large disk reads. Move that work to an explicit `start()`/`load()` the caller invokes.\n-- Prefer initializing properties at `init` time over deferring them. (Pairs with the IUO rule that moves to SwiftLint.)\n-- Extract complex property observers (`didSet`/`willSet`) into methods. This reduces nesting and separates side effects from the property declaration.\n-- Extract complex callback blocks into methods, for the same reason.\n-\n-NEW `immutability.md`\n-- Prefer immutable values. Build a collection with `map`/`compactMap`, not by appending to a `var` accumulator.\n-- Prefer stored `static let` or computed `static var` over mutable stored `static var`.\n-- Avoid global functions. Prefer a method inside a type.\n-\n-NEW `preconditions.md` — or fold into `error-handling.md`\n-- Validate preconditions at the start of a scope with `guard`, not `if`. This keeps the happy path unindented. (Our `optionals.md` teaches this for optional binding only; the general precondition form is missing.)\n-- Handle an unexpected but recoverable condition with `assert` plus production logging, not a crash and not silence.\n-\n-ADD to `naming-clarity.md`\n-- Name event-handling functions as past-tense sentences: `didTap`, not `handleTap`. Omit the subject when it is not needed for clarity.\n-\n-ADD to `value-semantics.md` or a new `swiftui.md`\n-- For internal SwiftUI views, declare properties `internal` so the synthesized memberwise init is usable. Keep dynamic properties (`@State`, `@StateObject`) `private`.\n-\n-## Style\n-\n-Match the existing rules in this bundle. Each bullet is a bold claim, then DON'T, then DO, with real Swift. Short sentences. Every rule that fires must be fixable — review is binary pass/fail, with no advisory tier. Do not add a rule you are not willing to block on.\n-\n-## We have no SwiftUI content anywhere\n-\n-Check this before you write: the repo has no SwiftUI guidance in any validator, partial, or rule. The one bullet above is a start, not coverage. If it grows past three bullets, make `swiftui.md` its own file.\n-\n-## Acceptance\n-\n-- Each new rule has a fixture pair proving it fires on the bad form and stays quiet on the good form.\n-- `crates/mirdan/src/builtin_validators.rs` fixture roster updated if new `.tmpl` files appear.\n-- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0MVP9VPRX6F099193M6ESK1","timestamp":"2026-08-22T13:48:46.326303Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n\n - Each new rule has a fixture pair proving it fires on the bad form and stays quiet on the good form.\n - `crates/mirdan/src/builtin_validators.rs` fixture roster updated if new `.tmpl` files appear.\n-- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names.\n\\ No newline at end of file\n+- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names. #tool-validators\n\\ No newline at end of file\n","reverse_patch":"--- original\n+++ modified\n@@ -47,4 +47,4 @@\n\n - Each new rule has a fixture pair proving it fires on the bad form and stays quiet on the good form.\n - `crates/mirdan/src/builtin_validators.rs` fixture roster updated if new `.tmpl` files appear.\n-- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names. #tool-validators\n\\ No newline at end of file\n+- `builtin/validators/swift/VALIDATOR.md` description lists the new rule names.\n\\ No newline at end of file\n","transaction_id":null}
{"id":"01M0NATQR9Z76Z7BXBH8MX9NN0","timestamp":"2026-08-22T18:13:20.265209Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: todo\n-position_ordinal: ffeb80\n+position_column: doing\n+position_ordinal: '8280'\n project: swift-validator\n title: 'swift: add the ten judgment rules we are missing (init cost, immutability, preconditions, handler naming)'\n ---\n","reverse_patch":"--- original\n+++ modified\n@@ -1,8 +1,8 @@\n ---\n assignees:\n - claude-code\n-position_column: doing\n-position_ordinal: '8280'\n+position_column: todo\n+position_ordinal: ffeb80\n project: swift-validator\n title: 'swift: add the ten judgment rules we are missing (init cost, immutability, preconditions, handler naming)'\n ---\n","transaction_id":null}
{"id":"01M0NB806NQSX7YFPB4N7CS631","timestamp":"2026-08-22T18:20:34.901643Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -1,6 +1,29 @@\n ---\n assignees:\n - claude-code\n+comments:\n+- actor: claude-code\n+  id: 01m0nb806n374f8364gw9j7579\n+  text: |-\n+    Research done. Measured every one of the eleven proposed bullets against the shipped Swift tool gates before writing a word. swiftformat 0.62.1, swiftlint 0.65.0.\n+\n+    Method: pulled the `tool.run` block out of each shipped rule file into a runnable script, then ran `idioms-swift`, `disallowed-constructs-swift`, `function-length-swift`, `magic-numbers-swift` and `missing-docs-swift` over a DON'T/DO probe pair for each bullet. `dead-code-swift` needs a built SPM package and decides unused declarations, which is a different question.\n+\n+    Result: 22 probes, 0 findings from all four file-scope gates. The harness is live, not broken — a sanity file holding `value!` and `Optional<Int>` draws `force_unwrapping` from `disallowed-constructs-swift` and `typeSugar` from `idioms-swift`.\n+\n+    ONE of the eleven IS tool-decidable and will NOT be written as a prompt bullet:\n+\n+    - \"Keep dynamic properties (`@State`, `@StateObject`) `private`\". Measured over one probe holding `@State var counter` and `@StateObject var model` beside their private forms: swiftformat `privateStateVariables` reports both bad lines and stays silent on the good ones; swiftlint `private_swiftui_state` reports the same two lines with `SwiftUI state properties should be private`. Neither rule stands in a shipped roster today. `builtin/validators/swift/VALIDATOR.md` states \"If a tool can decide it, the tool owns it\", so the prompt bullet keeps the memberwise-init half alone and names the dynamic-property half as a carve-out. A card follows for the roster.\n+\n+    Two more bullets are SPLIT, and each says which part the tool misses:\n+\n+    - \"Set every stored property at `init` time\" — swiftlint `implicitly_unwrapped_optional` owns the `Type!` spelling. Measured: it says nothing about `private var session: Session?` a later `configure()` assigns. The bullet reads that shape.\n+    - \"A callback closure that branches names a method\" — swiftlint `closure_body_length` owns the LENGTH at 250 lines. Measured: silent on a 10-line branching closure.\n+\n+    The autocorrect lesson from ^qs32yvp applies to the immutability bullet. Measured: `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`. The tool's own fix LANDS ON an accumulator loop, so the DON'T names the `for` + `append` shape, not the `forEach` shape it started from.\n+\n+    swiftlint 0.65.0 knows no `early_exit` rule — `swiftlint rules early_exit` answers `Error: No rule with identifier: early_exit`. The guard-precondition bullet has no tool owner at all.\n+  timestamp: 2026-08-22T18:20:34.901382+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -1,29 +1,6 @@\n ---\n assignees:\n - claude-code\n-comments:\n-- actor: claude-code\n-  id: 01m0nb806n374f8364gw9j7579\n-  text: |-\n-    Research done. Measured every one of the eleven proposed bullets against the shipped Swift tool gates before writing a word. swiftformat 0.62.1, swiftlint 0.65.0.\n-\n-    Method: pulled the `tool.run` block out of each shipped rule file into a runnable script, then ran `idioms-swift`, `disallowed-constructs-swift`, `function-length-swift`, `magic-numbers-swift` and `missing-docs-swift` over a DON'T/DO probe pair for each bullet. `dead-code-swift` needs a built SPM package and decides unused declarations, which is a different question.\n-\n-    Result: 22 probes, 0 findings from all four file-scope gates. The harness is live, not broken — a sanity file holding `value!` and `Optional<Int>` draws `force_unwrapping` from `disallowed-constructs-swift` and `typeSugar` from `idioms-swift`.\n-\n-    ONE of the eleven IS tool-decidable and will NOT be written as a prompt bullet:\n-\n-    - \"Keep dynamic properties (`@State`, `@StateObject`) `private`\". Measured over one probe holding `@State var counter` and `@StateObject var model` beside their private forms: swiftformat `privateStateVariables` reports both bad lines and stays silent on the good ones; swiftlint `private_swiftui_state` reports the same two lines with `SwiftUI state properties should be private`. Neither rule stands in a shipped roster today. `builtin/validators/swift/VALIDATOR.md` states \"If a tool can decide it, the tool owns it\", so the prompt bullet keeps the memberwise-init half alone and names the dynamic-property half as a carve-out. A card follows for the roster.\n-\n-    Two more bullets are SPLIT, and each says which part the tool misses:\n-\n-    - \"Set every stored property at `init` time\" — swiftlint `implicitly_unwrapped_optional` owns the `Type!` spelling. Measured: it says nothing about `private var session: Session?` a later `configure()` assigns. The bullet reads that shape.\n-    - \"A callback closure that branches names a method\" — swiftlint `closure_body_length` owns the LENGTH at 250 lines. Measured: silent on a 10-line branching closure.\n-\n-    The autocorrect lesson from ^qs32yvp applies to the immutability bullet. Measured: `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`. The tool's own fix LANDS ON an accumulator loop, so the DON'T names the `for` + `append` shape, not the `forEach` shape it started from.\n-\n-    swiftlint 0.65.0 knows no `early_exit` rule — `swiftlint rules early_exit` answers `Error: No rule with identifier: early_exit`. The guard-precondition bullet has no tool owner at all.\n-  timestamp: 2026-08-22T18:20:34.901382+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0NBX4CA2994R5BVP6WSKP81","timestamp":"2026-08-22T18:32:07.306859Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -24,6 +24,55 @@\n\n     swiftlint 0.65.0 knows no `early_exit` rule — `swiftlint rules early_exit` answers `Error: No rule with identifier: early_exit`. The guard-precondition bullet has no tool owner at all.\n   timestamp: 2026-08-22T18:20:34.901382+00:00\n+- actor: claude-code\n+  id: 01m0nbx4ca1c0jjd0e7pr2zqx6\n+  text: |-\n+    Implementation landed.\n+\n+    NEW `builtin/validators/swift/rules/initialization.md` — 4 bullets: `init` does no slow work; `init` sets every stored property (splits with swiftlint `implicitly_unwrapped_optional`); a property observer that branches calls a named method; a callback closure that branches calls a named method (splits with swiftlint `closure_body_length` at 250 lines).\n+\n+    NEW `builtin/validators/swift/rules/immutability.md` — 3 bullets: build a collection with `map`/`compactMap`, not a `var` accumulator; a `static` member is a `let` or a computed `var`; a function belongs to a type.\n+\n+    NEW `builtin/validators/swift/rules/preconditions.md` — 2 bullets: validate a precondition with `guard` at the head of a production scope; an unexpected but recoverable condition gets `assertionFailure` AND a production log.\n+\n+    ADDED to `naming-clarity.md` — an event-handling method reads as a past-tense sentence.\n+\n+    ADDED to `access-control.md` — a SwiftUI view keeps its input properties `internal` and its dynamic properties `private`. PLACEMENT: this went to `access-control.md`, not to `value-semantics.md` and not to a new `swiftui.md`. It IS an access-control requirement, `value-semantics.md` is about struct-versus-class, and one SwiftUI bullet is under the three-bullet threshold the card set for a file of its own.\n+\n+    The accumulator bullet names the loop in BOTH shapes, because of the autocorrect lesson from ^qs32yvp: the `where` form and the nested-`if` form. Measured, `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`, so the nested-`if` accumulator is where the tool's own fix LANDS. A bullet that named only the `forEach` shape would walk the author into a hole.\n+\n+    ## The fixture pair, and why it is not a `.tmpl` pair\n+\n+    `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism — \"Each tool rule ships two fixture files in the set's `fixtures/` directory\" — and doctor materializes them to run a tool over them. A prompt rule has no tool to run, so no `.tmpl` file appears and `crates/mirdan/src/builtin_validators.rs` needs no edit. Checked: no roster in that file, or in any other Rust file, names a Swift prompt rule.\n+\n+    The measurable half of the contract IS shipped, as `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `shipped.rs`. It carries a DON'T probe and a DO probe for each of the five rules, and two tests:\n+\n+    - `every_swift_judgment_rule_states_the_forms_its_probes_hold` — 26 forms, each held to standing in the body of the prompt rule that states it AND in one of the two probes. Without it the probes would measure a house style nothing ships.\n+    - `no_shipped_swift_gate_decides_a_swift_judgment_rule` — drives the shipped script of all five file-scope Swift gates over both probes of each rule. 10 probes, 50 runs, 0 findings. The DON'T half proves ONE owner; the DO half proves the prompt rule does not walk the author into a tool finding.\n+\n+    RED verified by hand for both, then reverted: a form spelled `func neverStatedAnywhere()` fails the first; a force unwrap written into the naming-clarity DO probe fails the second with `` `disallowed-constructs-swift` reports the shape `naming-clarity.md` ASKS for ... the run reported [\"force_unwrapping\"] ``.\n+\n+    ## Token cost\n+\n+    Measured with `tiktoken` `cl100k_base` over the shipped files:\n+\n+    | the file | tokens |\n+    |---|---|\n+    | `initialization.md` (new) | 542 |\n+    | `immutability.md` (new) | 457 |\n+    | `preconditions.md` (new) | 356 |\n+    | `naming-clarity.md` (delta) | 93 |\n+    | `access-control.md` (delta) | 156 |\n+    | rule bodies, total | **1604** |\n+    | `VALIDATOR.md` (delta) | 216 |\n+    | everything, total | **1820** |\n+\n+    Set against the 217 tokens the supersede card saved: the rule bodies add 7.4 times that. Eleven bullets is what the cost buys.\n+\n+    ## Follow-up card\n+\n+    ^w54rk97 — give the SwiftUI private-state half a tool owner. Both swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` decide it cleanly and neither stands in a shipped roster; the card carries the measurement, the roster edit, and the note that `no_shipped_swift_gate_decides_a_swift_judgment_rule` will fail loudly the moment the roster takes the rule, which is how the two-owner state is caught.\n+  timestamp: 2026-08-22T18:32:07.306192+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -24,55 +24,6 @@\n\n     swiftlint 0.65.0 knows no `early_exit` rule — `swiftlint rules early_exit` answers `Error: No rule with identifier: early_exit`. The guard-precondition bullet has no tool owner at all.\n   timestamp: 2026-08-22T18:20:34.901382+00:00\n-- actor: claude-code\n-  id: 01m0nbx4ca1c0jjd0e7pr2zqx6\n-  text: |-\n-    Implementation landed.\n-\n-    NEW `builtin/validators/swift/rules/initialization.md` — 4 bullets: `init` does no slow work; `init` sets every stored property (splits with swiftlint `implicitly_unwrapped_optional`); a property observer that branches calls a named method; a callback closure that branches calls a named method (splits with swiftlint `closure_body_length` at 250 lines).\n-\n-    NEW `builtin/validators/swift/rules/immutability.md` — 3 bullets: build a collection with `map`/`compactMap`, not a `var` accumulator; a `static` member is a `let` or a computed `var`; a function belongs to a type.\n-\n-    NEW `builtin/validators/swift/rules/preconditions.md` — 2 bullets: validate a precondition with `guard` at the head of a production scope; an unexpected but recoverable condition gets `assertionFailure` AND a production log.\n-\n-    ADDED to `naming-clarity.md` — an event-handling method reads as a past-tense sentence.\n-\n-    ADDED to `access-control.md` — a SwiftUI view keeps its input properties `internal` and its dynamic properties `private`. PLACEMENT: this went to `access-control.md`, not to `value-semantics.md` and not to a new `swiftui.md`. It IS an access-control requirement, `value-semantics.md` is about struct-versus-class, and one SwiftUI bullet is under the three-bullet threshold the card set for a file of its own.\n-\n-    The accumulator bullet names the loop in BOTH shapes, because of the autocorrect lesson from ^qs32yvp: the `where` form and the nested-`if` form. Measured, `swiftformat --rules preferForLoop --single-line-for-each convert` rewrites `users.forEach { if $0.isActive { names.append($0.name) } }` into `for user in users { if user.isActive { names.append(user.name) } }`, so the nested-`if` accumulator is where the tool's own fix LANDS. A bullet that named only the `forEach` shape would walk the author into a hole.\n-\n-    ## The fixture pair, and why it is not a `.tmpl` pair\n-\n-    `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism — \"Each tool rule ships two fixture files in the set's `fixtures/` directory\" — and doctor materializes them to run a tool over them. A prompt rule has no tool to run, so no `.tmpl` file appears and `crates/mirdan/src/builtin_validators.rs` needs no edit. Checked: no roster in that file, or in any other Rust file, names a Swift prompt rule.\n-\n-    The measurable half of the contract IS shipped, as `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `shipped.rs`. It carries a DON'T probe and a DO probe for each of the five rules, and two tests:\n-\n-    - `every_swift_judgment_rule_states_the_forms_its_probes_hold` — 26 forms, each held to standing in the body of the prompt rule that states it AND in one of the two probes. Without it the probes would measure a house style nothing ships.\n-    - `no_shipped_swift_gate_decides_a_swift_judgment_rule` — drives the shipped script of all five file-scope Swift gates over both probes of each rule. 10 probes, 50 runs, 0 findings. The DON'T half proves ONE owner; the DO half proves the prompt rule does not walk the author into a tool finding.\n-\n-    RED verified by hand for both, then reverted: a form spelled `func neverStatedAnywhere()` fails the first; a force unwrap written into the naming-clarity DO probe fails the second with `` `disallowed-constructs-swift` reports the shape `naming-clarity.md` ASKS for ... the run reported [\"force_unwrapping\"] ``.\n-\n-    ## Token cost\n-\n-    Measured with `tiktoken` `cl100k_base` over the shipped files:\n-\n-    | the file | tokens |\n-    |---|---|\n-    | `initialization.md` (new) | 542 |\n-    | `immutability.md` (new) | 457 |\n-    | `preconditions.md` (new) | 356 |\n-    | `naming-clarity.md` (delta) | 93 |\n-    | `access-control.md` (delta) | 156 |\n-    | rule bodies, total | **1604** |\n-    | `VALIDATOR.md` (delta) | 216 |\n-    | everything, total | **1820** |\n-\n-    Set against the 217 tokens the supersede card saved: the rule bodies add 7.4 times that. Eleven bullets is what the cost buys.\n-\n-    ## Follow-up card\n-\n-    ^w54rk97 — give the SwiftUI private-state half a tool owner. Both swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` decide it cleanly and neither stands in a shipped roster; the card carries the measurement, the roster edit, and the note that `no_shipped_swift_gate_decides_a_swift_judgment_rule` will fail loudly the moment the roster takes the rule, which is how the two-owner state is caught.\n-  timestamp: 2026-08-22T18:32:07.306192+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0NC07KC55D9S2SPGQQSJFM3","timestamp":"2026-08-22T18:33:48.908470Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -73,6 +73,19 @@\n\n     ^w54rk97 — give the SwiftUI private-state half a tool owner. Both swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` decide it cleanly and neither stands in a shipped roster; the card carries the measurement, the roster edit, and the note that `no_shipped_swift_gate_decides_a_swift_judgment_rule` will fail loudly the moment the roster takes the rule, which is how the two-owner state is caught.\n   timestamp: 2026-08-22T18:32:07.306192+00:00\n+- actor: claude-code\n+  id: 01m0nc07kc9grde2h5aejkaz0h\n+  text: |-\n+    ### implement — changed\n+    - evidence: 9 files. NEW `builtin/validators/swift/rules/initialization.md`, `builtin/validators/swift/rules/immutability.md`, `builtin/validators/swift/rules/preconditions.md`; EDITED `builtin/validators/swift/rules/naming-clarity.md`, `builtin/validators/swift/rules/access-control.md`, `builtin/validators/swift/VALIDATOR.md`; NEW `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`; NEW card `.kanban/tasks/01M0NBVN6Z77DS4FZBXW54RK97.md`.\n+    - eleven bullets: 4 initialization, 3 immutability, 2 preconditions, 1 naming-clarity, 1 access-control.\n+    - tool check: every bullet measured against the five shipped file-scope Swift gates before it was written. 10 probes, 50 runs, 0 findings. ONE bullet-half rejected as tool-decidable — the SwiftUI dynamic-property `private` — and card ^w54rk97 carries the roster edit.\n+    - token cost: 1604 tokens of rule body, 216 more of VALIDATOR.md prose, 1820 in all, measured with `tiktoken` `cl100k_base`. The supersede card saved 217.\n+    - fixture pair: realized as measured probe pairs, NOT `.tmpl` files. `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism (\"Each tool rule ships two fixture files\"), and a prompt rule has no tool for doctor to run over one. `no_shipped_swift_gate_decides_a_swift_judgment_rule` holds each pair. No `.tmpl` file appears, so `crates/mirdan/src/builtin_validators.rs` needs no edit; checked, and no roster in any Rust file names a Swift prompt rule.\n+    - tests: `cargo nextest run` at the repo root — 14186 tests run, 14186 passed, 0 failed, 0 skipped. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.\n+    - RED verified for both new tests, then reverted.\n+    - next: `/review`.\n+  timestamp: 2026-08-22T18:33:48.908100+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -73,19 +73,6 @@\n\n     ^w54rk97 — give the SwiftUI private-state half a tool owner. Both swiftformat `privateStateVariables` and swiftlint `private_swiftui_state` decide it cleanly and neither stands in a shipped roster; the card carries the measurement, the roster edit, and the note that `no_shipped_swift_gate_decides_a_swift_judgment_rule` will fail loudly the moment the roster takes the rule, which is how the two-owner state is caught.\n   timestamp: 2026-08-22T18:32:07.306192+00:00\n-- actor: claude-code\n-  id: 01m0nc07kc9grde2h5aejkaz0h\n-  text: |-\n-    ### implement — changed\n-    - evidence: 9 files. NEW `builtin/validators/swift/rules/initialization.md`, `builtin/validators/swift/rules/immutability.md`, `builtin/validators/swift/rules/preconditions.md`; EDITED `builtin/validators/swift/rules/naming-clarity.md`, `builtin/validators/swift/rules/access-control.md`, `builtin/validators/swift/VALIDATOR.md`; NEW `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped/swift_judgment_rules.rs`, registered in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`; NEW card `.kanban/tasks/01M0NBVN6Z77DS4FZBXW54RK97.md`.\n-    - eleven bullets: 4 initialization, 3 immutability, 2 preconditions, 1 naming-clarity, 1 access-control.\n-    - tool check: every bullet measured against the five shipped file-scope Swift gates before it was written. 10 probes, 50 runs, 0 findings. ONE bullet-half rejected as tool-decidable — the SwiftUI dynamic-property `private` — and card ^w54rk97 carries the roster edit.\n-    - token cost: 1604 tokens of rule body, 216 more of VALIDATOR.md prose, 1820 in all, measured with `tiktoken` `cl100k_base`. The supersede card saved 217.\n-    - fixture pair: realized as measured probe pairs, NOT `.tmpl` files. `builtin/validators/README.md` defines a fixture as a TOOL-rule mechanism (\"Each tool rule ships two fixture files\"), and a prompt rule has no tool for doctor to run over one. `no_shipped_swift_gate_decides_a_swift_judgment_rule` holds each pair. No `.tmpl` file appears, so `crates/mirdan/src/builtin_validators.rs` needs no edit; checked, and no roster in any Rust file names a Swift prompt rule.\n-    - tests: `cargo nextest run` at the repo root — 14186 tests run, 14186 passed, 0 failed, 0 skipped. `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean.\n-    - RED verified for both new tests, then reverted.\n-    - next: `/review`.\n-  timestamp: 2026-08-22T18:33:48.908100+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
{"id":"01M0NCAD2JTV51F5B83D19P2R3","timestamp":"2026-08-22T18:39:22.194210Z","op":"update","item_id":"01M0MVN544K28FHXDM5052W80D","forward_patch":"--- original\n+++ modified\n@@ -86,6 +86,25 @@\n     - RED verified for both new tests, then reverted.\n     - next: `/review`.\n   timestamp: 2026-08-22T18:33:48.908100+00:00\n+- actor: claude-code\n+  id: 01m0ncad2hzacp4faawa9byw4c\n+  text: |\n+    ### test — green\n+    - evidence: `cargo nextest run --workspace` — 14186 tests run, 14186 passed, 0 failed, 0 skipped (29 slow, 1 leaky). `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean (0 warnings).\n+    - Baseline was 14184, this run is 14186 (+2), matching the implementer's claim exactly. No drop.\n+\n+    **Confirmation 1 — new test file registered and running.**\n+    `mod swift_judgment_rules;` is present in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`. Targeted run `cargo nextest run --workspace -E 'test(swift_judgment_rules)'` shows both tests executing and passing:\n+    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::every_swift_judgment_rule_states_the_forms_its_probes_hold` — PASS\n+    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::no_shipped_swift_gate_decides_a_swift_judgment_rule` — PASS\n+    These 2 tests account for the +2 over baseline.\n+\n+    **Confirmation 2 — no `.tmpl` fixtures, mirdan roster unchanged.**\n+    `git status --porcelain` shows no new `.tmpl` files (only new `.md` rule files and the new test file). `git diff HEAD -- crates/mirdan/src/builtin_validators.rs` is empty — the file is genuinely unchanged. Its coverage-guard tests still pass: `cargo nextest run --package mirdan -E 'test(builtin_validator) or test(fixture)'` — 9/9 passed, including `test_every_builtin_validator_file_is_embedded` and `test_each_set_has_a_manifest`, which would catch a stale roster against the new on-disk `.md` files.\n+\n+    **Confirmation 3 — new rule files are loaded at runtime and part of the `swift` set.**\n+    `swift_prompt_rule_body()` in `shipped.rs` calls `loader.get_ruleset(SWIFT_VALIDATOR_SET)` (`SWIFT_VALIDATOR_SET = \"swift\"`) built by `builtin_loader()` (`crates/swissarmyhammer-validators/src/review/test_support.rs`), which calls `crate::load_builtins(&mut loader)` — a real disk read via `builtin_validators_dir()`, not a compiled-in fixture. It then does `.rules.iter().find(|shipped| shipped.name == rule)`, panicking `\"the swift set must carry a {rule} prompt rule\"` if missing. `every_swift_judgment_rule_states_the_forms_its_probes_hold` calls this for `initialization`, `immutability`, and `preconditions` (plus `naming-clarity`, `access-control`) and passed, proving all three new rule files load into the `swift` set at runtime. `builtin/validators/swift/VALIDATOR.md` also lists `initialization`, `immutability`, `preconditions` by name in its rule listing.\n+  timestamp: 2026-08-22T18:39:22.193815+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","reverse_patch":"--- original\n+++ modified\n@@ -86,25 +86,6 @@\n     - RED verified for both new tests, then reverted.\n     - next: `/review`.\n   timestamp: 2026-08-22T18:33:48.908100+00:00\n-- actor: claude-code\n-  id: 01m0ncad2hzacp4faawa9byw4c\n-  text: |\n-    ### test — green\n-    - evidence: `cargo nextest run --workspace` — 14186 tests run, 14186 passed, 0 failed, 0 skipped (29 slow, 1 leaky). `cargo fmt --all -- --check` clean. `cargo clippy --workspace --all-targets -- -D warnings` clean (0 warnings).\n-    - Baseline was 14184, this run is 14186 (+2), matching the implementer's claim exactly. No drop.\n-\n-    **Confirmation 1 — new test file registered and running.**\n-    `mod swift_judgment_rules;` is present in `crates/swissarmyhammer-validators/src/review/tool_rules/tests/shipped.rs`. Targeted run `cargo nextest run --workspace -E 'test(swift_judgment_rules)'` shows both tests executing and passing:\n-    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::every_swift_judgment_rule_states_the_forms_its_probes_hold` — PASS\n-    - `swissarmyhammer-validators review::tool_rules::tests::shipped::swift_judgment_rules::no_shipped_swift_gate_decides_a_swift_judgment_rule` — PASS\n-    These 2 tests account for the +2 over baseline.\n-\n-    **Confirmation 2 — no `.tmpl` fixtures, mirdan roster unchanged.**\n-    `git status --porcelain` shows no new `.tmpl` files (only new `.md` rule files and the new test file). `git diff HEAD -- crates/mirdan/src/builtin_validators.rs` is empty — the file is genuinely unchanged. Its coverage-guard tests still pass: `cargo nextest run --package mirdan -E 'test(builtin_validator) or test(fixture)'` — 9/9 passed, including `test_every_builtin_validator_file_is_embedded` and `test_each_set_has_a_manifest`, which would catch a stale roster against the new on-disk `.md` files.\n-\n-    **Confirmation 3 — new rule files are loaded at runtime and part of the `swift` set.**\n-    `swift_prompt_rule_body()` in `shipped.rs` calls `loader.get_ruleset(SWIFT_VALIDATOR_SET)` (`SWIFT_VALIDATOR_SET = \"swift\"`) built by `builtin_loader()` (`crates/swissarmyhammer-validators/src/review/test_support.rs`), which calls `crate::load_builtins(&mut loader)` — a real disk read via `builtin_validators_dir()`, not a compiled-in fixture. It then does `.rules.iter().find(|shipped| shipped.name == rule)`, panicking `\"the swift set must carry a {rule} prompt rule\"` if missing. `every_swift_judgment_rule_states_the_forms_its_probes_hold` calls this for `initialization`, `immutability`, and `preconditions` (plus `naming-clarity`, `access-control`) and passed, proving all three new rule files load into the `swift` set at runtime. `builtin/validators/swift/VALIDATOR.md` also lists `initialization`, `immutability`, `preconditions` by name in its rule listing.\n-  timestamp: 2026-08-22T18:39:22.193815+00:00\n position_column: doing\n position_ordinal: '8280'\n project: swift-validator\n","transaction_id":null}
